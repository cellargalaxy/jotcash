# 对话1 · 前端全功能页面（mock 验收版）

## 一、依据来源

| 事实 | 出处 |
| ---- | ---- |
| 静态资源以 `embed.FS` 整目录暴露 | `static/static.go:5` 的 `//go:embed *` |
| 挂载点 `/static`，带 `max-age=86400` 缓存头 | `handler/handler.go:41-43`、`util.PathStatic`（`go_common/util/gin.go:23`） |
| 8 个已实现接口的路径 | `config/handler.go:8-17` |
| 上传字段名统一为 `file` | `config/handler.go:19-22`（`ExpenseFileKey`/`ImportFileKey`） |
| 请求凭据：前端自签 jwt，HS256，secret = `sha256(后端口令)` | `go_common/util/codec.go:93-101`、`handler/handler.go:52`、`handler/handler_test.go:107-125` |
| jwt 载荷含「前端口令 + 记账币种」 | `model/claims.go:9-13` |
| 响应体形如 `{code,msg,data:{object,count}}`，HTTP 状态恒 200 | `go_common/model/http.go:31-45`、`go_common/util/gin.go:31-41` |
| 明细字段与 JSON 键名（21 项） | `model/expense.go:32-56` |
| 查询条件字段 | `model/ExpenseInquiry`/`OperationLogInquiry`/`FileMetaInquiry` |
| 排序白名单（越界直接报错） | `rdb/expense.go:13-22`、`rdb/operation_log.go:11-16`、`rdb/file_meta.go:11-18`、`rdb/inquiry.go:29-38` |
| 分页：page 从 1 起，pageSize 默认 20、上限 200 | `service/repo/inquiry.go:12-25`、`rdb/inquiry.go:19-27` |
| 删除筛选三态枚举 | `model/expense.go:12-16` `DeletedNo/DeletedAll/DeletedOnly` |
| 审计类型 8 类、结果 3 类 | `model/operation_log.go:10-25` |
| CSV 列契约（**11 列、顺序固定、表头须完全相等**） | `service/expense/base_csv/base_csv.go:16-28`、`checkColumns` 的 `slices.Equal` |
| CSV 行级规则（日期格式、汇率填了就必须填记账币种、摊分月数≥1） | `base_csv.go:78-120` |
| 记账金额 = 支出金额 × 折算汇率，按 `AmountScale` 取整；摊分起止月派生 | `service/expense/expense.go:76-84` |
| 口令强度：≥12 位、不得纯数字/纯字母、不得含空格 | `tool/token.go:60-83` |
| 页面功能范围（8 域 32 项、默认 10 列、全字段顺序、去重三要素） | `doc/decisions/记账系统功能与模型.md` §二、§四.1、§8.3 |
| 命名/注释/组织风格 | `doc/decisions/编码规范.md` §四、§六 |

### 资料冲突（列出并给出采用依据）

1. **CSV 列数：文档 8 列 vs 代码 11 列。** 需求文档 E-1 写「契约仅 8 个业务列」且不含记账币种；`base_csv.go:16-28` 实际是 11 列，多出 `记账币种`、`摊分月数`（`支出类型` 两边都有）。**采用代码**：`checkColumns` 用 `slices.Equal` 做全等比较，少一列连解析器都不认领（`Support` 返回 false），按文档写模板会直接「文件格式无法识别」。前端模板与单行新增均按 11 列生成。
2. **记账币种来源：文档「新增行取请求携带值」vs 代码「CSV 列优先」。** `expense.go:62-64` 是 `if object.AccountingCurrency == ""` 才取 jwt 里的值，即 CSV 列非空时以 CSV 为准。**采用代码**：模板保留该列，前端单行新增时留空，让 jwt 携带值生效，与文档语义一致。

## 二、目标与边界

- **交付物**：`static/` 下一套零构建、纯静态的前端，覆盖需求文档 8 域的全部页面，数据全走前端 mock，可直接 `go run .` 后在 `/static/` 打开验收。
- **不做**：与服务端联调（本轮明确不要求）、任何后端代码改动、月度统计（G-2 本轮无消费方）。
- **硬约束**：
  1. 一切资源走**相对路径**——页面在 `<前缀>/static/`，接口按 `../api/` 拼，反代加前缀后自动跟随，全仓无以 `/` 开头的 URL。
  2. 三方库**落本地**，不走 CDN；已下载 Bootstrap 5.3.3（CSS + bundle JS）、decimal.js 10.4.3，并剥掉 `sourceMappingURL` 注释避免 404 噪声。
  3. 标识符英文、注释与文案中文；同一概念全仓一个名字（`编码规范` §4.2/§4.3）。

## 三、方案

### 3.1 页面切分（4 页 + 1 道闸）

| 路由 | 页面 | 承载的功能项 |
| ---- | ---- | ------------ |
| （覆盖层） | **解锁** | A-3 逐请求携带口令、A-4 口令探针、A-2 初始口令说明；口令存 sessionStorage |
| `#/expense` | **明细** | E-2 三入口新增、E-3 批次核实视图、E-4 内联编辑、E-5 批量软删除、E-6 疑似重复高亮、E-7 查询与列选择、F-5 记账币种表头提示、§8.5 复制已删除行 |
| `#/operation-log` | **审计** | C-3 只读列表与筛选、跳转按审计ID 筛选的明细/文件 |
| `#/file-meta` | **文件** | D-4 列表与下载、D-3 完整性校验提示 |
| `#/setting` | **设置** | F-2/F-4 记账币种与切换、A-5 更换口令、B-1 导出、B-2 导入、H-2 口令不落日志的说明 |

选 **单页 + hash 路由**而非多份 html：解锁闸、导航壳、口令与记账币种的会话态只需写一遍；hash 不参与请求路径，天然不受反代前缀影响。

### 3.2 模块职责

| 文件 | 职责 | 输入 → 输出 |
| ---- | ---- | ----------- |
| `index.html` | 外壳：导航、解锁覆盖层、页面容器、模态框宿主 | — |
| `js/config.js` | 常量：接口路径、CSV 列契约、币种表、审计枚举、默认列、mock 开关 | 纯声明 |
| `js/util.js` | DOM 构建、日期/金额/文件大小格式化、CSV 编解码、下载、提示条 | — |
| `js/store.js` | sessionStorage：后端口令/前端口令/记账币种/列偏好 | 关标签页即失效 |
| `js/api.js` | 统一入口：签 jwt → `fetch` → 拆 `{code,msg,data}`；`mock` 开关时转发给 `mock.js` | inquiry 对象 → `{object,count}` |
| `js/mock.js` | 内存库：明细/审计/文件三张表 + 与后端同语义的筛选、排序、分页、乐观锁、CSV 解析 | 同 `api.js` 契约 |
| `js/page_*.js` | 每页一个 `render(container, query)`，自带筛选区、表格、交互 | — |
| `js/app.js` | 路由分发、导航高亮、解锁闸守卫 | — |

**api 与 mock 是一对镜像**（`编码规范` §二·第三层）：同一组函数签名、同一种返回形态、同一套错误语义，切换开关不改调用方一行代码。

### 3.3 关键逻辑

- **疑似重复分组**：以 `支出日期|支出金额|支出币种` 为键分组（§8.3），组内行数 >1 即整组加框高亮，同组同色、相邻组换色；新增、编辑、删除后重新分组。
- **核实视图（E-3）**：`#/expense?operation_id=X&verify=1` → 先取该批次的三要素集合，再用集合反查全库未删除明细，合并展示。
- **内联编辑（E-4）**：行内切换为输入态，仅 10 个可手编字段可改；记账金额随支出金额/汇率实时重算（decimal.js，按 `AmountScale=2` 取整）；支出币种 = 记账币种时汇率锁死为 1（§8.1）；保存带 `version` 乐观锁，冲突文案「数据已落后，请刷新页面重新加载」。
- **新增一行（E-2①）**：草稿行 → 按 11 列拼一份仅 1 行的 CSV → 走入库接口，与上传真实 CSV 同一条链路。
- **批量删除（E-5）**：勾选当前页 / 全选当前筛选结果全集；删除前提示「本次将删除 N 笔」；已删除行不可勾选。
- **记账币种提示（F-5）**：表头旁列出全库记账币种集合，多于一种或与当前选择不一致时挂黄条并给出切换入口。

### 3.4 后端尚未实现、本轮只有 mock 的能力（待确认）

| 能力 | 需求出处 | 现状 |
| ---- | -------- | ---- |
| 单行编辑（乐观锁） | E-4 | 无接口；`rdb.NewExpenseUpdateHandler` 已具备乐观锁，缺 service/handler 与路径 |
| 记账币种切换 | F-4 | 无接口；`ExpenseInquiry.AccountingCurrencyNot` 已为它预留 |
| 文件下载 + 哈希校验 | D-3/D-4 | 无接口；`file_meta/select` 只出元数据 |
| 支出类型候选（`distinct`）、全库记账币种集合 | E-7、F-5 | 无接口，且无法由现有接口推导 |

**处置**：`api.js` 的真实分支对这四项直接抛「接口后端未实现」的中文错误，**不臆造路径**；mock 分支给出完整实现，供本轮验收页面交互。路径待你定后再补。

## 四、实施步骤

1. 下载并剥离三方库 → `static/lib/`。
2. `config.js` / `util.js` / `store.js` 三个无业务依赖的底座先行（`编码规范` §1.2 边缘先行）。
3. `mock.js` 与 `api.js` 成对落地。
4. 四个页面模块，明细页最后写（依赖最多）。
5. `app.js` 路由收口，`index.html` 外壳与 `css/app.css` 收尾。
6. 启动实跑验收。

## 五、验证说明

见文末「验证与交付」一节。

---

## 六、实施结果

### 6.1 交付清单（17 个新增文件，无一行后端改动）

```
static/
├── index.html                    外壳：导航 + 解锁闸 + 页面容器 + toast 宿主
├── css/app.css                   表格密度、疑似重复高亮、已删除态、编辑器样式
├── js/config.js                  接口路径 / CSV 契约 / 明细 21 字段 / 枚举 / 排序白名单 / 币种
├── js/util.js                    DOM 构建、时间与金额格式化、CSV 编解码、下载、toast、二次确认
├── js/store.js                   sessionStorage 口令与记账币种，localStorage 列偏好
├── js/api.js                     jwt 自签 + fetch + 响应拆包；mock 开关在这一层分流
├── js/mock.js                    内存库：三张表 + 与后端同语义的筛选/排序/分页/乐观锁/CSV 解析
├── js/component.js               筛选格、分页条、币种控件、多选组、按类型渲染单元格
├── js/page_unlock.js             解锁闸
├── js/page_expense.js            明细页（最大，含编辑器、核实视图、批量删除、列设置）
├── js/page_operation_log.js      审计页
├── js/page_file_meta.js          文件页
├── js/page_setting.js            设置页
├── js/app.js                     hash 路由 + 解锁守卫
└── lib/                          bootstrap 5.3.3（CSS 227K + bundle JS 79K）、decimal.js 10.4.3（32K）
```

三方库合计 340K，已剥掉 `sourceMappingURL` 注释（否则每次打开控制台都会去请求不存在的 `.map`，在反代前缀下必然 404）。

### 6.2 页面、功能块与布局

**页面数：4 个路由页 + 1 道解锁闸。** 选单页 hash 路由的理由见 §3.1。

| # | 页面 | 自上而下的功能块 | 覆盖的功能项 |
| - | ---- | ---------------- | ------------ |
| 0 | **解锁闸**（覆盖整页，未解锁时导航不渲染） | 后端口令（带明文开关）→ 前端口令 → 记账币种 → 解锁按钮 → 三条风险须知 | A-2 / A-3 / A-4 / A-6 |
| 1 | **明细** `#/expense` | ① 可折叠筛选卡（16 个条件，2~3 行栅格）② 记账币种黄色提示条（条件满足才出现，右端直接挂切换按钮）③ 工具条（左：新增一行 / 上传 CSV / 下载模板 / 批量删除(N) / 全选筛选结果并删除；右：表头设置 / 刷新）④ 表格（勾选列 + 数据列 + 操作列）⑤ 分页条 ⑥ 脚注说明 | E-1~E-7、F-5、§8.5 复制 |
| 2 | **审计** `#/operation-log` | ① 筛选卡（审计ID / 摘要 / 时间区间 / 排序 + 操作类型 8 复选 + 操作结果 3 复选）② 表格（9 列，变更内容点开看 JSON）③ 分页 ④ 「审计只留痕」脚注 | C-1 / C-2 / C-3 |
| 3 | **文件** `#/file-meta` | ① 筛选卡（文件名 / 文件ID / 审计ID / 时间区间 / 排序）② 表格（哈希截断展示、悬停看全量）③ 分页 | D-1~D-4 |
| 4 | **设置** `#/setting` | 四张卡片竖排：记账口径（本会话口径 + F-4 切换）/ 更换口令 / 数据库导入导出 / 会话与运行信息 | A-5、B-1~B-3、F-2、F-4、H-2 |

**明细页的表格内布局是全系统唯一需要特别设计的地方**，三条决定：

1. **编辑器是「行下展开的整行区域」，不是「单元格变输入框」。** 单元格内联编辑有个绕不过去的洞：可手编字段有 10 个，而默认表头只有 10 列且用户可以隐藏任意列——汇率列一旦被隐藏，这一行就永远改不了汇率。行下展开固定渲染全部 10 个可编辑字段，与列显隐彻底解耦。
2. **新增与编辑共用同一个编辑器**（`expenseEditor`）。两者的字段集、校验、派生预览完全一致，差别只有出口：新增拼一份 1 行 CSV 走入库接口，编辑走单行更新接口。这正是「一次新增 = 一份 CSV」这条规则在界面上的直接体现。
3. **派生字段实时预览而不可编辑**：编辑器底部一行常驻显示「记账币种 · 记账金额 · 摊分起止月」，随支出金额/汇率/日期/月数即时重算，让 §8.1 的联动看得见。

### 6.3 功能之间的交互（跨页面的四条主链路）

1. **入库 → 核实**：新增一行 / 上传 CSV 成功 → 拿到返回的审计ID → 自动跳 `#/expense?operation_id=X&verify=1` → 核实视图取本批次三要素反查全库 → 分组高亮 → 人工裁定 → 勾选批量软删除。这条链路把 E-2 → E-3 → E-6 → E-5 串成一次连续操作，中途不需要用户自己去填筛选条件。
2. **审计 ↔ 明细 ↔ 文件 三角互跳**：明细行的「来源」→ 审计页按审计ID 定位；审计行的「本批明细」「本批文件」→ 分别跳明细页与文件页；文件行的「来源审计」「本文件明细」→ 跳回去。三处跳转都只是带参数的查询，不构成任何回滚入口（C-2）。
3. **记账币种提示 → 切换**：表头在「记账币种」列名后挂出全库币种集合；一旦多于一种或与当前选择不一致，表格上方出现黄条并直接挂切换按钮；设置页也有同一个入口。切换完成后提示条自动消失——这是 F-5 与 F-4 的闭环。
4. **已删除 → 复制新增**：删除是终态没有恢复入口，所以「只看已删除」筛选出来的行，操作列不给「编辑」而给「复制新增」，点下去把字段灌进新增编辑器（清掉明细ID 与删除时间），走新增路径产生新的审计ID / 文件ID。

### 6.4 实施中发现并修掉的三个缺陷

| # | 缺陷 | 后果 | 处置 |
| - | ---- | ---- | ---- |
| 1 | 单行新增拼 CSV 时，支出币种 = 记账币种会让编辑器把汇率锁成 1，而记账币种列被固定留空 | 命中 `base_csv.parseExpense` 的「填了折算汇率就必须填记账币种」，**单行新增 100% 失败** | 汇率非空时一并写出记账币种，汇率留空时两列都留空以继承请求口径（`page_expense.js` `draftToCsv`） |
| 2 | 行勾选框的 `onchange` 直接重绘整张表 | 正在编辑的输入框被重建，焦点与填到一半的内容都会丢 | 拆出 `renderToolbar`，勾选只刷工具条与勾选框本身；编辑草稿提升到 `state.editDraft`，任何重绘都不再冲掉 |
| 3 | 页面在 `<前缀>/static/` 下，浏览器仍会去请求根路径的 `favicon.ico` | 反代前缀下必然 404，污染服务端访问日志 | `index.html` 加 `<link rel="icon" href="data:,">` |

缺陷 1、2 都是实跑才暴露的——前者靠「新增一行走通了吗」，后者靠「勾选之后编辑器还在不在」，都不是读代码能看出来的。

## 七、验证与交付

### 7.1 实跑验证（全部在真实 Chromium 里跑通，非静态检查）

编译出二进制、在临时目录起服务，用无头 Chromium 驱动真实交互：

| 验证项 | 结果 |
| ------ | ---- |
| 静态资源挂载 | `/static/` 200，`js/`、`css/`、`lib/` 全部 200，`Content-Type: text/javascript`，`Cache-Control: max-age=86400` |
| 解锁闸 | 填两把口令 + 记账币种 → 探针通过 → 导航显出 → 落到明细页 |
| 明细列表 | 59 条种子数据，默认 10 列，分页「共 59 条 · 第 1/3 页」，默认按支出日期倒序 |
| 疑似重复高亮 | 首页 2 行带框高亮（同日期 / 同金额 / 同币种的一组） |
| 记账币种提示 | 「全库记账币种：CNY（56 笔）、USD（3 笔）；当前选择：CNY」+ 切换按钮 |
| 单行新增 | 填 2026-09-02 / 328.00 → 入库 → 自动跳核实视图，命中本批 1 行 + 库内 2 行同组，3 行全部高亮 |
| **CSV 上传（契约往返）** | 11 列契约文件入库 2 笔；含逗号的字段 `物美超市,大兴店` 与含换行的字段 `游戏\n打折` 都正确还原；USD 42.00 × 7.12 = **299.04** 与恒等式一致；摊分月数 3 生效 |
| CSV 契约拒绝 | 表头不符的文件 → 「解析明细，文件格式无法识别」，与后端 `Support` 不认领时的文案一致 |
| 内联编辑 | 改金额 → 预览实时重算 → 保存 → 列表刷新，审计页多出一条「明细编辑」且变更内容可展开 |
| 批量软删除 | 勾 2 行 → 按钮显示「批量删除（2）」→ 确认文案「本次将删除 2 笔…」→ 总数 59 → 57 |
| 只看已删除 + 复制 | 筛出 2 行已删除，划线灰显，操作列是「复制新增」而非「编辑」 |
| 表头设置 | 「全字段平铺」→ 23 列（21 字段 + 勾选列 + 操作列），顺序与 §四.1 完全一致 |
| 记账币种切换 | 执行后「成功 3 笔，失败 0 笔」，黄色提示条自动消失（全库统一为 CNY） |
| 审计页 | 6 条，8 类枚举筛选、结果标记、变更内容 JSON 展开、三角互跳均可用 |
| 文件页 / 设置页 | 文件 4 条可下载；设置页四张卡片布局正常 |
| **反代前缀** | 起一个把 `/xxx/*` 转发到后端的反代，从 `http://127.0.0.1:7679/xxx/static/` 打开：**页面、`lib/`、`css/`、`js/`、`../api/` 请求全部带上 `/xxx/` 前缀，无一条漏前缀**；hash 路由不受影响 |
| 手机宽度（420px） | 筛选区单列堆叠，表格横向滚动，导航不溢出 |
| 控制台 | **0 报错、0 失败请求、0 404** |
| 绝对路径审计 | `grep` 全量静态文件，无 `href="/`、`src="/`、`url(/`、`"/api` |
| Go 测试 | `go test ./...` 全绿（handler 23.4s、rdb 35.7s、repo 8.9s 等），`go build ./...` 通过 |

### 7.2 与 prompt 的逐条核对

| prompt 要求 | 交付 |
| ----------- | ---- |
| 前端代码放 `static` 下，通过 StaticFile 暴露 | 17 个文件全在 `static/`，`//go:embed *` 自动纳入，实跑验证挂载正常 |
| 把预期的全部功能页面都写出来 | 8 域 32 项功能落到 4 页 + 1 闸，§6.2 逐项映射 |
| 先不联调，前端 mock 数据用于展示 | `USE_MOCK = true`（`config.js:20`），`mock.js` 是与后端同语义的内存库；改一个常量即切真实接口 |
| 设计多少页面 / 每页功能 / 块间布局 / 功能间交互 | §6.2（页面与布局）、§6.3（四条跨页链路）、§6.2 末尾三条表内布局决定 |
| 可用开源库，但要下载到本地、不走 CDN | bootstrap 5.3.3 + decimal.js 10.4.3 落在 `static/lib/`，页面内无任何外链；实跑请求列表可证 |
| 相对路径，适配 `/xxx/static` 反代 | 接口前缀 `../api/`，资源全相对；已用真实反代实测通过 |

### 7.3 清理

- 临时产物（二进制、反代脚本、无头浏览器脚本、测试用 CSV、截图）全部在会话临时目录，仓库内无残留。
- 运行期进程已停止；`git status` 显示新增文件只有 `static/` 下的交付物与本 answer。
- 未提交任何变更。

## 八、待确认（不臆造，留给你裁决）

1. **四个后端还没有的接口**（§3.4）：单行编辑、记账币种切换、文件下载、支出类型候选 / 记账币种集合。前端已按 mock 跑通全部交互，真实分支直接抛「接口后端未实现」而不臆造路径。**需要你定这四条的路径与请求体**，我再补 `api.js` 的真实实现与对应的后端。
2. **核实视图（E-3）的查询方式**：现有 `ExpenseInquiry` 没有「三要素整组匹配」的条件，我用「日期区间 + 币种集合 + 金额区间」把候选集缩小后在前端精确命中。批次大时会多拉一些行，且受 `pageSizeMax = 200` 限制。**要不要后端加一个按审计ID 直接返回「本批 + 疑似重复」的接口？**
3. **前端如何拿到后端口令**：`ValidateGin` 的 secret 是配置文件里的 `server_token`，前端必须持有它才能自签 jwt。当前设计是让用户在解锁页手填。**确认这就是预期交互？**
4. **币种枚举**：后端认 ISO 4217 全集（`bojanz/currency` 的 `IsValid`），前端下拉只列了 20 个常用币种，其余允许直接敲三位代码。**要不要后端出一个币种枚举接口，把「代码 / 名称 / 小数位数 / 是否启用」这四项统一到一处？**
5. **CSV 契约的文档与代码不一致**（§一 冲突 1）：文档 E-1 写 8 列，代码是 11 列。本轮按代码实现。**要不要回头把需求文档订正成 11 列？**
6. **金额展示**：后端 decimal 序列化会丢尾随零（`68.00` 算出来是 `68`），前端按金额精度补齐小数位、只补不截。**确认这个展示口径。**
