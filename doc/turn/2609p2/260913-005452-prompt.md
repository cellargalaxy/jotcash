# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260913-005452-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
    - [记账系统功能与模型.md](../../decisions/%E8%AE%B0%E8%B4%A6%E7%B3%BB%E7%BB%9F%E5%8A%9F%E8%83%BD%E4%B8%8E%E6%A8%A1%E5%9E%8B.md)
- 代码风格：
    - /home/ms/document/code/survive_monitor
    - 应该优先使用github上有积极维护且star多（至少需要上百）的代码库，避免造轮子
- 交付路径：

## 具体任务

+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 创建`model`代码包，创建实体的struct
+ 全部代码层级都使用同一个struct，不划分上面PO/BO之类的
+ `model`内不能包含任何业务逻辑的代码，String ()这类的可以有
+ 字段标签需要有json/grom等
+ 金额使用这个代码库：https://github.com/shopspring/decimal
+ 货币使用这个代码库：https://github.com/bojanz/currency

### 对话2

+ 依然是`model`代码包
+ 去除全部没有业务功能的字段
    + FileMeta的Purpose字段有业务功能吗，没有就去掉
    + FileMeta的FileSize保留用于页面展示
+ 表的Id需要是int64
    + 业务逻辑的轮子优先使用：https://github.com/cellargalaxy/go_common
    + 使用go_common的GenId函数生成ID
    + 再其次才是使用github上有积极维护且star多代码库
+ AuditLog的OperatedAt，采用CreatedAt会更加规范
    + grom约定的就是created_at/updated_at/deleted_at？
+ Expense的CardLastFour，这翻译也太死板了
    + 还有没有其他又臭又长，或者死板翻译，也一并优化
+ 需要写单测
    + 确保字段类型的读写精度无误
    + 如果在grom层再写单测也可以
+ 需要支持服务第一次启动的时候，自动建表
    + 我看有些字段没有标明在数据库中的字段类型，有影响吗

### 对话3

+ 依然是`model`代码包
+ 去除「用途」这个筛选的功能，并且删除「用途」这个字段

+ 创建`db`代码包，用于实现无业务逻辑的数据库增删查改
+ 具体代码本次对话先不写，主要是需要将migrate.go移动到`db`代码包下

### 对话4

+ migrate.go移动了，单测呢？
+ 不要写一大堆有的没的注释，都没用的注释/过程注释全部都删除

### 对话5

+ `model`代码包与`db`代码包
+ 之前的回话AI幻觉严重，我手动编辑了示例代码
+ 你需要按照我的代码风格，完成`model`与`db`
+ `model`只定义结构体，不能涉及任何业务代码或者连接性代码
+ `db`只做无业务逻辑的通用的增删查改

### 对话6

+ Open/Close需要留在db包里，db包除了增删查改，连接性代码也是在db包里的，db包负责完整数据库连接
+ 前端传来的口令，会保持在jwt的Claims中，虽然确实无登录概念，但是为了能复用go_common的代码，也依然使用jwt
    + 在[260911-220830-prompt.md](260911-220830-prompt.md)中提到有一对口令，前端口令（clientToken）与后端口令（serverToken）的想法
    + 虽然这个想法对于安全性没有帮助，但是为了能使用jwt，也依然保留前端口令与后端口令的设计
    + jwt的secret用的就是后端口令
    + 还有一个好处里，对于非法攻击，在gin里大概率就能通过jwt交验给拦截下来，不会走到后面的业务逻辑中，减少暴露面
+ go_common会将Claims对象保存在ctx中
    + func GetClaims (ctx context.Context) *model.Claims {
+ db包需要实现这个逻辑
    + 在ctx中取出Claims的ClientToken，完成数据库连接
    + 数据库连接的逻辑需要在db包内部完成，上层代码不感觉也不处理这些底层
+ ExpenseInquiry也好，其他的Inquiry也好，如果有业务逻辑需要的查询，但是现在没有对于的查询字段，都可以家
+ 排序可配，创建一个map[int,str]，上层传int，Inquiry里映射为str，避免SQL注入
+ OperationType/Result 的枚举常量放model包里，例如OperationType与OperationLog放在同一个文件里