# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260915-102207-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 在[db.go](../../../db/db.go)里，我手写里CreateDbHandler/DbRemoveHandler/TokenLogHandler
+ 不再在/db加锁，将锁迁移到service/db
+ 在service/db里组合CreateDbHandler/DbRemoveHandler/TokenLogHandler/OperationLogInsertHandler
+ 然后在service/db里调用NewTransaction来提交事务
+ 要按照严格我的思路/代码风格/函数边界来，严禁换思路/换风格/新编造函数/删除我的函数/魔改我的函数

```go
NewTransaction(ctx).
AddCommit(CreateDbHandler, OperationLogInsertHandler,TokenLogHandler).
AddBack(DbRemoveHandler).
Exec(ctx)
```

### 对话2

connect函数只留给open数据库使用，创建数据库不再使用connect，创建数据库改为纯touch文件
将创建数据库的touch逻辑封装到NewTransaction里，NewTransaction的逻辑边界编程：
如果数据库文件不存在，则NewTransaction创建数据库文件，并open数据库
如果数据库文件已经存在，则直接open数据库
这样上层能减少一步创建数据库的操作感知
这样会更加合理吗

### 对话3

我感觉之前我的思路不对，创建数据库的逻辑依然还是需要在/db里，而非不是在/service/db，锁也要迁移回/db
同时，创建数据库（包含创建文件/建表/写操作日志/失败回滚）需要封装在NewTransaction
上层有业务逻辑，需要保证入参正确，上层需要往哪里进行普通业务的读写，/db照执行即可
上层就不需要关注创建数据库这些底层细节，并且也与mysql之类的情况一致，上层不会关注创建数据库，建表，链接关闭等底层细节

### 对话4

由于NewTransaction里封装里创建数据库，换句话说，创建数据库比NewTransaction
因此里创建数据库的代码不应该依赖NewTransaction
因此之前我的思路不对，创建数据库不应该封装为Handler
现在需要将创建数据库的代码逻辑整合回到一个函数里，代码逻辑需要平铺直观，避免拆出小函数

### 对话5

我手动整理里一下代码，你review一下，如果有问题的话进行修复
我删除的代码不要再加回来
不需要init了，第一次读写的时候，NewTransaction会自动创建数据库
create函数是放在NewTransaction里合适还是newTransaction里合适
如果数据库已经创建，有两个请求并发发开数据库文件进行普通的业务读写，会有并发问题吗（换句话说，创建数据库需要控制并发，读写数据库需要控制并发吗）