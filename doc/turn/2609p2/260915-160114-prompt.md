# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260915-160114-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 通读本代码仓库的全部代码

+ 单测是否覆盖充分，补充缺失的单测case
+ 去哪里寻找case思路
+ 阅读这些代码的历史改动
+ 阅读doc/turn的历史对话

+ 单测是否都通过，没通过的哪些，是单测不对，还是代码不对，修复不对的代码
+ 修复代码需要以最小改动去修复
+ 修复代码不能将历史删除的代码给恢复，历史代码给删除或者修改，正说明其不符合预期

+ 除了单测本身，你阅读完全部的代码，也需要直接依据代码本身，判断代码是否有错误
+ 锁与并发，链接的释放等等

### 对话2

+ 需要以最小化的代码改动达到目的
+ 阅读代码发现的问题，都需要添加单测，避免日后又出错但是不知道
+ rdb.ClearBackup 全仓没有生产调用点
    + 维持现状
+ rdb.Open 的读锁在交出连接那一刻就放掉
    + 需要修复
    + 对数据库的库文件的写操作，例如导入/创建等，都需要拿到dbLock的写锁
    + 对数据库的库文件的读操作，以及对业务数据的读写操作，都需要拿到dbLock的读锁
    + 并且在操作完整后，才能是否锁，避免并发读写导致一次
+ Export 在读锁里做 io.Copy 到 HTTP 响应
    + 没看懂，你先修复问题
+ 同一协程里嵌套 NewTransaction 会死锁
    + 好像并没有比较好的解决办法
    + 维持现状，添加注释
+ DbTransaction.Close 重复调用会 panic
    + 好像并没有比较好的解决办法
    + 但是其实可以认为重复调用Close并不合理，重复调用的行为需要修复
    + 维持现状
+ service.InsertExpense 的 filename == "" 兜底从 HTTP 入口够不到
    + 什么叫作够不到
    + 如果是说这条代码规则其实永远不会进入，那维持现状
+ onfig.ConfigService 每分钟按相对工作目录重载 —— 与 t.Chdir 组合有 flaky 隐患
    + 维持现状
+ //go:embed * 编译不过
    + 已经添加上文件了

### 对话3

+ 数据库导出
    + 只允许Export与export两个函数，不允许在创建新的函数
    + 保持原有风格，代码平铺在函数内，避免创建小函数
+ 如果Open函数只有CheckToken使用
    + 删除Open函数，将Open函数的代码逻辑移动到CheckToken内
    + CheckToken加锁，完整token校验后，释放数据库，释放锁
    + 避免又创建一个DbConn结构体