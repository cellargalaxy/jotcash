# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260915-143400-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 所涉及的代码范围如下，完整阅读这些代码
    + [config](../../../config)
    + [db](../../../db)
    + [model](../../../model)
    + [tool](../../../tool)

+ 这几个代码包的单测是否覆盖充分，补充缺失的单测case
+ 去哪里寻找case思路
+ 阅读这些代码的历史改动
+ 阅读doc/turn的历史对话

+ 这几个代码包的单测是否都通过，没通过的哪些，是单测不对，还是代码不对，修复不对的代码
+ 修复代码需要以最小改动去修复
+ 修复代码不能将历史删除的代码给恢复，历史代码给删除或者修改，正说明其不符合预期

+ 除了单测本身，你阅读完全部的代码，也需要直接依据代码本身，判断代码是否有错误
+ 锁与并发，链接的释放等等

### 对话2

+ 没有init：库在第一次开事务时才建出来，口令就是那次请求带的
    + 是不符合预期的，第一次用户哪知道前端口令与后端口令是多少
    + 服务第一次启动就要创建数据库，并且在日志中打印口令
+ NewTransaction与newTransaction只作为普通读写请求使用
    + 不耦合创建数据库的逻辑
+ /db包下面的静态增删查改函数，都需要去除
    + 未来有些功能，会是多个表的读写在一个事务中
    + 代码的职责边界是
    + /service/db负责将业务功能所需要的handler创建出来
    + /service/db负责new一个transaction对象发起一个事务
    + /service/db负责提交事务并关闭链接
    + 尽量做到一次请求，只打开一次数据库，将全部需要做的读写都一次性完成，再关闭数据库
    + 减少数据库反复打开的损耗，也使得读写更加事务