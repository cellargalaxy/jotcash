# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260915-160758-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 当前后端代码的大体架构已经搭建起来，实现里一部分功能的接口
+ 现在需要开始写前端
+ 前端代码放在[static](../../../static)下面，通过StaticFile暴露出去
+ 你需要将本系统预期的全部功能的页面都写出来
+ 先不需要跟服务端联调，只需要在前端代码里先mock一些测试代码用于数据展示，给我验收
    + 需要考虑设计多少页面，每个页面有哪些功能，功能块之间在页面里是如何布局，功能与功能之间的交互如何设计等等
+ 可以使用各种开源的html/js/css库，这些库需要下载到本地，也通过StaticFile暴露出去，避免走CDN
+ 系统在本地启动的时候，路径使用的是/static，但是到生产环境，会可能有caddy反代，路径变成/xxx/static
    + 因此需要使用相对路径，避免使用绝对路径

### 对话2

+ 一些需要优化的内容
+ 币种的筛选框与输入框，都需要支持下拉选择
+ 支出类型的筛选框与输入框，都需要支持下拉选择已有的支出类型，并且支持录入新的支出类型
    + 录入新的支出类型后，下拉列表中需要自动添加上这个新的支出类型
+ 卡号后四位的筛选框与输入框，都需要支持下拉选择已有的卡号后四位，并且支持录入新的卡号后四位
    + 录入新的卡号后四位后，下拉列表中需要自动添加上这个新的卡号后四位
+ 银行名称的筛选框与输入框，都需要支持下拉选择已有的银行名称，并且支持录入新的银行名称
    + 录入新的银行名称后，下拉列表中需要自动添加上这个新的银行名称
+ 交易备注与交易对手方，需要小字说明的模糊搜索
+ 「上传CSV」按钮的文案，日后会增加各种格式，因此这个文案不合适
+ 「下载CSV模板」的按钮，点击没有反应
+ 需要新增一个按钮，将当前筛选条件（不分页）的数据导出到标准CSV文件中下载
    + 这个功能需要前端来做，后端不会有对于的接口
    + 现在后端校验里分页参数，不允许大分页，这个校验之后会去除
+ 右下角下拉选择分页大小，下拉后部分选择超出到里浏览器页面以外，导致选择不了

### 对话3

+ 在页面顶部增加一个公告
+ 公告的内容是，当前页面的链接，例如：http://xxx.com/path/xxx.html
+ 如果是非https，需要是红色告警，提示用户不安全可能会有泄露口令
+ 如果是https，则是绿色，表示安全
+ 如果浏览器里能够获取到https证书是否合法，如果不合法则是黄色，并且有对于的提示语，如果浏览器获取不到这些信息就算了
+ 公告在解锁页面不支持关闭
+ 解锁后；如果是安全的，过几秒自动消失，也支持关闭；如果是不安全，则不会自动消失，也不支持手动关闭

+ 编辑明细，点开支出类型的下拉，下拉框超出里原本表格的高度，表格就新增出现里一个滚动条
    + 虽然由于有滚动条，滚动一下还是能选择下拉框底部的选项的
    + 但是新增的滚动条占用里空间，表格一下子缩里一下，页面发生跳动，这个需要优化
+ 点击「下载导入目标」依然没有任何反应，没有触发下载
+ 点击「导出查询结构」只有右下角一个提示框，没有触发下载

### 对话4

+ 切换记账币种的文案，其中有显示每个币种有多少笔
    + 明细的笔数量，服务端不会提供，只会提供记账币种现在有哪些
+ 现在我是在goland内置的浏览器打开的页面
    + 不知道是不是这个原因导致的下载没反应
    + 我尝试将公告里的链接复制到chrome里，但是打开是goland图标的404页面
    + 公告里展示的链接是完整的吗

### 对话5

+ 将前端所需要的接口列出来，我复核没问题后，让后端进行开发
    + 你只需要接口列出来，不需要去开发