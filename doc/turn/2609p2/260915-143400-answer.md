# 对话1 · config/db/model/tool 四包的单测补全、红灯修复与代码审读

> 依据来源：
> - **本仓库代码**：`config/config.go`、`config/handler.go`、`db/*.go`、`model/*.go`、`tool/token.go` 全量通读；连带 `service/db/expense.go`、`service/expense.go`、`handler/handler_test.go`
> - **依赖本体**：`go_common@v0.0.0-20260915021254-46858b7344b5` 的 `util/gorm.go`（`Transaction`/`NewTransaction`/`InsertHandler`/`UpdateHandler`/`DeleteHandler`/`SelectHandler`/`CloseDb`）、`util/gen.go:38 GenId`
> - **历史改动**：`git log -S"func Transaction("`（`069b9e3`「删 `Transaction`、引入 `NewTransaction`」）、`74cc600`、`ac65279`、`361da7f`
> - **历史对话**：`doc/turn/2609p2/260915-102207-answer.md`（对话3 §1 建库形状、对话5 §1 并发、对话6 `Open` 的锁、对话7 §6 方案甲落地）、`260915-130316-answer.md`（§4.2 与 §五「59 条红灯是同一个病根」、对话5 close 审计）
>
> 结论：**四个包开工时有三个能跑、`db` 整包编译不过**（`undefined: Transaction`，13 处生产调用点 + 6 处测试调用点）。病根是 `069b9e3` 那次重构只删了旧实现、没迁调用点，以及「建库搬进 `NewTransaction`」这条线只做了一半。**按「不把删掉的代码捡回来」修的是调用方**，生产代码净改 3 个点、31 行。修完四个包 **全绿**，覆盖率 `db 76.6%→86.9%`、`model 61.1%→100%`、`tool 78.8%→93.9%`、`config 93.8%`（新增 20 个用例）。另有 5 处读代码读出来的问题，**只报不改**，列在 §五。

---

## 一、开工时的实际状态

```
go build ./...
# github.com/cellargalaxy/jotcash/db
db/expense.go:154:9: undefined: Transaction      （另 12 处同类）
```

`db` 包编译不过，所以 `go test ./db/` 一条都没跑起来；`config`/`model`/`tool` 三个包本来就是绿的。

| 包 | 开工 | 收工 |
| -- | ---- | ---- |
| `config` | 绿，覆盖 93.8% | 绿，覆盖 93.8%（新增 2 类断言） |
| `model` | 绿，覆盖 61.1% | 绿，覆盖 **100%** |
| `tool` | 绿，覆盖 78.8% | 绿，覆盖 **93.9%** |
| `db` | **编译不过** | 绿，覆盖 **86.9%**，`-race` 干净 |

---

## 二、红灯定性：是代码不对，不是单测不对

### 2.1 `undefined: Transaction` —— 调用点没跟上重构

`git log -S"func Transaction(" -- db/`只有两条命中，最近一条是 **`069b9e3`（2026-09-14 23:24）**：

```diff
-func Transaction(ctx context.Context, handlers ...util.TransactionHandler) error {
-	ctx = detachCtx(ctx)
-	...
-	dbLock.RLock()
-	defer dbLock.RUnlock()
-	return transaction(ctx, dbPath, token, handlers...)
-}
+func NewTransaction(ctx context.Context) (*util.Transaction, error) {
```

而 `db/db_test.go` 里那批 `Transaction(ctx, ...)` 调用是 **`aed0459`（2026-09-13）** 写的，**比删除早一天**。所以这不是「单测写错了」，是**旧 API 的调用点在删除那天漏迁**——`db/expense.go`(4)、`db/file_blob.go`(3)、`db/file_meta.go`(3)、`db/operation_log.go`(3) 共 13 处生产代码，`db/db_test.go`(4)、`../../../rdb`(1)、`db/file_blob_test.go`(1) 共 6 处测试，外加 `service/db/expense.go` 2 处。

按「历史代码给删除或者修改，正说明其不符合预期」，**没有恢复 `Transaction`**，改的是调用方。`260915-102207-answer.md` 对话7 §5 里列的方案乙正是「恢复 `Transaction(ctx, handlers...)`」，当时你选的是**方案甲**（`DbTransaction` 包一层），并且那条选择里明写了「得先把那 18 处迁到 `NewTransaction`」——本轮做的就是这件事。

### 2.2 建库搬进 `NewTransaction` 只做了一半

`db/db_test.go:78` 与 `handler/handler_test.go:63` 两条注释把目标形状写死了：

> 没有init了：库在第一次开事务时才建出来，口令就是那次请求带的

但 `db/db.go` 还是 `init()` + `Create()`（口令来自 `tool.GenToken` 随机生成）的老形状，`NewTransaction` 碰上库文件不存在直接报「打开数据库，库文件不存在」。这正是 `260915-130316-answer.md` §4.2 统计的 **59 条红灯的唯一病根**，也是那轮结尾「**说一声我来做**」留下的活。本轮做完。

---

## 三、改了什么（生产代码 3 个点，31 行）

### 3.1 `db/db.go`：删 `init()`、口令来源换成本次请求、`NewTransaction` 接上建库

```diff
-func init() {
-	ctx := util.GenCtx()
-	err := Create(ctx)
-	if err != nil {
-		panic(err)
-	}
-}
-
+// 没有init：库在第一次开事务时才建出来，口令就是那次请求带的
 func Create(ctx context.Context) error {
 	dbPath := config.DbPath
-	token, err := tool.GenToken(ctx, tool.TokenLen)
+	token, err := tool.GetToken(ctx)
 	if err != nil {
-		panic(err)
+		return err
 	}
```

```diff
 func NewTransaction(ctx context.Context) (*DbTransaction, error) {
 	...
+	//库没就绪就地建出来，上层只管拿事务做业务读写，不感知建库建表
+	err = Create(ctx)
+	if err != nil {
+		return nil, err
+	}
+
+	//建库自己要拿写锁，必须排在读锁之前，顺序反了同一条协程先读后写，RWMutex不可重入会自锁
 	dbLock.RLock()
```

- `Create` 的**双检写锁骨架、0 字节残骸判定（`info.Size() > 0`）、`create` 的平铺实现一个字没动**——`260915-102207-answer.md` 对话3 §1 那张「`create` 卡 `Size()>0` / `open` 只卡存在」的对照表继续成立。
- 口令来源从 `tool.GenToken` 换成 `tool.GetToken`，正是 `260915-130316-answer.md` §五 写的收口方式。
- `Create` 排在 `dbLock.RLock()` **之前**：`Create` 内部要拿写锁，顺序反了同一协程先读后写，`sync.RWMutex` 不可重入会自锁（对话7 §3 前提二有实测样本）。

### 3.2 13 处 DAO 包装函数：`Transaction(ctx, handler)` → `NewTransaction`

统一形状（以 `db/expense.go:152` 为例，其余 12 处逐字同构）：

```go
func InsertExpense(ctx context.Context, object ...*model.Expense) (int64, error) {
	transaction, err := NewTransaction(ctx)
	if err != nil {
		return 0, err
	}
	defer transaction.Close(ctx)

	handler := NewExpenseInsertHandler(object...)
	err = transaction.AddCommit(handler).Exec(ctx)
	if err != nil {
		return 0, err
	}
	return handler.Count, nil
}
```

**没有再抽一个 `execTransaction(ctx, handlers...)` 小函数**：那等于换个名字把删掉的 `Transaction` 捡回来，与你在对话7 里选甲弃乙的结论冲突。`defer transaction.Close(ctx)` 是这套形状里唯一的解锁点，13 处全部带上。

### 3.3 `service/db/expense.go`：2 处多 handler 事务（范围外，为了让仓库能 build）

`InsertExpense` / `DeleteExpense` 是 `db.Transaction` 仅剩的两个外部调用点，不改的话 `go build ./...` 一直红。改法与 §3.2 同构，多 handler 直接进同一个 `AddCommit`：

```go
	transaction, err := db.NewTransaction(ctx)
	if err != nil {
		return err
	}
	defer transaction.Close(ctx)
	...
	return transaction.AddCommit(fileBlobHandler, fileMetaHandler, expenseHandler, operationLogHandler).Exec(ctx)
```

**这是本轮唯一越出 config/db/model/tool 四包的改动**，共 16 行，事务语义（四个 handler 同生共死）不变。

### 3.4 6 处测试调用点

`db/db_test.go`（`TestTransaction` ×2、`TestConcurrentTransaction`、`TestChangeTokenDuringTransaction`）、`db/expense_test.go:138`、`db/file_blob_test.go:36`，全部改成「开事务 → `AddCommit(...).Exec` → `Close`」。**断言一条没动、没删任何用例**——`TestChangeTokenDuringTransaction` 那条钉读锁的用例形状照旧（读锁必须罩住整条连接，摘掉就红）。

---

## 四、补的单测（20 个用例）

### 4.1 case 思路从哪儿来

按优先级，本轮实际用到的六个来源：

1. **覆盖率报告**：`go test -coverprofile` + `go tool cover -func | awk '$3+0 < 90'`。0% 的函数一眼就是漏网（`tool.GetToken`、`db.UpdateFileBlob`/`UpdateFileMeta`、6 个 `String()`），部分覆盖的函数则指向没走到的分支。
2. **结构体字段逐项对表**：`model.ExpenseInquiry` 有 20 个筛选项，`db/expense.go ExpenseInquiry.Where` 就该有 20 个 `if`——漏接一项的表现是**静默查全表**，不报错，只有用例能钉住。`TestExpenseInquiryField` 就是照字段表写出来的。
3. **守卫语句**：函数开头那串 `if xxx == "" { return errors.Errorf(...) }` 每一条都是一个用例。`export`/`import_`/`changeToken`/`backup`/`open` 的口令与库文件守卫挡的是越权，值钱。
4. **注释里写明的「为什么」**：`//金额列是文本，按文本排序会串位`、`//空格会被URI编码成加号`、`//0字节是上次建库没建成的残骸`——每一句都是一条已经被踩过的坑，对应一条回归用例。
5. **`git log` 与 `doc/turn/*-answer.md` 的遗留清单**：历史上修过的 bug 最容易复发。`TestTransactionUnlockOnFail` 就出自对话7 §4 的「新引入的风险一条：忘了 `Close` 读锁就永久泄漏」。
6. **依赖本体的语义**：`util.InsertHandler.Exec` 对空入参是「warn + return nil」，`util.SelectHandler` 把 `ErrRecordNotFound` 吞成空结果——这些约定得有用例钉住，否则哪天换库就静默变行为。

### 4.2 清单

| 文件 | 新增用例 | 钉的是什么 |
| ---- | -------- | ---------- |
| `tool/token_test.go` | `TestGetToken` | ctx 无 Claims / Claims 无口令 / 正常取值；`TestGenToken` 补「不含 `01lIO` 与空格」（字符集刻意剔掉肉眼易混字符） |
| `model/token_test.go`（新） | `TestChangeTokenReq` | 新口令 json 要传得出去，`String()` 里必须抹掉 |
| `model/operation_log_test.go` | `TestOperationLogJson`、`TestOperationLogInquiryString` | int64 主键走 json 不被浮点精度削掉；`String()` 与 `json.Marshal` 一致 |
| `model/file_meta_test.go` | `TestFileMetaJson`、`TestFileMetaInquiryString` | 同上 |
| `model/file_blob_test.go` | `TestFileBlobInquiryString` | 同上 |
| `config/config_test.go` | `TestConfigParse` 补负数兜底与「填了合法值不被默认值顶掉」；`TestConfigDefault` 补「默认配置自己要解析得回来」 | `Parse` 的三条 `<= 0` 兜底只覆盖了 0，负数没走到；首次启动写出去的就是 `GetDefault`，它必须能被 `Parse` 吃回去 |
| `db/inquiry_test.go` | `TestSortOrder` | 排序是拼进 SQL 的：默认值走 `defaultSort`、`expense_amount desc` 必须取到 `cast(...)` 映射值、四张表白名单各管各的、默认值本身不在白名单也要报错 |
| `db/expense_test.go` | `TestExpenseInquiryField`、`TestExpenseEmptyObject` | 10 个等值/模糊筛选项逐个命中（含 `expense_type` 的 `_` 转义）；空入参是「什么都不做」而非报错或误伤 |
| `db/file_meta_test.go` | `TestFileMetaInquiry`、`TestFileMetaUpdate` | Id/FileName/OperationId/时间区间/`_`与`%`转义/白名单外排序/倒序分页；更新不该把 `CreatedAt` 抹成零值 |
| `db/operation_log_test.go` | `TestOperationLogInquiry` | ObjectType/ObjectId/Result/`SummaryLike` 转义/倒序分页/非法排序 |
| `db/file_blob_test.go` | `TestFileBlobUpdate` | 更新链路 + 非法排序 + 默认排序分页 |
| `db/backup_test.go` | `TestBackupIllegalArgument`、`TestClearBackupIllegalArgument`、`TestBackupGuard` | `backup` 的四条守卫 + 「目标已存在不许覆盖」（顺带断言占位内容原样）；`clearBackup` 空目录/`limit<=0`/没到上限不删；`export`/`import_`/`changeToken`/`open` 的口令与库文件守卫，并断言被拦下的操作**没碰库、没留临时文件** |
| `db/db_test.go` | `TestTransactionRollbackHook`、`TestTransactionUnlockOnFail` | 提交链失败时 `AddRollback` 要跑起来（失败审计补得进去、提交链的插入不留痕）；**开事务失败必须还回读锁** |

### 4.3 反向验证：新用例真的抓得住东西

把 `db/db.go` 里 `newTransaction` 失败分支的 `dbLock.RUnlock()` 摘掉：

```
--- FAIL: TestTransactionUnlockOnFail (10.17s)
    db_test.go:551: 开事务失败没还回读锁，换口令被永久挡住
```

装回去即绿。这条用例覆盖的正是对话7 §4 列的那条「新引入的风险」。

---

## 五、读代码读出来的问题（**只报不改**，等你定）

### 5.1 `Open` 的读锁在交出连接的那一刻就放掉了 —— 锁作用域错位

`db/db.go:114`：

```go
dbLock.RLock()
defer dbLock.RUnlock()      // ← 函数 return 就放，连接刚交出去
db, err := open(ctx, dbPath, token)
return db, nil              // ← 危险窗口从这里才开始
```

危险窗口是「连接活着的那一整段」，而这把锁只罩住了开库那几微秒。`DbTransaction`（`db/db.go:216`）当初就是为了把解锁点挪到 `Close` 才引入的，`Open` 还是老形状。

- **现在不出事**的原因：唯一的生产调用方是 `CheckToken`（`db/db.go:171`），下一行就 `util.CloseDb`，窗口以微秒计；`handler/ping.go` → `service/db.CheckToken` 走的就是它。
- **两条自洽的收尾**（对话6 §3 列过，仍未选）：① 干脆把这两行删掉——它现在提供的不是保护而是「看起来有保护」；② 让 `Open` 也返回一个带解锁的 closer，与 `DbTransaction` 对齐。
- 不动的理由：`Open` 是公开 API，`TestOpenWithoutDbFile`/`TestPageLimit`/`TestMigrateTrigger*` 都在用，改签名是设计决定不是修 bug。

### 5.2 同一协程里嵌套 `NewTransaction` 会死锁 —— 现在没人踩，但是个地雷

`sync.RWMutex` 的文档规则：**读锁持有期间若有写者在等，新的读锁必须排在写者后面**。所以

```go
transaction, _ := db.NewTransaction(ctx)   // 持读锁
defer transaction.Close(ctx)
db.SelectExpense(ctx, inquiry)             // ← 内部又要 RLock，此时若 Import 正在等写锁 → 死锁
```

**全仓库当前没有这种写法**（`service/db/expense.go` 的两个事务、`service/expense.go` 的三个入口都是平的，已逐个核对），所以不是现存 bug。但方案甲把「持锁」这件事藏进了 `NewTransaction`，调用方看不见，以后 `service` 层想在一个事务里顺手查一把就会撞上。建议在 `NewTransaction` 的注释里写一句「同一协程内不得嵌套」。

### 5.3 `import_` 失败时会在备份目录留下半个 `originPath`

`db/backup.go:257`：

```go
err = backup(ctx, dbPath, token, originPath, token)
if err != nil {
	util.RemoveFile(ctx, backupPath)   // ← 只清了上传的那份，originPath 没清
	return err
}
```

同一个文件里的 `changeToken`（`db/backup.go:308`）碰到 `backup` 失败是清自己 dst 的，两个兄弟函数口径不一致。**触发条件**：`backup` 在真正写 dst 的中途失败（磁盘满 / IO 错），实际概率很低，也因此 `TestImportWrongSchema`/`TestImportIllegal` 那两条「不应留下临时文件」断言碰不到它——它们在更早的守卫就返回了。修法是一行 `util.RemoveFile(ctx, originPath)`。没改是因为 `import_` 是你对话3/对话4 手写并 review 过的函数。

### 5.4 建库日志现在打的是「用户自带的口令」，不再是「系统生成的口令」

`db/db.go:106`：

```go
logrus...WithFields(logrus.Fields{"dbPath": ..., "serverToken": ..., "clientToken": token}).Info("创建数据库，口令")
```

`init()` + `GenToken` 时代，这行是**用户唯一能拿到随机口令的渠道**，非打不可。本轮口令来源换成 `tool.GetToken` 之后，`clientToken` 是请求自己带来的，用户本来就知道——这行等于把一个用户机密原样落进 `log/` 里的日志文件。`serverToken` 同理（它来自 `resource/jotcash.yaml`）。

**没动**，因为 `../../../rdb`、`:96`、`:145` 三条断言正钉着它，是你重构之后写的，属于明确意图。要收的话是「保留 `serverToken`、去掉 `clientToken`」还是「整行降级到 Debug」，你定。

### 5.5 金额区间筛选与金额排序都过一遍 float64

`db/expense.go:69`：

```go
tx = tx.Where("cast(expense_amount as real) >= ?", this.ExpenseAmountMin.InexactFloat64())
```

列是 `varchar(32)` 存 `decimal`，两边都被压成 float64（约 15~16 位有效数字）。`db/expense_test.go:20` 那个 `-1234567890123456.7890` 已经在临界上，边界值上的 `>=` / `<=` 可能多一条少一条。排序同理（`expense_amount asc` → `cast(... as real)`，已有注释说明）。这是「金额存文本」这个设计的既定代价，不是实现 bug；真要精确只能改成定长补零的字符串比较或者拆整数分列，代价是迁移存量数据。**列在这里备案。**

### 5.6 扫过但确认没问题的几处

- **连接释放**：生产代码 18 个 close 点全部复核，与 `260915-130316-answer.md` 对话5 的结论一致——`backup` 里 `defer CloseDb(gormDb)` 先注册、`defer CloseIo(conn)` 后注册，LIFO 下先还连接再关 DB，顺序是对的；`create`/`export`/`import_`/`changeToken` 四处都是「先显式 `CloseDb` 再碰文件」。**新增的 13 个包装函数每一个都带 `defer transaction.Close(ctx)`**，没有漏网。
- **`clearBackup` 的 `sort.Strings`**：备份文件名是 `fmt.Sprintf("%d.db", util.GenId())`，`util.GenId` 走 `GenIdByTime`，恒为 16 位定宽数字，字典序与数值序一致，按名字排 = 按时间排。
- **`migrate` 的触发器 SQL 用 `fmt.Sprintf` 拼串**：`Table`/`Reason` 都是 `migrateTriggers` 里的编译期常量，没有注入面。
- **`DeleteHandler` 会把 `Order` 也套到 DELETE 上**：gorm 构建 DELETE 时只取 `Delete/From/Where` 三个 clause，`ORDER BY` 被丢弃，不影响语义；而非法排序仍然会在 `Order` 这一步报错，`TestExpenseSort` 最后三条断言钉的就是这个。
- **`model.Claims.String()` / `Config.String()` / `ChangeTokenReq.String()`** 全是值接收者，抹字段抹在副本上，不会污染原对象——新增用例已覆盖。

---

## 六、验证

```
go vet ./config/... ./db/... ./model/... ./tool/...     # 干净
gofmt -l .                                              # 无输出

go test ./config/... ./db/... ./model/... ./tool/... -count=1 -cover
ok  github.com/cellargalaxy/jotcash/config  0.012s  coverage: 93.8% of statements
ok  github.com/cellargalaxy/jotcash/db      28.806s coverage: 86.9% of statements
ok  github.com/cellargalaxy/jotcash/model   0.018s  coverage: 100.0% of statements
ok  github.com/cellargalaxy/jotcash/tool    0.019s  coverage: 93.9% of statements

go test ./db/ -count=1 -race -timeout 600s
ok  github.com/cellargalaxy/jotcash/db      66.343s          # 无 data race

go test ./service/... -count=1                               # 三个包全绿
```

`db` 剩下的 13% 未覆盖集中在磁盘 IO 失败与 `gorm.Open` 失败这类拿不到的分支（`create` 的建目录/写文件失败、`migrate` 的 `AutoMigrate` 失败、`open` 的 gorm 初始化失败）；`config` 剩的 6% 是 `init()` 与 `GetDefault` 的两个 `panic` 分支。

### 6.1 仓库整体还差一处，**不在本轮范围，也不是我改出来的**

```
go build ./...
# github.com/cellargalaxy/jotcash/handler
handler/ping.go:16:9: undefined: db
```

`handler/` 从 14:46 起一直在被手工改（`ping.go` 先并进 `rdb.go` 又拆回来，中途出现过 `model.PingData` 未定义——`PingData` 在 `go_common/model`，而 `handler/db.go` 里的 `model` 被 `jotcash/model` 占了名字，需要给其中一个起别名）。这是 `handler` 包内的在途改动，**四包范围外，没有碰**。四个在范围内的包加 `service/...` 全部 build 通过、测试全绿。

### 6.2 改动清单

```
 db/db.go                    |  20 +++----      ← 生产代码
 db/expense.go               |  32 ++++++--     ← 生产代码
 db/file_blob.go             |  24 ++++--       ← 生产代码
 db/file_meta.go             |  24 ++++--       ← 生产代码
 db/operation_log.go         |  24 ++++--       ← 生产代码
 service/db/expense.go       |  16 +++-         ← 生产代码（范围外，为 build 而改）
 config/config_test.go       |  31 +++++
 db/backup_test.go           | 129 ++++++++++++
 db/db_test.go               |  89 ++++++--
 db/expense_test.go          |  81 ++++++-
 db/file_blob_test.go        |  42 +++-
 db/file_meta_test.go        |  82 ++++++
 db/inquiry_test.go          |  59 ++++-
 db/operation_log_test.go    |  48 ++++
 model/file_blob_test.go     |   7 ++
 model/file_meta_test.go     |  28 ++
 model/operation_log_test.go |  38 ++
 model/token_test.go         |  30 ++（新增）
 tool/token_test.go          |  24 ++
```

`db/inquiry_test.go` 上你原有的那处改动（`Close` → `util.CloseDb`）原样保留，只在文件尾部追加了 `TestSortOrder`。无临时文件、无调试代码、无新增依赖，按约定未提交。

---

# 对话2 · 建库回到启动时、`NewTransaction` 只做读写、`/db` 的静态增删查改收归 `/service/db`

> 依据来源：
> - **本轮三条指令**：①「服务第一次启动就要创建数据库，并且在日志中打印口令」②「`NewTransaction` 与 `newTransaction` 只作为普通读写请求使用，不耦合创建数据库的逻辑」③「`/db` 包下面的静态增删查改函数，都需要去除」，职责边界落到 `/service/db`（建 handler → new 一个 transaction → 提交并关闭），目标是「一次请求只打开一次数据库」
> - **本仓库代码**：`db/db.go`、`db/{expense,file_blob,file_meta,operation_log}.go`、`service/db/*.go`、`handler/handler_test.go`、`db/*_test.go`、`handler/*_test.go`
> - **交叉印证**：`service/db/operation_log_test.go:16` 那句注释「**db包的init会在测试二进制的工作目录建库**」是 `init()` 时代写的、一直没删，与本轮指令①对上；`git show d9f0b52:db/db_test.go` 里 `TestCreate` 的形状就是「`Create(ctx)` → 从日志捞 clientToken → 用它开库」，本轮把用例翻回这个语义
>
> 结论：三条指令全部落地。`db/db.go` 退回 `init()` + `Create(GenToken)` 并把 `Create` 从 `NewTransaction` 里摘掉（净 22 行）；`/db` **删掉 13 个静态增删查改函数（195 行）**，`/db` 对外只剩「连接 / 建库 / 备份 / handler 构造器」；`/service/db` 的三个查询接管事务生命周期。**全仓库 8 个包首次全绿**（对话1 收工时 `handler` 还因在途改动红着），`-race` 干净。`service/db` 覆盖率 **35.7% → 94.2%**（新增 6 个用例、262 行），`db` 86.9% → 86.4%（静态函数删掉后分母变小，绝对覆盖没丢）。

---

## 一、指令①：建库回到启动时，口令在日志里

对话1 那句「没有init：库在第一次开事务时才建出来」确实立不住——**首次部署时用户手上一把口令都没有**，没有口令就签不出 jwt，签不出 jwt 就发不出第一个请求，「第一次请求带的口令」这个前提本身不成立。`../../../rdb` 退回：

```go
// 服务起来就把库建出来：首次部署时用户还不知道任何口令，只能由这里生成再从日志里捞
func init() {
	ctx := util.GenCtx()
	err := Create(ctx)
	if err != nil {
		panic(err)
	}
}

func Create(ctx context.Context) error {
	dbPath := config.DbPath
	token, err := tool.GenToken(ctx, tool.TokenLen)   // ← 口令由建库这一步生成
	...
```

`db/db.go:118` 那行 `Info("创建数据库，口令")` 同时打 `serverToken`（来自 `resource/jotcash.yaml`）与 `clientToken`（这里刚生成的），现在重新变成**用户唯一的入口**——对话1 §5.4 提的「这行只剩泄漏」随指令①自动作废，那条观察撤回。

`Create` 的双检写锁、`Size() > 0` 的 0 字节残骸判定、`create` 的平铺实现，一个字没动。

## 二、指令②：`NewTransaction` 与建库解耦

```diff
 func NewTransaction(ctx context.Context) (*DbTransaction, error) {
 	...
-	//库没就绪就地建出来，上层只管拿事务做业务读写，不感知建库建表
-	err = Create(ctx)
-	if err != nil {
-		return nil, err
-	}
-
-	//建库自己要拿写锁，必须排在读锁之前，顺序反了同一条协程先读后写，RWMutex不可重入会自锁
+	//读锁挡的是导入/换口令的os.Rename与建库的截断删除，读锁之间共享，业务请求彼此不排队
 	dbLock.RLock()
```

**顺带修掉的一个锁隐患**：对话1 里 `NewTransaction` 先拿写锁（`Create` 内）再拿读锁，是「同一协程先写后读」的形状，靠「`Create` 一进门就返回」才没自锁；现在这条路上只剩一把读锁，隐患从结构上消失。对话1 §5.2 提的嵌套问题**还在**，见 §六。

**行为变化（请确认是你要的）**：库文件在服务运行期间被删掉时，业务请求现在**直接报错**「打开数据库，库文件不存在」，不再拿请求带的口令悄悄建一个空库顶上。`db/db_test.go TestOpenWithoutDbFile` 的三条断言跟着翻回来了——这正是 `260915-102207-answer.md` 对话3 §4 记的那条「残留后果」（库丢了会被静默重建成空库），现在被指令②消掉了。

## 三、指令③：`/db` 的静态增删查改全部删除

删掉的 13 个（195 行）：

| 文件 | 删掉的函数 |
| ---- | ---------- |
| `db/expense.go` | `InsertExpense` / `UpdateExpense` / `DeleteExpense` / `SelectExpense` |
| `db/file_blob.go` | `InsertFileBlob` / `UpdateFileBlob` / `SelectFileBlob` |
| `db/file_meta.go` | `InsertFileMeta` / `UpdateFileMeta` / `SelectFileMeta` |
| `../../../rdb` | `InsertOperationLog` / `UpdateOperationLog` / `SelectOperationLog` |

`/db` 对外现在只剩四类，职责干净：

```
连接与事务： NewTransaction / Open / CheckToken
建库：       Create
整库文件操作：Export / Import / ChangeToken / ClearBackup
handler构造：NewMigrateHandler + 13 个 NewXxxHandler
```

`db.NewTransaction` 的**生产调用方只剩 `/service/db` 五处**（`InsertExpense`、`SelectExpense`、`DeleteExpense`、`SelectFileMeta`、`SelectOperationLog`），一个请求一次事务。

### 3.1 `/service/db` 接管的三个查询

`SelectExpense` / `SelectFileMeta` / `SelectOperationLog` 原本是一行转发，现在是「校验入参 → 开事务 → 建 handler → `AddCommit().Exec()` → `defer Close`」。以 `service/db/expense.go:47` 为例：

```go
func SelectExpense(ctx context.Context, inquiry model.ExpenseInquiry) ([]*model.Expense, int64, error) {
	inquiry, err := checkExpenseInquiry(ctx, inquiry)
	if err != nil {
		return nil, 0, err
	}

	transaction, err := db.NewTransaction(ctx)
	if err != nil {
		return nil, 0, err
	}
	defer transaction.Close(ctx)

	expenseHandler := db.NewExpenseSelectHandler(inquiry)
	err = transaction.AddCommit(expenseHandler).Exec(ctx)
	if err != nil {
		return nil, 0, err
	}
	return expenseHandler.Object, expenseHandler.Count, nil
}
```

**入参校验刻意排在开库之前**——非法排序、区间倒挂这类请求根本不该占一次连接。`InsertExpense`（4 个 handler）与 `DeleteExpense`（2 个 handler）在对话1 已经是这个形状，本轮没动。

### 3.2 「一次请求只打开一次数据库」核对

逐条走了一遍 `handler` 的入口：每个 handler 只调一个 `service` 函数，每个 `service` 函数只调一个 `service/db` 函数，每个 `service/db` 函数只开一次 `NewTransaction`。**没有任何一条请求链路会开两次库**。`CheckToken`（ping）走 `Open` + 立刻关，是探针不是事务；`Export`/`Import`/`ChangeToken` 天然要开多次（快照 + 审计），属整库文件操作，不在这条规则内。

---

## 四、测试怎么跟着改的

### 4.1 `/db` 的用例：加脚手架，不重建 API

`/db` 的 132 处调用点原先走的就是那 13 个静态函数。把每处都展开成「开事务→AddCommit→Exec→Close」四行，测试会从「断言 handler 与 inquiry 的行为」变成「每条断言都重抄一遍事务样板」，可读性直接没掉。

做法是在 `db/db_test.go` 里加**一组测试脚手架**（`execTransaction` + 13 个小写包装），调用点只改首字母大小写：

```go
// 增删查改的静态函数已经收归/service/db，事务由调用方自己开。
// 下面这组是db包用例的脚手架：把「开事务→AddCommit→Exec→Close」四步收成一行……
func execTransaction(ctx context.Context, handlers ...util.TransactionHandler) error {
	transaction, err := NewTransaction(ctx)
	if err != nil {
		return err
	}
	defer transaction.Close(ctx)
	return transaction.AddCommit(handlers...).Exec(ctx)
}

func selectExpense(ctx context.Context, inquiry model.ExpenseInquiry) ([]*model.Expense, int64, error) { ... }
```

**这不是把删掉的函数捡回来**：它们只存在于 `_test.go`，不在包的导出面上，生产代码引用不到；指令③要收的是**生产职责边界**（谁来开事务、谁来关连接），而 handler 与 inquiry 的行为仍然得在 `/db` 里测。如果你要的是连测试也一律展开成事务样板，说一声，我把这 13 个换成逐处展开。

### 4.2 翻回来的四条用例

| 用例 | 对话1 的断言 | 现在的断言 |
| ---- | ------------ | ---------- |
| `TestCreate` | `NewTransaction` 建库，口令 = 请求带的 | `Create(ctx)` 建库，口令从日志捞、并断言它满足 `tool.CheckToken` 强度 |
| `TestCreateEmptyDbFile` | `NewTransaction` 重建 | `Create(ctx)` 重建，捞出的口令能开库、别的口令开不了 |
| `TestConcurrentCreate` | 6 个协程并发 `NewTransaction` | 6 个协程并发 `Create`，仍然只有 1 条建库审计 |
| `TestOpenWithoutDbFile` | 「库文件不存在时查询应就地建库」 | 「库文件不存在时查询**应报错**」，且**不应把库文件建出来** |

另外把 `TestTransactionWithoutClaims` 的夹具从 `newTestDb` 换成 `newTestCtx`：库先建好，这条用例才只钉「Claims 缺失」这一件事，不会被「库文件不存在」顺带弄绿。

### 4.3 `handler` 的夹具

`newTestEngine` 原先靠 `db.NewTransaction` 顺手建库、返回写死的 `testClientToken`。现在改成 `db.Create` + 从日志捞口令返回（`testClientToken` 常量随之删除，它只在这一处用过）。捞口令那段**只临时放开 Info 级并立刻还原**，不用 `catchLog`，免得把整条用例的日志都吞进 buffer。另加一个 `execTransaction(t, clientToken, handlers...)` 夹具，四处铺数据的地方改掉；`newTestExpenses` 顺势把「插三条 + 软删一条」并进**同一次事务**，少开一次库。

### 4.4 新增 `../../../service/repo`（6 个用例，262 行）

事务逻辑从 `/db` 搬到了 `/service/db`，覆盖率也得跟过来（`35.7% → 94.2%`）。钉的是新边界上真正会错的东西：

- `TestInsertExpense` —— 明细/文件/存档/审计**四张表同一次事务**：主键冲突时，文件元数据与审计都必须跟着回滚，不能只落下一半
- `TestSelectExpense` —— 默认排序兜成「支出日期最新在前」（db 层默认是 `id asc`）、枚举外的 `Deleted`、白名单外的 `Sort` 都报错
- `TestDeleteExpense` —— 软删 + 删除审计同一次事务；区间倒挂报错
- `TestSelectFileMetaAndOperationLog` —— 两个列表都最新在前，建库那条「系统初始化」排在最后
- `TestToken` —— 口令强度校验排在动库文件之前，被拦下的换口令不影响原库；换完新口令生效、旧口令失效
- `TestExportImport` —— 导出再导回，原数据还在；非数据库文件拒绝导入

---

## 五、验证

```
gofmt -l .        # 无输出
go vet ./...      # 干净

go test ./... -count=1 -cover
ok  config       93.8%      ok  db            86.4%      ok  handler        84.6%
ok  model       100.0%      ok  service/db    94.2%      ok  service/expense 92.4%
ok  tool         93.9%      ok  service/expense/base_csv 95.1%

go test ./db/ ./handler/ ./service/db/ -count=1 -race -timeout 900s
ok  db 79.034s   ok  handler 55.776s   ok  service/db 14.827s      # 无 data race
```

**8 个包全绿**——对话1 收工时 `handler` 还因你在途的 `ping.go` 改动编译不过，本轮起点已经是好的，收工时整仓库干净。

`db` 覆盖率 86.9% → 86.4% 是分母变化：删掉的 13 个静态函数原本各带 4~5 条已覆盖语句，绝对覆盖没有减少。

### 5.1 改动清单

```
 db/db.go                       |  22 ++--          ← init 回来、Create 用 GenToken、NewTransaction 摘掉建库
 db/expense.go                  |  60 -----         ← 删 4 个静态函数
 db/file_blob.go                |  45 ----          ← 删 3 个
 db/file_meta.go                |  45 ----          ← 删 3 个
 db/operation_log.go            |  45 ----          ← 删 3 个
 service/db/expense.go          |  14 ++-           ← SelectExpense 接管事务
 service/db/file_meta.go        |  14 ++-           ← SelectFileMeta 接管事务
 service/db/operation_log.go    |  14 ++-           ← SelectOperationLog 接管事务
 service/db/db_test.go          | 262 ++（新增）
 db/db_test.go                  | 239 ++--          ← 脚手架 + 四条用例翻回来
 db/{expense,file_blob,file_meta,operation_log,backup,migrate}_test.go | 222 ++--  ← 调用点改大小写
 handler/handler_test.go        |  36 ++-           ← newTestEngine 改走 db.Create，加 execTransaction 夹具
 handler/{expense,expense_insert,file_meta,operation_log}_test.go | 44 ++-
```

`handler/ping.go` 那 18 行是你自己的在途改动，没碰。无临时文件、无调试代码、无新增依赖，按约定未提交。

---

## 六、本轮之后还剩的问题

### 6.1 嵌套 `NewTransaction` 的死锁地雷 —— 现在更值得防了

`sync.RWMutex` 的规则：读锁持有期间若有写者在等，**新的读锁必须排在写者后面**。`/service/db` 现在是持锁方，一旦以后写出

```go
transaction, _ := db.NewTransaction(ctx)   // 持读锁
defer transaction.Close(ctx)
count, _ := SelectExpense(ctx, inquiry)    // ← 同一协程又要一把读锁，此时若 Import 在等写锁 → 死锁
```

就会卡死。**当前五个调用点都是平的**（逐个核对过：`InsertExpense`/`DeleteExpense`/三个 `Select` 都在开事务之前把校验做完），所以不是现存 bug。指令③把「一次请求一次事务」立成了规矩，正好也是这条地雷的解药——建议在 `NewTransaction` 上补一句注释「同一协程内不得嵌套」，把规矩写进代码。**没擅自加。**

### 6.2 `Create` 里 `tool.GenToken` 失败是 `panic` 不是 `return err`

随指令①原样退回（`260915-130316-answer.md` §3.3 也扫到过，当时归入「不用改」）。`init()` 那条路上 panic 无所谓，但 `Create` 现在还被 `handler`/`service/db` 的测试夹具直接调用，那边 panic 比返回错误难定位。改成 `return err` 是两行，**没动**。

### 6.3 对话1 §五里仍然成立的三条

- **`Open` 的读锁在交出连接那一刻就放掉**（`db/db.go:122`）——作用域错位，唯一生产调用方 `CheckToken` 下一行就关连接，窗口以微秒计
- **`import_` 失败时会在备份目录留下半个 `originPath`**（`db/backup.go:257`）——与兄弟函数 `changeToken` 口径不一致，修法一行
- **金额区间与排序都过一遍 float64**（`db/expense.go:69`）——「金额存文本」的既定代价

对话1 §5.4（建库日志打客户端口令）随指令①**作废**：那行现在是用户唯一拿到口令的渠道，必须打。
