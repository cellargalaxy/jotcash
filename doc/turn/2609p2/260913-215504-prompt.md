# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260913-215504-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
    - [记账系统功能与模型.md](../../decisions/%E8%AE%B0%E8%B4%A6%E7%B3%BB%E7%BB%9F%E5%8A%9F%E8%83%BD%E4%B8%8E%E6%A8%A1%E5%9E%8B.md)
- 代码风格：
- 交付路径：

## 具体任务

+ 代码风格，注释多寡，函数边界，都需要严格按照当前代码的风格
+ 代码轮子优先使用：https://github.com/cellargalaxy/go_common
    + 其次使用github上有积极维护且star多（至少需要上百）的代码库
    + 最后真的没有方案，才允许自己造轮子
+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 之前提到，用户在前端填写口令之后，需要有一个接口用于验证口令填写是否正常
+ 我写里这个接口的功能：[handler.go](../../../handler/handler.go)的engine.POST (util.PathPing, validate, Ping)
    + validate校验里后端口令，Ping校验里前端口令
+ [service](../../../service)包是用于编写业务逻辑的
+ service的[db](../../../service/db)包，用于编写包含业务逻辑的数据库读写逻辑，例如某个功能的多个表的在一个事务里的逻辑
    + 上层不能直接调用/db包，只能调用/service/db
    + 因为/db没有业务逻辑，可能会缺少必要的参数校验
    + 参数校验与业务逻辑有关，因此/service/db才会有足够的业务逻辑校验
+ 第一个问题：这个ping的代码与调用链路，是否有错/有漏
+ 第二个问题：能从发起http的高层级，写对应的单元测试吗，如果能真的发起http来写单测，能降低之后前端的联通成本
+ 第三个问题：接下来选一个比较简单的功能，来完成从[handler](../../../handler)开始的完整代码链路
    + 先不需要写代码，先选择，提供大概的技术方案，我先review以下方案