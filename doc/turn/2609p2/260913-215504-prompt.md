# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260913-215504-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
    - [记账系统功能与模型.md](../../decisions/%E8%AE%B0%E8%B4%A6%E7%B3%BB%E7%BB%9F%E5%8A%9F%E8%83%BD%E4%B8%8E%E6%A8%A1%E5%9E%8B.md)
- 代码风格：
- 交付路径：

## 具体任务

+ 代码风格，注释多寡，函数边界，都需要严格按照当前代码的风格
+ 代码轮子优先使用：https://github.com/cellargalaxy/go_common
    + 其次使用github上有积极维护且star多（至少需要上百）的代码库
    + 最后真的没有方案，才允许自己造轮子
+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 之前提到，用户在前端填写口令之后，需要有一个接口用于验证口令填写是否正常
+ 我写里这个接口的功能：[handler.go](../../../handler/handler.go)的engine.POST (util.PathPing, validate, Ping)
    + validate校验里后端口令，Ping校验里前端口令
+ [service](../../../service)包是用于编写业务逻辑的
+ service的[db](../../../service/db)包，用于编写包含业务逻辑的数据库读写逻辑，例如某个功能的多个表的在一个事务里的逻辑
    + 上层不能直接调用/db包，只能调用/service/db
    + 因为/db没有业务逻辑，可能会缺少必要的参数校验
    + 参数校验与业务逻辑有关，因此/service/db才会有足够的业务逻辑校验
+ 第一个问题：这个ping的代码与调用链路，是否有错/有漏
+ 第二个问题：能从发起http的高层级，写对应的单元测试吗，如果能真的发起http来写单测，能降低之后前端的联通成本
+ 第三个问题：接下来选一个比较简单的功能，来完成从[handler](../../../handler)开始的完整代码链路
    + 先不需要写代码，先选择，提供大概的技术方案，我先review以下方案

### 对话2

+ go_common的ValidateGin我改了一下，支持自定义传入结构体
+ c.JSON (http.StatusOK, util.NewHttpRespByErr (model.PingData{Ip: util.GetIP (), ServerName: util.GetServerName (),
  Timestamp: time.Now ().Unix ()}, err))
    + 是符合预期的，报错归报错，还是需要将服务的基础信息返回
+ 校验口令，Claims为空
    + 在ValidateGin里会校验，无须在CheckToken里重复校验
    + CheckToken用于生成口令，不用来校验口令是否正确
    + err在CheckToken内部已经封装过，err是什么就给前端返回什么，不要加工
+ 前端的jwt由自己签名，不通过后端的接口获取，后端也无须有接口

### 对话3

+ go_common/util/gin.go:134 的 &claims
    + go_common我已修复，你再检查一下
+ jwt 走 query 会把前端口令明文写进日志
    + 依然复用go_common的ValidateGin
    + 要求前端jwt只允许通过header传，不要在query里传即可
    + 如果前端不遵守约定。非要通过这种暴露的方式传，为什么不直接将口令发布到报纸上公之于众
+ ping流程还有问题吗

### 对话4

+ 参考ping的链路代码，实现 审计查看

### 对话5

+ 参考ping的链路代码，实现 文件列表

### 对话6

+ 参考ping的链路代码，实现 更换口令

### 对话7

+ 参考ping的链路代码，实现 数据库导出

### 对话8

+ 参考ping的链路代码，实现 数据库导入

### 对话9

+ 参考ping的链路代码，实现 明细查询

### 对话10

+ 参考ping的链路代码，实现 明细删除
+ 正确的做法是分别创建deleteHandler与operationLogInsertHandler，然后将两个Handler一起放进一个db.Transaction
    + 是组装的思路
+ 而不是像你写的，在expenseDeleteHandler里耦合operationLog
+ 单测有覆盖空删除的情况吗，不允许没有任何where条件导致全量数据删除
+ 你的单测肯定有问题？util.NewDeleteHandler是物理删除

### 对话11

+ 参考ping的链路代码，实现 明细入库
+ 时间用这些
    + util.ParseStr2Time (ctx, util.DateLayout_2006_01_02, csvValue (columns, line, model.CsvExpenseDate), nil)
    + util.DateLayout_2006_01_02
+ genExpenseCsv为什么要手动拼接，需要复用go_common的csv函数
+ newCsvFile这种函数的逻辑太过单薄，属于过度拆分，去除该函数
+ snapshotMeta, snapshotBlob := newCsvFile (snapshotFileName (filename), snapshot)
    + 这是干嘛，为什么要这么做，哪冒出来的逻辑
+ 代码风格严重偏离原有风格，简直是两个人写的代码一样

### 对话12

+ 对话11让AI实现 明细入库，但是降智严重，代码我都回滚里，你也不需要去看之前的commit
+ 你需要参考ping的链路代码，实现 明细入库
+ 阅读本文件与[260913-005452-prompt.md](260913-005452-prompt.md)的全部对话，有很多要点我都是三番五次提醒但是不如得不到遵守
    + 你需要先梳理需要遵守的代码风格，注释多寡，函数边界，都需要严格按照当前代码的风格，再去开始写代码
+ 注释严禁「引用文档条款」只留「不看就会改错」，不写过程注释
+ 代码严禁过度拆分
+ 哪来的快照，为什么你要瞎编快照的功能
+ 我给Claims增加里AccountingCurrency字段
+ util.GenId ()已经优化，不会超出 JS 安全整数
+ 现在没有「复制自明细 X」的功能了
+ 前端上传的文件未来会有多种不同的格式，需要使用不同的解析器来进行解析
    + 不同的解析器在[expense](../../../service/expense)里实现
    + 例如本次的标准CSV在[base_csv](../../../service/expense/base_csv)里实现
    + 需要使用设计模式，，收纳规范不同格式的解析器
+ 不能使用文件扩展名来判断解析器，同样的CSV/PDF，内容的格式可能完全不同，需要使用不同的解析器

+ Parser的Support严格一点，需要CSV的表头与columns完全一致才Support
+ 标准CSV新增一些字段与规则
    + 新增AccountingCurrency
    + 如果ExchangeRate填写里，AccountingCurrency必填。AccountingCurrency填写里，允许ExchangeRate留空，通过接口查询
    + AccountingAmount不在CSV表格中，只允许通过系统计算得到
    + 新增AmortizationMonths