# 对话1 · jotcash 代码规范标准（从现有代码 + 历次对话反向梳理）

> 依据来源：
> - **代码（唯一事实源）**：当前工作区全部 Go 文件，非测试 2511 行 / 150 个函数 / 44 条注释，测试 3675 行；`go build ./...`、`gofmt -l .`、`go vet ./...` 全绿。
> - **对话（规范的立法记录）**：`doc/turn/2609p2/260913-005452-prompt.md` 对话 4/5/6/7/10/11/13/14/15/16/17/18；`doc/turn/2609p2/260913-215504-prompt.md` 对话 1/2/3/10/11/12；`doc/turn/2609p2/260911-220830-prompt.md` 对话 2/3/4；`doc/turn/2609p0/260904-*` 全系列。
> - **上游轮子**：`go_common@v0.0.0-20260914025533-40cbc559ce24` 的 `util/gorm.go`、`util/gin.go`（本仓库的错误/日志/Handler 形态直接继承自它）。
> - **commit 历史**：244 个 commit，message 一律是 `1`，**不承载任何规范信息**；规范信息全部在 prompt 与代码里。历史 commit 另有被明文作废的部分（`260911-220830` 对话1「历史提交的那版方案与代码不被采纳」、`260913-215504` 对话12「代码我都回滚里，你也不需要去看之前的commit」），故本规范不引用任何历史 commit 的代码形态。
>
> 范围：本轮只输出规范，**未改动任何生产代码或测试**。第十六节是按本规范对现有代码做的体检，列出偏离项但未擅自修改。

---

## 〇、这份规范的效力等级

冲突时按此顺序取胜：

| 级别 | 来源 | 说明 |
| --- | --- | --- |
| 1 | 当前工作区代码 | 「严格按照当前代码的风格」——`260913-005452` 对话16/17/18、`260913-215504` 卷首，三番五次 |
| 2 | prompt 中的明文指示 | 尤其 `260913-215504` 对话12 那一串「严禁」 |
| 3 | `go_common` 的既有形态 | 优先复用，不自己造 |
| 4 | Go 社区通例 | 仅在上面三级都没说时才生效 |

**Go 社区通例排在最后**，这一点要特别注意：本仓库的 `this` 接收器、`//` 无空格注释、几乎不写函数 doc comment，都与社区通例相反，但它们是级别 1，不得"纠正"。

---

## 一、分层与包职责：函数该落在哪一层

实测依赖图（`go list -deps`，无环）：

```
model   ← 谁都可以依赖，它谁都不依赖
tool    → model
config  → model tool
db      → model tool config
exchange_rate → （无内部依赖）
service/expense        → model exchange_rate
service/expense/base_csv → model exchange_rate service/expense
service/db → model tool config db
service    → model tool config service/db service/expense service/expense/base_csv
handler    → model tool config service service/db static
```

| 包 | 职责 | 硬约束 |
| --- | --- | --- |
| `model` | 只放 struct、常量枚举、`String()`、`TableName()` | **不得包含任何业务逻辑或连接性代码**（`260913-005452` 对话1/5）。唯一例外是 `model/config.go` 的 `init()` 调 `util.Init(DefaultServerName)`，这是 go_common 的日志初始化契约 |
| `tool` | 无业务语义的工具：JWT 收发、口令生成/强度校验、ctx 里的 Claims 存取 | 不碰数据库，不碰 HTTP |
| `config` | 配置文件加载与热更 | 路径类常量（`ListenAddress`/`ConfigPath`/`DbPath`/`DbBackupPath`）是**不变常量，不允许可配**（`260913-005452` 对话10） |
| `db` | 数据库连接 + **无业务逻辑**的增删查改 | 连接性代码必须在包内完成，上层不感知（`260913-005452` 对话6）。不做参数校验 |
| `exchange_rate` | 汇率获取 | 外部接口对接**挂起中**，现写死返回 1（`exchange_rate.go:11` 的 `//todo`） |
| `service/expense` | 解析器注册表 + 派生字段填充 | 解析器接口与派生逻辑在此，具体格式在子包 |
| `service/expense/<格式>` | 单一格式的解析器实现 | 只填文件里给了的字段，派生字段留给上层 |
| `service/db` | **带业务逻辑**的数据库读写：多表同事务、参数校验、默认值兜底 | 上层**只能**调 `service/db`，不能直接调 `/db`（`260913-215504` 对话1）。理由：`/db` 没有业务逻辑，会缺必要的参数校验 |
| `service` | 业务编排，出参统一 `(any, error)` 以喂 `util.NewGinPost` | |
| `handler` | 路由注册、多段表单/二进制流这类 gin 专属的活 | 能用 `util.NewGinPost` 的绝不手写 handler |
| `static` | `embed.FS` | 6 行，不要长大 |

**当前生产代码 100% 遵守"上层不直接调 `/db`"**：只有 `service/db/{db,expense,file_meta,operation_log}.go` 四个文件 import 了 `jotcash/db`。测试可以越层（`handler/*_test.go` 直接调 `db.Create` 建库），这是允许的例外。

---

## 二、命名

### 2.1 接收器

**一律 `this`**，36 个方法零例外。值接收器还是指针接收器按是否改字段决定：

- 值接收器：`func (this Expense) String() string`、`func (this ExpenseInquiry) Where(...)`
- 指针接收器：`func (this *ExpenseUpdateHandler) Transaction(...)`（要写 `this.Count`）

`String()` 用值接收器还有一个刻意的用途，见 §8.3。

### 2.2 变量名（全仓通用词表）

| 名字 | 含义 | 出现次数 |
| --- | --- | --- |
| `object` / `objects` | 实体单个 / 列表 | 107 / 141 |
| `inquiry` | 查询条件 | 162 |
| `handler` | 事务 Handler | 106 |
| `one` | `for _, one := range` 的元素 | — |
| `i` | `for i := range` 的下标 | — |
| `tx` | `*gorm.DB` 事务句柄 | — |
| `gormDb` / `sqlDb` | gorm 连接 / database/sql 连接 | — |
| `ctx` | `context.Context`，**永远是第一个参数** | — |

新代码需要给"某个实体的一条记录"命名时，用 `object`，不要用 `item`、`record`、`row`、`e`、`exp`。

### 2.3 中英文边界

- **标识符一律英文**，且不做死板直译：`CardLast4` 而不是 `CardLastFour`（`260913-005452` 对话2 明文点名）
- **注释、日志、错误文案一律中文**
- 领域名词用中文文档里的标准名：「记账币种/记账金额」不是「本位币」（`260911-220830` 对话3 采纳）；「支出类型」不是「标签」（同上）；「口令」不是「密码」（`260911-220830` 对话2 采纳）；前端动作叫「解锁」，服务端只有「口令校验」
- 私有函数与 Go 关键字冲突时加下划线后缀：`import_`（`db/backup.go:157`）

### 2.4 文件名

`snake_case.go`，与主类型同名：`model/operation_log.go` 放 `OperationLog`。枚举常量与它修饰的实体放**同一个文件**（`260913-005452` 对话6 明文），例如 `OperationTypeXxx` 和 `ResultXxx` 都在 `model/operation_log.go`。

---

## 三、文件与包内组织顺序

固定顺序，不要打乱：

```go
package xxx

import (
    // 标准库
    ""
    // 空行
    // 第三方 + 本仓库（同一组，按路径字母序，gofmt 自动排）
)

// 1. 排序白名单 var + 默认排序 const
// 2. 类型别名 / Inquiry 定义
// 3. Where / Order / Limit
// 4. NewXxxHandler 构造 + Handler struct + Transaction 方法
// 5. 对外的 Insert/Update/Delete/Select 函数
```

`db/expense.go`、`db/operation_log.go`、`db/file_meta.go`、`db/file_blob.go` 四个文件是这个顺序的样板，新增实体照抄。

### 3.1 import 分组

只有两组：标准库 / 其余，中间一个空行。本仓库包和第三方包**混在同一组**（gofmt 默认行为），不要人为再分第三组。重名 import 才起别名，且用下划线风格：`common_model "github.com/cellargalaxy/go_common/model"`。

### 3.2 空行：成对函数要贴在一起

**导出包装 + 非导出实现** 这一对之间**不留空行**，视觉上是一个单元：

```go
func Create(ctx context.Context) error {
    ...
}
func create(ctx context.Context, dbPath, token string) error {
    ...
}
```

已成对的：`Create/create`、`Open/open`、`Transaction/transaction`、`Export/export`、`Import/import_`、`ChangeToken/changeToken`、`ClearBackup/clearBackup`。

同样贴紧的还有：`String()` 与 `TableName()`、`Order()` 与 `Limit()`、`GetClaims()` 与 `SetClaims()`、`ConfigHandler` 的三个方法。

其余函数之间留一个空行。函数体内用空行分段，段与段之间是"取参数 / 加锁 / 干活"这种粗粒度，不要每两行就空一行。

---

## 四、函数边界（被点名次数最多的一条）

### 4.1 量化基线

| 指标 | 现值 | 新代码应落在 |
| --- | --- | --- |
| 函数长度中位数 | **8 行** | 10 行上下 |
| p90 | **24 行** | 不超过 30 行 |
| 最长 | **63 行**（`db/expense.go:28` `ExpenseInquiry.Where`） | 只有"平铺的 if 列表"允许这么长 |

`ExpenseInquiry.Where` 63 行不是反例，是**正例**：它是 18 个平级的 `if` 拼 where 条件，拆成子函数只会更难读。判据不是行数，是**分支深度与职责数**。

### 4.2 两条对称的红线

**红线 A —— 严禁过度拆分**（`260913-215504` 对话11/12）

原话：「`newCsvFile` 这种函数的逻辑太过单薄，属于过度拆分，去除该函数」。

判据：一个函数如果只是把 2~4 行原地代码换个名字包起来、且只有一个调用方、且不含分支，就是薄封装，内联掉。

反面样本（历史上被删掉的）：`newCsvFile`、`genExpenseCsv`、`parseCsvColumns`、`fillExpenseDerived`、`tokenWriter` 那套。

**红线 B —— 严禁职责焊死**（`260913-215504` 对话10）

原话：「正确的做法是分别创建 `deleteHandler` 与 `operationLogInsertHandler`，然后将两个 Handler 一起放进一个 `db.Transaction`，是组装的思路；而不是像你写的，在 `expenseDeleteHandler` 里耦合 `operationLog`」。

判据：一个函数/Handler 只干一件事，多件事靠**组装**（把多个 Handler 塞进一个 `db.Transaction`）完成。

`service/db/expense.go:36-42` 是这条的标准落地：

```go
fileBlobHandler := db.NewFileBlobInsertHandler(fileBlob)
fileMetaHandler := db.NewFileMetaInsertHandler(fileMeta)
expenseHandler := db.NewExpenseInsertHandler(expenses...)
operationLogHandler := db.NewOperationLogInsertHandler(&operationLog)
return db.Transaction(ctx, fileBlobHandler, fileMetaHandler, expenseHandler, operationLogHandler)
```

### 4.3 导出/非导出的切分法则

`db` 包把"环境"和"逻辑"切开，是全仓最该照抄的边界形态：

| 层 | 干什么 | 例 |
| --- | --- | --- |
| **导出函数** | 读 `config` 常量、从 ctx 取口令、**加锁**、调非导出函数 | `db.Open`（`db/db.go:150-170`） |
| **非导出函数** | 参数**全部显式传入**（`dbPath`、`token`），不碰锁、不读全局 | `db.open`（`db/db.go:172-182`） |

好处是非导出函数可被测试直接构造任意环境调用（`db/db_test.go:46` 的 `create(ctx, config.DbPath, testClientToken)`），且锁的持有范围一眼可见。新增需要加锁或读全局配置的能力，照这个切。

### 4.4 创建归创建，打开归打开

`260913-005452` 对话14 明文：「数据库文件，创建归创建，打开归打开，不要打开的函数又通过 create 耦合创建；打开的时候，没有文件或者文件损坏或者口令错误就报错，不要 create」。

现状 `db/open()` 首行就是 `if !existDb(...) { 报错 }`，不兜底建库。**任何"读路径顺手补写"的诱惑都要拒绝。**

---

## 五、错误与日志

### 5.1 唯一句式

错误产生点，**先 logrus 后 errors.Errorf，两条文案保持一致**：

```go
logrus.WithContext(ctx).WithFields(logrus.Fields{"err": err}).Error("连接数据库，异常")
return nil, errors.Errorf("连接数据库，异常: %+v", err)
```

要点：

- `logrus.WithContext(ctx)` 一定要带 ctx，logId 靠它串联
- **`WithFields(logrus.Fields{})` 即使为空也要写**，不要退化成 `logrus.WithContext(ctx).Error(...)`
- 文案格式：**`动作，问题`** / **`动作，完成`**，中间是**全角逗号**。带上下文时 `动作，问题: %+v`（冒号后一个半角空格）
- 只用 `github.com/pkg/errors` 的 `errors.Errorf`。全仓 **0 处** `fmt.Errorf`、`errors.New`、`errors.Wrap`、`errors.WithStack`
- 动作名用中文业务词或表名：「连接数据库」「插入file_blob」「解析CSV」「校验口令」

### 5.2 日志级别

| 级别 | 用于 | 样本 |
| --- | --- | --- |
| `Error` | 系统/IO/连接/配置层面的异常，或"不该发生"的状态 | 「连接数据库，异常」「自动建表，连接为空」「排序，不在白名单内」 |
| `Warn` | **用户输入或业务校验不过**；以及"入参为空所以什么都没干" | 「解析CSV，支出金额非法」「查询，时间区间倒挂」「插入file_blob，为空」 |
| `Info` | 成功完成、启动期事实 | 「导出数据库，完成」「加载配置」 |

另有一处刻意的 `Warn`：初始前后端口令 `db/db.go:147-148`，用 Warn 是为了在默认级别下必定可见，且**允许写进日志文件**（`260913-005452` 对话14 明文否决了此前的 `tokenWriter` 方案）。

### 5.3 错误的传递：不重复包装

下层返回的 err **原样 `return err`**（全仓 46 处），不再加一层文案。理由见 `260913-215504` 对话2：「err 在 CheckToken 内部已经封装过，err 是什么就给前端返回什么，不要加工」。

唯一允许"不在原地记日志"的情形：错误文案是个**片段**，由调用方拼前缀并在那里记日志。例如 `base_csv.go:81` 返回 `支出日期非法: %+v`，由 `base_csv.go:70` 记「解析CSV，行非法」并拼成 `解析CSV，第%d行，%s`。

---

## 六、注释：多寡与形态

### 6.1 密度基线

**44 条注释 / 2511 行 / 150 个函数 ≈ 每 57 行一条，每 3.4 个函数一条。**

这个密度是刻意压出来的，新代码不得显著超过。

### 6.2 只写两类，其余一律删

`260913-215504` 对话12 原话：「注释严禁『引用文档条款』只留『不看就会改错』，不写过程注释」。

| 允许 | 样本 |
| --- | --- |
| **踩过的坑 / 不看就会改错** | `../../../rdb`「gin 会把 `*gin.Context` 放回 sync.Pool 给下个请求复用，而 database/sql 的 awaitDone 协程可能在请求结束后才去读 ctx，交给 gorm 之前必须脱钩」 |
| **非显然的取值语义** | `model/expense.go:78`「金额允许 0 与负数，用指针区分『不限』与『限定为 0』」 |
| **常量组的中文释义** | `model/expense.go:12-14` 的 `DeletedNo/All/Only` 行尾注释 |

| 禁止 | 说明 |
| --- | --- |
| 引用文档条款号（`§8.1`、`E-1`、`D-2`） | `260913-215504` 对话12 明令删除，历史上被批量清理过 |
| 过程注释（"先查库再更新"） | 代码自己说得清的不写 |
| Go 风格的函数 doc comment（`// Xxx does ...`） | 全仓 **0 条**。不要因为是导出函数就补 |
| 包注释 `// Package xxx ...` | 全仓 0 条 |

### 6.3 形态

**位置决定有没有空格**，这条全仓零例外：

| 位置 | 写法 | 样本 |
| --- | --- | --- |
| 声明之上（包级） | `// ` **有**空格 | `// CSV表头的列名`（`model/expense.go:17`） |
| 函数体内 | `//` **无**空格 | `//随机结果可能全是字母，不满足强度就重来`（`tool/token.go:34`） |
| 行尾 | `//` **无**空格 | `//只查未删除`（`model/expense.go:12`） |

一条注释就是一行，不写多行块注释，不写 `/* */`。

### 6.4 `todo` 的写法

```go
//todo
//具体实现先挂起，代码写死返回1
```

小写 `todo`，无空格，第二行说明挂起原因。用于**已知会做但本轮明确不做**的位置（`exchange_rate/exchange_rate.go:11`）。

---

## 七、并发与资源

1. **全局并发收敛到一把读写锁**：`db/db.go:23` 的 `var dbLock sync.RWMutex`。`260913-005452` 对话15 明文：「db 包里的并发控制要不收敛到一个读写锁中，并发性能低一点可以接受」。
   - `RLock`：读写数据（`Open`/`Transaction`/`Export`）
   - `Lock`：动库文件本身（`Create`/`Import`/`ChangeToken`/`ClearBackup`/`autoMigrate` 的建表分支）
2. **ctx 脱钩**：交给 gorm 之前必须 `detachCtx`（`db/db.go:45`），否则 gin 复用 `*gin.Context` 会串请求。
3. **双检锁**：`autoMigrate`（`db/migrate.go:50-65`）先 `RLock` 读标记，再 `Lock` 复检，标准形态。
4. **关闭资源用 `defer`**，且走 go_common：`defer util.CloseIo(ctx, file)`、`defer Close(ctx, gormDb)`。
5. **临时文件失败即删**：`db/backup.go` 的 `import_`/`changeToken` 每个错误分支都 `util.RemoveFile(ctx, backupPath)`，成功路径靠 rename 消费掉。这是刻意不用 `defer` 的地方——成功时文件要被 rename 走，defer 会误删。

---

## 八、`model` 包专项

1. **只有 struct、常量、`String()`、`TableName()`**。零业务逻辑、零连接代码。
2. **字段标签成对**：`json:"snake_case"` + `gorm:"column:snake_case;..."`，列名显式写出不靠推导。
3. **敏感字段两种遮蔽法，按用途选**：

| 手法 | 用于 | 样本 |
| --- | --- | --- |
| `json:"-"` | 这个字段**根本不该出现在任何 JSON 里** | `Config.ServerToken`、`FileBlob.FileData` |
| `String()` 值接收器里置空后再 marshal | 字段**必须能序列化**（要进 JWT / 要收请求体），但**不能进日志** | `Claims.ClientToken`、`ChangeTokenRequest.NewToken` |

第二种能成立，靠的正是 §2.1 的值接收器——`this.ClientToken = ""` 只改副本。`model/claims_test.go` 同时断言了"JWT 载荷要带上 ClientToken"和"`String()` 里不能有"，两条一起锁住这个设计。

4. **`decimal.Decimal` 必须声明 `type:varchar(32)`**。原因写在 `model/expense.go:32`：SQLite 按声明类型做亲和判定，`decimal`/`numeric` 落到 NUMERIC 亲和会把数字文本转成浮点丢精度。新增金额/汇率字段照抄。
5. **Id 一律 `int64` + `primaryKey;autoIncrement:false`**，值由 `util.GenId()` 生成（`260913-005452` 对话2）。
6. **时间字段沿用 gorm 约定**：`CreatedAt`/`UpdatedAt`/`DeletedAt gorm.DeletedAt`（`260913-005452` 对话2）。
7. **每个实体配一个 `XxxInquiry`**，字段形态固定：切片 = `in`，`XxxStart`/`XxxEnd` = 区间，`XxxLike` = 模糊，外加 `Sort`/`Page`/`PageSize`。允许为将来的业务需要提前加查询字段（`260913-005452` 对话6）。
8. **路径常量集中在 `model/path.go`**，含表单字段名（`ImportFileKey`/`ExpenseFileKey`）。

---

## 九、`db` 包专项

1. **`XxxInquiry` 是 `model.XxxInquiry` 的类型别名**（`type ExpenseInquiry model.ExpenseInquiry`），只为挂 `Where`/`Order`/`Limit` 三个方法，不新增字段。
2. **排序走字符串白名单**。`260913-005452` 对话7 改判：不用 `map[int]string`，让上层直接传字符串更直观，`Inquiry` 里查白名单，**不在白名单就 `Error` + 报错**，不是静默降级。
   ```go
   var expenseSortMap = map[string]string{
       "expense_amount asc": "cast(expense_amount as real) asc", //金额列是文本，按文本排序会串位
       ...
   }
   const expenseSortDefault = "id asc"
   ```
   key 是给上层看的语义，value 是真正拼进 SQL 的表达式，两者可以不同——这就是防注入的落点。
3. **`like` 必须转义**：统一走 `db/inquiry.go` 的 `likeValue()`（转义 `\` `%` `_`），SQL 侧写 `like ? escape '\'`。
4. **Handler 优先复用 go_common 泛型**：`util.NewInsertHandler[T]`、`NewUpdateHandler[T]`、`NewDeleteHandler[T]`、`NewSelectHandler[T]`。只有泛型满足不了才自己写，现有两个自定义 Handler 各有明确理由：
   - `ExpenseUpdateHandler`（`db/expense.go:104`）：需要 version 乐观锁 + `Omit("id","created_at","deleted_at")`
   - `FileBlobInsertHandler`（`../../../rdb`）：需要 `clause.OnConflict{DoNothing: true}` 做按 hash 去重
5. **自定义 Handler 的固定骨架**：`New...` 构造 → `struct{ Object; Count int64 }` → `Transaction(ctx, tx) error`；空入参 `Warn` 后 `return nil`；结束 `Info("...，完成")` 带 `count`。
6. **`db` 包不做业务校验**。`NewExpenseDeleteHandler` 里那两行（强制 `DeletedNo`、清掉 `Page/PageSize`）是**安全兜底不是业务校验**，业务校验在 `service/db`。

---

## 十、`../../../service/repo` 专项

这一层的存在理由就是"参数校验与业务逻辑有关，所以要有足够业务语义的地方来做"（`260913-215504` 对话1）。固定形态：

```go
const expenseSortDefault = "expense_date desc"      // 业务默认排序，覆盖 db 层的 id asc

var expenseDeleteds = map[int]bool{...}             // 枚举白名单

func SelectExpense(ctx, inquiry) (...) {
    inquiry, err := checkExpenseInquiry(ctx, inquiry)   // 先校验+补默认
    if err != nil { return nil, 0, err }
    return db.SelectExpense(ctx, inquiry)               // 再下沉
}

func checkExpenseInquiry(ctx, inquiry) (model.ExpenseInquiry, error) { ... }
```

必做的四件事：

1. **枚举白名单校验**，非法值 `Warn` + 报错，**不静默降级**（`service/db/expense.go:58` 的注释把理由写死了：「db 层对没见过的取值会按『只查未删除』处理，静默少查数据不如直接报错」）
2. **区间倒挂校验**：`checkTimeRange`、`checkAmountRange`（`service/db/inquiry.go`）
3. **分页兜底**：`checkPageSize`，`<=0 → 20`，`>200 → 200`。理由在注释里：db 层 `pageSize<=0` 是不加 limit，上层不兜底会整表拉出来
4. **业务默认排序**：`Sort == ""` 时补上，因为 db 层默认 `id asc` 不符合列表页「最新在前」的预期

写操作在这一层组装审计日志（`model.OperationLog`）与业务 Handler，放进同一个 `db.Transaction`（见 §4.2 红线 B）。

---

## 十一、`handler` 专项

1. **能用 `util.NewGinPost` 就不手写**：`NewGinPost("明细查询", service.SelectExpense)`。它已经包了参数绑定、入参日志、`NewHttpRespByErr`。
2. **只有 gin 专属的活才手写 handler**：多段表单取文件（`InsertExpense`、`Import`）、二进制流响应（`Export`）。
3. 手写 handler 的骨架：
   ```go
   func Xxx(c *gin.Context) {
       logrus.WithContext(c).WithFields(logrus.Fields{"claims": tool.GetClaims(c)}).Info("动作")
       ...
       c.JSON(http.StatusOK, util.NewHttpRespByErr(service.Xxx(c, ...)))
   }
   ```
   **HTTP 状态码永远 200**，业务码在 body 的 `code` 里（go_common `HttpResp` 契约）。
4. **`*gin.Context` 直接当 `context.Context` 传**，不额外包。
5. **中间件链**：`engine.POST(path, validate, 业务)`，`validate` 校验后端口令，业务侧再校验前端口令。
6. **报错也要返回基础信息**：`Ping` 即使 `err != nil` 也把 `PingData{Ip, ServerName, Timestamp}` 带回去（`260913-215504` 对话2 明文确认）。
7. **JWT 只从 header 取**，不支持 query（`260913-215504` 对话3：「如果前端不遵守约定、非要通过这种暴露的方式传，为什么不直接将口令发布到报纸上」）。

---

## 十二、可扩展点用注册式，不用 if-else

`260913-215504` 对话12：「前端上传的文件未来会有多种不同的格式，需要使用不同的解析器……需要使用设计模式，收纳规范不同格式的解析器」「**不能使用文件扩展名来判断解析器**，同样的 CSV/PDF 内容格式可能完全不同」。

落地形态（`service/expense/expense.go` + `base_csv/base_csv.go`）：

```go
type Parser interface {
    Support(ctx context.Context, data []byte) bool
    Parse(ctx context.Context, data []byte) ([]*model.Expense, error)
}

var parsers []Parser
func Register(parser Parser) { parsers = append(parsers, parser) }
```

- 子包在 `init()` 里 `expense.Register(new(Parser))`
- 上层用**空白 import** 触发注册：`_ "github.com/cellargalaxy/jotcash/service/expense/base_csv"`（`service/expense.go:13`）
- **`Support` 认内容不认后缀**，且要严格：`base_csv` 要求表头与 `columns` **完全相等**（顺序、数量都要对，`slices.Equal`），`260913-215504` 对话12 明文要求
- 解析器**只填文件里给的字段**，`Id`/`AccountingAmount`/`Version`/摊分起止月这些派生字段由 `fillExpense` 统一填

新增格式时只加子包 + 一行空白 import，不动 `Parse` 的分发逻辑。

---

## 十三、测试规范

### 13.1 包选择

**默认外部测试包 `package xxx_test`**（黑盒）。只有需要访问未导出符号时才用内部包：

| 内部包 | 因为要访问 |
| --- | --- |
| `db` | `dbLock`、`migrated`、`create`、`open`、`clearBackup` |
| `service/db` | `checkPageSize`、`checkExpenseInquiry` |
| `base_csv` | `parseExpense`、`columns` |

### 13.2 `TestMain`

每个会落盘的测试包配一个，固定形态：

```go
func TestMain(m *testing.M) {
    gin.SetMode(gin.TestMode)     // 只有 handler 需要
    logrus.SetLevel(logrus.WarnLevel)
    code := m.Run()
    os.RemoveAll("resource")      // db 的 init() 会在原始工作目录建库
    os.RemoveAll("log")
    os.Exit(code)
}
```

`db.init()` 会在测试二进制的原始工作目录建库，不清会污染代码目录（`handler/handler_test.go:28` 注释）。

### 13.3 隔离

用 `t.Chdir(t.TempDir())` 换工作目录做隔离（`newTestDb`/`newTestEngine`），不要手工造临时路径。辅助函数一律首行 `t.Helper()`。

### 13.4 断言与文案

- **手写 `if` + `t.Errorf`/`t.Fatalf`，不引入断言库**
- 致命（后续断言无意义）用 `Fatalf`，其余用 `Errorf`
- 文案中文，格式：`t.Errorf("Expense表名: got=%s want=expense", got)`、`t.Fatalf("序列化异常: %+v", err)`
- 测试里**可以**用 `if err := ...; err != nil` 内联形式（测试里 79 处），生产代码里**一处都没有**——生产代码一律先赋值再判断
- 简单多例用 `map[string]string` 描述场景 + 中文 key 做子场景名（`base_csv_test.go:24` 的 `rejects`），不引入 table-driven 的结构体切片

### 13.5 覆盖要求

从历次对话沉淀下来的必测项：

- **精度往返**：`decimal` 序列化/反序列化不丢精度（`model/expense_test.go`）
- **敏感字段不泄漏**：`String()` 里不能出现口令/文件内容（`model/claims_test.go`、`model/config_test.go`、`model/file_blob_test.go`）
- **并发**：混合读写事务、导入导出并发（`db/db_test.go` 的 `TestConcurrentTransaction`/`TestConcurrent`）
- **空条件删除**：`260913-215504` 对话10 明文要求「不允许没有任何 where 条件导致全量数据删除」
- **软删除不能变物理删除**：同上，「`util.NewDeleteHandler` 是物理删除」这条被专门纠过
- **能从 HTTP 层发起就从 HTTP 层发起**（`260913-215504` 对话1 第二问），`handler/*_test.go` 用 `httptest` + 真 `gin.Engine` 跑完整链路，降低前端联调成本
- **日志断言**：初始口令只在建库那一次进日志，测试用 `catchLog` + `findLogField` 从日志里捞（`db/db_test.go:53-70`）

---

## 十四、依赖与轮子选型

顺序是硬性的（`260913-215504` 卷首、`260913-005452` 对话2）：

1. **`github.com/cellargalaxy/go_common`** —— 业务轮子优先用它
2. **GitHub 上积极维护且 star 上百的库**
3. **真的没方案，才允许自己造轮子**

必用清单：

| 场景 | 用什么 |
| --- | --- |
| ID 生成 | `util.GenId()`（已保证不超 JS 安全整数） |
| ctx 生成/拷贝 | `util.GenCtx()` / `util.CopyCtx()` |
| ctx 存取 | `util.GetCtxValue[T]` / `util.SetCtxValue` |
| JSON/YAML 与 struct 互转 | `util.JsonStruct2Str` / `util.YamlStr2Struct` / `util.JsonStr2Struct` |
| 时间解析/格式化 | `util.ParseStr2Time(ctx, util.DateLayout_2006_01_02, s, nil)` / `util.Time2Str`（`260913-215504` 对话11 点名） |
| CSV 解析 | `util.CsvData2Strs`（同上：「为什么要手动拼接，需要复用 go_common 的 csv 函数」） |
| 文件 IO | `util.OpenReadFile` / `util.WriteData2File` / `util.WriteReader2File` / `util.RemoveFile` / `util.CreateFolderPath` / `util.ListFile` / `util.GetFileInfo` / `util.CloseIo` |
| 事务 | `util.Transaction(ctx, gormDb, handlers...)` |
| gorm 日志 | `util.NewDefaultGormLog()` |
| 批量大小 | `util.DbBatchSize` |
| 哈希 | `util.EnSha256Hex` |
| 配置热更 | `util.NewConfigService(handler)` |
| 金额 | `github.com/shopspring/decimal` |
| 币种 | `github.com/bojanz/currency`（`currency.IsValid`） |
| 加密 SQLite | `github.com/ncruces/go-sqlite3` + `vfs/adiantum` + `gormlite` |
| 错误 | `github.com/pkg/errors` |
| 日志 | `github.com/sirupsen/logrus` |

**禁止自己实现**：ID 生成器、CSV 解析、时间格式化、JSON 序列化、随机字符串以外的加解密。

---

## 十五、禁止清单（历史上被点名的反面样本）

| # | 禁止 | 出处 |
| --- | --- | --- |
| 1 | 薄封装（`newCsvFile` 那种） | `260913-215504` 对话11 |
| 2 | 把审计耦合进业务 Handler 内部 | `260913-215504` 对话10 |
| 3 | 注释引用文档条款号 | `260913-215504` 对话12 |
| 4 | 过程注释、"有的没的"注释 | `260913-005452` 对话4 |
| 5 | 凭空发明需求（"哪来的快照，为什么你要瞎编快照的功能"） | `260913-215504` 对话12 |
| 6 | 打开数据库时顺手 create | `260913-005452` 对话14 |
| 7 | 用文件扩展名选解析器 | `260913-215504` 对话12 |
| 8 | 排序值直接拼进 SQL（注入） | `260913-005452` 对话6/7 |
| 9 | 未在白名单的排序静默降级 | `db/inquiry.go:36` |
| 10 | 手工拼 CSV / 手工格式化时间 | `260913-215504` 对话11 |
| 11 | 口令进 `String()` 或进 query 参数 | `260913-215504` 对话3、`model/claims.go` |
| 12 | `model` 里塞业务逻辑或连接代码 | `260913-005452` 对话1/5 |
| 13 | 上层直接 import `jotcash/db`（测试除外） | `260913-215504` 对话1 |
| 14 | 死板直译命名（`CardLastFour`） | `260913-005452` 对话2 |
| 15 | 无 where 条件的删除 | `260913-215504` 对话10 |
| 16 | 数据库文件路径做成可配 | `260913-005452` 对话10 |
| 17 | 颠覆性重构（"严禁颠覆性改动"） | `260913-005452` 对话15 |

---

## 十六、按本规范对现有代码的体检

**结论：生产代码对本规范的遵守度接近满分，偏离只有 2 处，且都在测试代码里、都不改变行为。**

| # | 偏离 | 位置 | 性质 |
| --- | --- | --- | --- |
| 1 | `service/expense`、`service/expense/base_csv` 两个测试包**缺 `TestMain`**，跑完测试在源码树留下 `log/jotcash/log.log` | `service/expense/expense_test.go`、`service/expense/base_csv/base_csv_test.go` | 违反 §13.2。已被 `.gitignore` 挡住不会误提交，但与其余 6 个包不一致 |
| 2 | `db/db_test.go:100,167` 用 `[]interface{}`，全仓其余位置用 `any` | `db/db_test.go` | 风格不齐，无行为差异 |

**另有一条既存的功能性失败（不在本轮范围，未修）：**

```
--- FAIL: TestTokenSpecialChar (0.43s)
    backup_test.go:168: 导出的库用同一口令导不回来: 连接数据库，口令错误或数据库文件损坏
```

根因在 `db/backup.go:62-65`：备份目标库的 DSN 用 `url.Values.Encode()` 拼，**空格会被编码成 `+`**，而 SQLite 的 URI 只认 `%XX` 转义，于是含空格的口令在目标库上变成了另一把口令。`db/backup_test.go:152` 的注释把这个坑写得很清楚——**红的是 `backup.go` 不是测试**。

这条在 `260913-215504-answer.md`（对话1 补充 §四）已登记为"发现一条既存失败，不在本次范围，未修，要不要修你说"，至今未见指示。**本轮任务是梳理规范、未要求改代码，故仍未修。需要我修的话告诉我，改动面是 `db/backup.go` 一个函数。**

---

## 十七、验证与交付

| 项 | 结果 |
| --- | --- |
| `go build ./...` | 通过 |
| `gofmt -l .` | 无输出（全部已格式化） |
| `go vet ./...` | 无输出 |
| `go test ./...` | `model`/`tool`/`config`/`service/db`/`service/expense`/`service/expense/base_csv`/`handler` 全绿；`db` 包 **1 条失败**（`TestTokenSpecialChar`，既存问题，见 §十六） |
| 依赖图无环 | 已用 `go list -deps` 核对，见 §一 |
| 代码改动 | **零**。本轮只产出本文档 |
| 临时文件 | 无残留，`git status` 干净 |

### 对照 prompt 的交付自查

| prompt 要求 | 落点 |
| --- | --- |
| 阅读本仓库的全部代码 | 全部 30 个非测试 + 测试 Go 文件均已通读（共 6186 行） |
| 阅读 prompt.md | `doc/turn/` 下 33 份 prompt 全部读过任务段，`2609p2` 三份逐字读完 |
| 结合 commit 历史 | 已查：244 个 commit message 全是 `1`，不承载信息；且历史方案被 prompt 明文作废两次。结论写在卷首依据来源里 |
| 梳理代码风格 | §二、§三、§五、§六 |
| 梳理注释多寡 | §六（含密度基线 44/2511 与两类允许/四类禁止） |
| 梳理函数边界 | §四（含量化基线与两条对称红线） |
| 「等等，完整的规范标准」 | §一分层、§七并发、§八~§十二各包专项、§十三测试、§十四选型、§十五禁止清单 |
| 回答追加到 answer 末尾 | 本文即 `260914-123125-answer.md` 首轮内容，后续对话继续追加 |

---

# 对话2 · 通用编码规范（重新梳理，不限于本仓库）

> 本轮按你的订正重写。补读了 `go_common` 全部源码——它把本规范的几条核心原则实现得比业务仓库更系统，尤其是函数边界与成对设计，下文的抽象来自两个仓库的交集而非单一仓库的现状。
> 按要求：不列代码、不引用文档条款、不针对具体仓库。本文自身同样遵守它所规定的一切（自检见末节）。
> 本轮零代码改动。

---

## 一、包的层级：按"距外部的远近"排布

**唯一的排布轴是"离外部系统有多远"，不是"抽象程度"，也不是"调用先后"。**

- 与外部对接的包（配置文件、数据库、第三方接口、HTTP 入口、数据契约、工具）放在项目根部，**越外部越靠近根部**。
- 业务逻辑往内层目录收，按业务域再分子包。
- **越靠近边缘的包越通用、业务逻辑越少**；越靠内的包越专用、业务规则越密。

这条轴同时决定了依赖方向：内层可以依赖外层的适配能力，外层不得反向依赖内层的业务。依赖图必须无环，且每个包都能被单独实现与单独测试。

判断一个新包该放哪里，只问一句：**它知道多少业务规则？** 一条都不知道的，放根部；离不开业务规则的，往里放。

---

## 二、三组必须守住的包边界

这三组是同一条原则的三个切面：**通用与专用之间必须有边界，且边界要单向。**

### 2.1 通用工具 与 带规则的工具

- 完全无业务逻辑、换个项目照样能用的函数，沉到**跨项目共享库**里。
- 携带了本系统业务规则、或逻辑有本系统特殊性的工具函数，留在**项目内的工具包**。

判据是"能否原样搬到下一个项目"，不是"看起来像不像工具"。这条守住了，共享库才不会被业务污染，项目内工具包也不会膨胀成第二个共享库。

### 2.2 无校验的底层能力 与 有校验的业务门面

底层数据访问包只做无业务逻辑的增删查改，因此它**必然缺少充分的参数校验**——参数什么算合法，是业务问题，底层没有资格判断。

于是：

- 底层包**不允许被上层直接调用**。
- 由一个业务门面包做参数校验、默认值兜底、多表同事务的编排，**上层只能调门面**。
- 门面对非法输入要报错，不得静默降级。底层对没见过的取值往往有兜底行为，静默降级会让"少查了数据"或"多删了数据"变成无声故障。

底层包里也可以有安全兜底（比如强制清掉对批量删除无意义的分页参数），但那是**防呆不是校验**，不能替代门面的职责。

### 2.3 数据契约 与 行为

承载数据契约的包只放结构定义、常量枚举、以及序列化/表名这类纯声明性的方法，**不含任何业务逻辑与连接性代码**。

枚举常量与它所修饰的实体放在一起，不另设枚举包——常量脱离了它描述的实体就失去了含义。

---

## 三、命名

### 3.1 循环变量与局部变量：名字必须直接说明它是什么

用实际含义命名，不用 `one`、`it`、`v`、`e` 这类占位词。遍历操作类型的集合，变量就叫操作类型；遍历文件的集合，变量就叫文件。读代码的人不该为了知道一个变量是什么而回头去看它从哪个集合里来。

只有下标 `i`、事务句柄 `tx`、上下文 `ctx` 这类**含义由类型唯一确定**的短名例外。

### 3.2 同一概念在全仓只有一个名字

实体的一条记录、查询条件、事务处理器——每个概念在整个代码库里固定一个词，不允许同义词并存。新代码要给东西命名时，先找现有词表里有没有，有就用，没有才新造。

### 3.3 成对的功能用成对的词根

编码/解码、生成/解析、读取/写入、获取/设置、判定/设置、启动/停止、加锁/解锁、创建/打开、打开/关闭、导入/导出——成对的能力必须用**互为镜像的词根**，且参数顺序、返回形态也对称。

转换类函数用「源 2 目标」的形式命名，源和目标都是明确的形态名词。同一族转换要把形态矩阵铺满：如果支持了"从 A 读成中间形态"，就该同样支持"从中间形态写成 A"。

### 3.4 语言分工

标识符一律英文，且按语义翻译不做逐字直译；注释、日志、错误文案一律中文。领域名词以业务方认定的标准中文名为准，不允许在代码里另起一套叫法。

与语言关键字冲突时加后缀区分，不为了避让而扭曲语义。

---

## 四、函数边界

这是整套规范里最难也最重要的一节。

### 4.1 职责单一且明确

一个函数只干一件事。创建归创建，打开归打开——打开的路径上绝不能"顺手补写"，读的路径上绝不能"顺手创建"。资源不存在、已损坏、凭据不对，就是报错，不是兜底重建。

**一旦一个函数在失败时会改变外部状态，调用方就永远无法安全地重试它。**

### 4.2 边界的好坏，只能在镜像函数上检验

函数的功能与逻辑，在不少情况下都存在哲学意义上的一对：正向与反向、左链路与右链路。成对的函数内部实现会相似甚至完全镜像。

**单看一个函数看不出边界切得好不好。只有当同一套边界能同时容纳多组互为镜像的函数时，这个边界才算合理。**

具体怎么用：

- 设计一个函数时，先问"它的镜像是什么"。
- 再问"镜像函数能不能用同样形态的参数、同样的层级、同样的错误语义写出来"。
- 如果正向函数只需要少量参数、反向函数却要一大堆；或者正向留在这一层、反向被迫下沉到另一层；或者两者对同一种失败给出不同语义——**说明边界切在了错的地方，该改的是边界，不是给反向函数打补丁**。
- 一套边界如果能同时支撑三组以上的镜像关系，基本可以认定是对的。

这条也是重构的信号灯：当你发现某个反向操作"写起来特别别扭"，不要去优化那个反向函数，回头看正向函数的边界。

### 4.3 导出包装 与 非导出实现

成对出现，**中间不留空行，视觉上是一个单元**。

- **导出的那个负责可靠与安全**：加锁、读全局配置与环境、做兜底校验，对外提供无论怎么调都不会出事的入口。
- **非导出的那个负责纯逻辑**：所有依赖都由参数显式传入，不碰锁、不读全局。

这样切有两个收益，缺一不可：

1. 测试可以直接构造任意环境调用非导出实现，不必为了测一段逻辑去搭一整套全局状态。
2. **更高层的内部操作可以复用非导出实现来组合，而不会重入锁**。如果只有加锁的导出版本，任何"读了再写"的复合操作都会死锁或被迫把逻辑抄一遍。

第 2 点比第 1 点更根本。一个包里如果出现了复合操作，却没有对应的非导出纯逻辑层，那个包迟早会长出重复代码或死锁。

### 4.4 两条对称的红线

这两条本身就是一对镜像，**必须一起看**，只守一条会掉进另一条：

| 红线 | 症状 | 判据 | 处置 |
| --- | --- | --- | --- |
| **避免过度拆分** | 函数只是把几行原地代码换个名字包起来，单一调用方，内部无分支 | 内联回去之后，可读性不降反升 | 内联掉。适当平铺能提高可读性，平级的条件罗列不必强行拆 |
| **避免职责焊死** | 一个函数/处理器内部偷偷做了第二件事（典型如在业务写入里塞审计） | 想单独使用其中一件事时做不到 | 拆成各自独立的单元，由调用方**组合**成完整流程 |

两者的共同判据是**职责数**，不是行数。一个很长但只做一件事、且是平级罗列的函数是好函数；一个很短却做了两件事的函数是坏函数。

优先用组合而不是内嵌：把多个独立单元交给一个统一的执行器（事务、管道、中间件链）串起来，比把它们焊进彼此内部更解耦、更灵活，也更容易在下一个场景里换掉其中一个。

### 4.5 可扩展点用注册，不用条件分支

同一类能力会有多种实现时（多种文件格式、多种外部渠道），定义能力接口，各实现自行注册，由注册表统一分发。新增一种实现只加一个子包和一行引入，不改分发逻辑。

**识别用内容不用外壳**：文件格式不靠扩展名判断，协议不靠端口判断——外壳相同而内容不同的情况一定会出现。识别条件要严格到不会误认领，宁可不认领也不能认错。

---

## 五、错误与日志

### 5.1 唯一句式

在错误的产生点，**先记日志、再返回错误，两处文案保持一致**。

- 日志必须携带上下文对象，全链路才串得起来。
- 字段容器即使为空也保留，形态统一。
- 文案格式固定为「动作，问题」与「动作，完成」，需要携带原始错误时在其后附加。
- 全仓统一一种错误构造方式，不混用多种错误库。

动作名用中文业务词或资源名，让人不看堆栈也知道是哪一步出的事。

### 5.2 级别的判据是"代码能不能处理"，不是"事情有多严重"

| 级别 | 含义 |
| --- | --- |
| **Error** | 代码逻辑**无法兜底、无法处理**的错误情况 |
| **Warn** | 情况虽然奇怪、或需要特别提醒，但**有相应的兜底或处理逻辑** |
| **Info** | 预期之中的情况 |

所以同一件事在不同函数里可以是不同级别：一个查询在"目标不存在时返回空列表"的函数里是 Warn，在"目标不存在就没法继续"的函数里是 Error。判据永远是**这段代码接下来还能不能正常走下去**。

### 5.3 只封装来自业务逻辑外部的错误

- 错误来自**业务逻辑外部**——数据库、外部接口、文件系统、第三方库——**才封装**，加上本层的动作描述。
- 错误来自本系统内部的其他函数，**直接透传**，不再包一层。

内部函数在产生错误时已经按 5.1 记过日志、写过文案了，再包一层只会让最终呈现给调用方的信息变成一串套娃，且同一次故障会在日志里重复出现多遍。

例外只有一种：本层返回的是一个**片段式文案**，需要由调用方补上位置或批次信息才完整。这种情况下日志记在调用方，由它拼出完整文案。

---

## 六、注释

**严禁引用文档条款，只留"不看就会改错"的，不写过程注释。**

允许写的只有两类：

- **踩过的坑**：为什么必须这样写，不这样写会出什么事。这类注释保护的是下一个改这段代码的人。
- **非显然的取值语义**：某个值的特殊含义无法从类型和名字推出来时。

禁止写的：

- 文档条款号、需求编号、设计文档章节的引用——文档会变、会被重构、会改名，代码里的引用只会变成误导。要说就直接说清楚约束本身。
- 过程注释——代码自己说得清的事不重复说一遍。
- 仅为满足"导出符号要有注释"而写的门面注释。

密度上限由内容决定而不是比例决定，但一个健康的代码库里注释是**稀疏**的：几十行才有一条，且每一条都能回答"为什么"而不是"是什么"。

注释的形态在一个仓库内保持统一即可，重要的是统一，不是选了哪种。

---

## 七、测试

- **黑盒优先**：默认从包外测公开行为。只有需要触达内部状态时才用白盒。
- **非导出实现是测试的主要抓手**：按 4.3 切好边界之后，绝大部分逻辑都能在不搭建全局环境的前提下被直接调用验证。
- 每个会落盘的测试单元负责清理自己产生的残留，不污染源码树。测试之间用临时目录隔离，不共用固定路径。
- 辅助函数要声明自己是辅助函数，失败时才能报到真正的调用处。
- **失败信息要带实际值与期望值**，只说"不符"的断言在排查时等于没写。
- 必测的几类：边界与异常分支、并发路径、敏感信息不外泄、"无条件"的批量操作不会误伤全量数据、以及每一条被写进规范的不变式。
- 能从最外层入口发起的测试就从最外层发起——它顺带验证了整条链路的装配，也降低了对接方的联调成本。

---

## 八、自检：这份规范是否遵守了它自己

| 本规范的条款 | 本文的落实 |
| --- | --- |
| 越靠外越通用、越少特例（一） | 全文不绑定任何具体仓库、语言库或文件路径 |
| 职责单一（4.1） | 八节各管一件事，包层级、命名、边界、错误、注释、测试互不重叠 |
| 避免过度拆分（4.4） | 不为凑结构而切小节；平级的条款用表格平铺，不强行分层 |
| 避免职责焊死（4.4） | 每条规范单独可读、可单独引用，不依赖前文才成立 |
| 成对呈现（4.2） | 通用/专用、无校验/有校验、导出/非导出、过度拆分/职责焊死、封装/透传，全部成对给出 |
| 注释不引用文档条款（六） | 全文无 prompt 引用、无条款号、无代码路径 |
| 不写过程注释（六） | 无"接下来我们讨论"这类过场文字 |
| 命名要直接说明是什么（3.1） | 小节标题直述内容，不用代号 |
| 失败信息带判据（七） | 每条禁止项都给出可执行的判据与处置，不止于"不要这样" |

---

## 九、对上一轮的订正

| 上一轮的说法 | 本轮订正 |
| --- | --- |
| 循环变量用 `one` | **错**。用实际含义命名，读的人要能直接知道是什么 |
| "文件与包内组织顺序"是规范 | **撤销**。这一项并没有什么规范，不作要求 |
| Warn 用于"用户输入或业务校验不过" | **改判**。判据是有没有兜底逻辑，与输入来源无关 |
| Error 用于"系统/IO 层面异常" | **改判**。判据是代码能否处理，与故障所在层级无关 |
| 下层错误一律原样透传 | **收窄**。外部来源的错误要封装；内部来源的才透传 |
| 导出/非导出切分是"为了可测" | **补全**。更根本的收益是让复合操作能复用纯逻辑而不重入锁 |
| 函数边界以行数与分支深度为判据 | **深化**。真正的判据是能否容纳成对的镜像函数；行数与职责数只是表象 |

---

## 十、验证与交付

- 本轮为规范梳理，**未改动任何代码**，工作区除本文外无变更。
- 补读了共享库全部源码，其成对设计与导出/非导出分层是第四节的主要依据来源。
- 上一轮记录的既存测试失败仍未修（不在本轮范围），等你指示。
- 无临时文件残留。

---

# 对话3 · 完整编码规范（终版）

> 本轮合并前两轮的全部结论并按你的订正重写。镜像那一节是重新研读代码之后写的——你说得对，名字对称只是最表层，真正成对的是内部结构、契约、失败收尾与并发语义。
> 按前轮约定：不列代码、不引用文档条款。通用包名与设计按你的要求讲清楚。本文自身遵守它所规定的一切。
> 本轮零代码改动。

---

## 一、包：层级、名称、职责、实现顺序

### 1.1 排布轴：距外部的远近

**唯一的排布轴是"离外部系统有多远"**，不是抽象程度，也不是调用先后。

与外部对接的包放在项目根部，越外部越靠近根部；业务逻辑往内层目录收，按业务域再分子包。**越靠边缘越通用、业务逻辑越少；越靠内越专用、业务规则越密。**

依赖方向随之确定：内层可以依赖外层的适配能力，外层不得反向依赖内层的业务。依赖图无环，每个包能被单独实现、单独测试。

判断新包该放哪，只问一句：**它知道多少业务规则？** 一条都不知道的放根部，离不开业务规则的往里放。

### 1.2 同一条轴也是实现顺序

这条轴不只是空间布局，**也是动工顺序**：

- 边缘包是调用链的基础，且不含业务逻辑，所以**先实现**——它们的正确性不依赖对业务的理解，写完就能独立测试，后面的所有代码都站在它们上面。
- 深层的业务包**后设计**。等边缘铺好、对整个代码库有了把握，业务该怎么切分才看得清楚。过早设计深层包，切出来的边界大概率要推倒。

推论：如果一个项目刚起步就冒出了目录很深的业务包，通常说明设计跑在了理解前面。

### 1.3 通用包名与职责

这套包名与业务领域无关，任何项目都适用：

| 包 | 位置 | 承载 | 业务逻辑 | 动工顺序 |
| --- | --- | --- | --- | --- |
| `model` | 根部 | 数据契约：结构定义、常量枚举、序列化与表名这类声明性方法 | 无 | 最先 |
| `config` | 根部 | 配置文件的加载、解析、热更；不变的路径与地址常量 | 无 | 最先 |
| `tool` | 根部 | 带本系统规则的工具函数 | 少量规则 | 早 |
| `db` / `store` | 根部 | 数据库连接与无业务逻辑的增删查改 | 无 | 早 |
| 外部适配包（按外部系统命名，如汇率、消息、对象存储） | 根部 | 单个外部系统的对接与容错 | 无 | 早，可先留桩 |
| `handler` / `api` | 根部 | 协议入口：路由、参数绑定、响应封装、鉴权中间件 | 无 | 中 |
| `service` | 内层 | 业务编排 | 全部 | 后 |
| `service/db` | 内层 | 带业务逻辑与参数校验的数据访问：多表同事务、默认值兜底 | 全部 | 后 |
| `service/<能力>/<实现>` | 内层 | 同一能力的多种实现（多格式解析、多渠道推送） | 全部 | 最后 |

`handler` 虽在根部，却是边缘包里最晚动工的：它要等被它调用的东西都存在了才有得接。**"放在根部"说的是位置与业务浓度，不是动工的先后。**

### 1.4 三组必须单向的包边界

同一条原则的三个切面：**通用与专用之间必须有边界，且边界单向。**

**其一，通用工具 与 带规则的工具。** 完全无业务逻辑、换个项目照样能用的，沉到跨项目共享库；携带本系统规则或逻辑有本系统特殊性的，留在项目内的工具包。判据是"能否原样搬到下一个项目"，不是"看起来像不像工具"。守住这条，共享库才不会被业务污染，项目工具包也不会膨胀成第二个共享库。

**其二，无校验的底层能力 与 有校验的业务门面。** 底层数据访问包只做无业务逻辑的增删查改，因此**必然缺少充分的参数校验**——什么算合法是业务问题，底层没有资格判断。所以底层包**不允许被上层直接调用**，由业务门面做参数校验、默认值兜底、多表同事务的编排，上层只能调门面。门面对非法输入报错，不得静默降级；底层对没见过的取值往往有兜底行为，静默降级会让"少查了数据"或"多删了数据"变成无声故障。底层可以有防呆（比如强制清掉对批量删除无意义的分页参数），但防呆不是校验，替代不了门面。

**其三，数据契约 与 行为。** 契约包只放结构、枚举、声明性方法，不含业务逻辑与连接性代码。枚举常量与它所修饰的实体放在一起，不另设枚举包——常量脱离了它描述的实体就失去含义。

---

## 二、镜像：判断设计是否合理的核心方法

功能与逻辑在不少情况下都存在哲学意义上的一对：正向与反向、左链路与右链路。**名字对称只是最表层的一层；真正成对的是内部结构、契约、失败收尾与并发语义。**

**一个边界好不好，单看一个函数看不出来。只有当同一套边界能同时容纳多组互为镜像的能力时，这个边界才算合理。**

### 2.1 六个层次，由浅入深

**第一层·名称镜像。** 成对的能力用互为镜像的词根：编码/解码、生成/解析、读取/写入、获取/设置、判定/设置、启动/停止、加锁/解锁、创建/打开、打开/关闭、导入/导出。参数顺序与返回形态一并对称。这是最容易做到也最表面的一层。

**第二层·结构镜像。** 成对能力的内部步骤序列互为倒转。导出是"活资源 → 临时副本 → 流出"，导入是"流入 → 临时副本 → 活资源"——把其中一条的步骤反过来就是另一条。**中间那个共用的中转形态（临时副本）本身就是边界切对了的证据**：它是两条链路共同的原子，也让第三条链路（换凭据 = 以新凭据产出副本 + 副本顶替活资源）不用另起炉灶就能拼出来。

**第三层·契约镜像。** 一方的输出必须是另一方的合法输入。因此**验收标准是往返恒等**：导出的产物必须能被导入吃回去且内容不变，序列化的结果必须能反序列化回等价对象。往返测试是镜像对唯一充分的测试——分别测两侧都通过、合起来却对不上的情况真实存在（编码差异、转义差异、精度差异都会在往返处暴露）。

**第四层·判据镜像。** 成对的生产侧与校验侧**共用同一个判据，且由生产侧主动服从校验侧**：生成凭据的函数应当循环生成直到校验函数通过，而不是各自实现一套规则。这样两者永远不会漂移——改了校验规则，生成侧自动跟上。

同族的还有"判定能否处理"与"实际处理"：两者共用同一个判定函数，**且执行侧不得假设判定侧已经跑过**。执行侧可能被单独调用，一旦它省掉自检，调用顺序的约定就成了隐形前提，迟早被人踩破。

**第五层·副作用镜像。** 失败收尾必须互相呼应，但**呼应不等于照抄**：

- 读方向失败没有副作用可退，直接返回。
- 写方向失败必须退回调用前的状态：删掉半成品、复位标记，让重试是安全的。
- 同一个临时资源在两个方向上的**释放策略相反**：产出方向用延迟清理无条件删（产物已经流出，副本再无用处）；接收方向必须在每个失败分支显式删（成功时副本会被顶替动作消费掉，无条件的延迟清理反而会误删刚生效的资源）。

**能不能正确说出"这个方向上临时资源该怎么释放"，是检验有没有真正理解这对镜像的试金石。** 照抄另一侧的写法，正是这里出事。

**第六层·并发语义镜像。** 不改变共享资源的方向取读锁，改变的取写锁。在统一的入口骨架下，**锁的选择常常是唯一随方向变化的参数**——如果一对能力在锁上没有呈现出这种对称，通常是其中一方的副作用被低估了。

### 2.2 业务逻辑层面的镜像

镜像不止于工具函数，业务链路同样成对：

- **写与写的镜像**：录入与删除在业务上相反，在结构上却完全一致——都是"业务单元 + 审计单元放进同一个事务"。结构一致恰恰证明"业务动作 + 伴生记录"这个边界切对了。
- **读与写的镜像**：差别不在步骤而在校验对象与失败语义——写路径校验**内容**、失败要回滚；读路径校验**条件**、失败只需报错。
- **定义与取用的镜像**：列契约的定义顺序与按顺序取值的函数是一对，取用方以定义方为唯一事实源，不另行硬编码位置。
- **对外呈现与对内落库的镜像**：同一个实体面向接口的形态与面向存储的形态各有一套声明，两者都挂在契约上，不散落到使用处。
- **共用判据与专有判据的分界**：跨实体重复的判据抽出来共用，每个实体独有的判据留在各自文件里。抽多了是过度拆分，不抽是重复——**分界线是"这条判据是否依赖具体实体的语义"**。

### 2.3 怎么用这套方法

设计一个函数或一条链路时：

1. 先问"它的镜像是什么"。
2. 再问"镜像能不能用同样形态的参数、同一层级、同一错误语义写出来"。
3. 如果正向只需要少量参数而反向要一大堆；或者正向留在这一层而反向被迫下沉；或者两者对同一种失败给出不同语义——**说明边界切在了错的地方，该改的是边界，不是给反向打补丁**。
4. 一套边界如果能同时支撑三组以上的镜像关系，基本可以认定是对的。

这也是重构的信号灯：当某个反向操作"写起来特别别扭"，不要去优化那个反向函数，回头看正向函数的边界。

**反过来同样成立**：如果新增一种能力必须绕开既有骨架另起一套，先别急着承认这个能力特殊——更常见的情况是骨架的边界切错了。

---

## 三、函数边界

### 3.1 职责单一且明确

一个函数只干一件事。创建归创建，打开归打开——打开的路径上绝不"顺手补写"，读的路径上绝不"顺手创建"。资源不存在、已损坏、凭据不对，就报错，不兜底重建。

**一旦一个函数在失败时会改变外部状态，调用方就永远无法安全地重试它。**

### 3.2 导出包装 与 非导出实现

成对出现，**中间不留空行，视觉上是一个单元**。

- **导出的负责可靠与安全**：加锁、读全局配置与环境、做兜底校验，对外提供无论怎么调都不会出事的入口。
- **非导出的负责纯逻辑**：所有依赖由参数显式传入，不碰锁、不读全局。

两个收益缺一不可：

1. 测试可以直接构造任意环境调用非导出实现，不必为测一段逻辑去搭一整套全局状态。
2. **更高层的内部操作可以复用非导出实现来组合，而不重入锁**。只有加锁的导出版本时，任何"读了再写"的复合操作要么死锁，要么被迫把逻辑抄一遍。

第 2 点比第 1 点更根本。一个包里出现了复合操作却没有对应的非导出纯逻辑层，迟早会长出重复代码或死锁。

### 3.3 统一的入口骨架

同一个包里所有对外能力应当共享一套入口骨架：**脱钩上下文 → 读不变配置 → 取凭据 → 按需预热 → 按方向取锁 → 调纯逻辑实现**。骨架之间的差异只应出现在方向相关的那几个参数上（取锁类型、是否需要预热、消费凭据还是生产凭据）。

骨架统一了，新增能力就是填空；骨架一旦被绕开，说明边界需要重切。

### 3.4 两条对称的红线

这两条本身就是一对镜像，**必须一起看**，只守一条会掉进另一条：

| 红线 | 症状 | 判据 | 处置 |
| --- | --- | --- | --- |
| **避免过度拆分** | 函数只是把几行原地代码换个名字包起来，单一调用方，内部无分支 | 内联回去之后可读性不降反升 | 内联掉。适当平铺能提高可读性，平级的条件罗列不必强行拆 |
| **避免职责焊死** | 一个函数内部偷偷做了第二件事（典型如在业务写入里塞审计） | 想单独使用其中一件事时做不到 | 拆成各自独立的单元，由调用方**组合**成完整流程 |

共同判据是**职责数**，不是行数。很长但只做一件事、且是平级罗列的函数是好函数；很短却做了两件事的函数是坏函数。

优先组合而非内嵌：把多个独立单元交给统一的执行器（事务、管道、中间件链）串起来，比焊进彼此内部更解耦、更灵活，也更容易在下一个场景里换掉其中一个。

### 3.5 可扩展点用注册，不用条件分支

同一类能力有多种实现时，定义能力接口，各实现自行注册，由注册表分发。新增实现只加一个子包和一行引入，不改分发逻辑。

**识别用内容不用外壳**：文件格式不靠扩展名判断，协议不靠端口判断——外壳相同而内容不同的情况一定会出现。识别条件要严格到不会误认领，宁可不认领也不能认错。

---

## 四、命名与代码组织

### 4.1 名字必须直接说明它是什么

循环变量与局部变量用实际含义命名，不用 `one`、`it`、`v`、`e` 这类占位词。遍历操作类型的集合，变量就叫操作类型。读代码的人不该为了知道一个变量是什么而回头去看它从哪个集合里来。

只有下标、事务句柄、上下文这类**含义由类型唯一确定**的短名例外。

### 4.2 同一概念全仓只有一个名字

实体的一条记录、查询条件、事务处理器——每个概念固定一个词，不允许同义词并存。新代码命名时先查现有词表，有就用，没有才新造。

### 4.3 其余固定约定

- 转换类函数用「源 2 目标」的形式，源与目标都是明确的形态名词；同一族转换要把形态矩阵铺满。
- 方法接收器在全仓统一一个名字，不随类型变化。
- 标识符英文且按语义翻译，不逐字直译；注释、日志、错误文案中文。领域名词以业务方认定的标准中文名为准，不在代码里另起一套叫法。
- 与语言关键字冲突时加后缀区分，不为避让而扭曲语义。
- 依赖引入分标准库与其余两组，组内由格式化工具排序，不人为再分组。
- 上下文永远是第一个参数。
- 文件名与其主类型对应，枚举与实体同文件。
- 不变的路径与地址做成常量，**不开放为配置**——可配意味着要处理所有取值，而这类值根本不需要变。

文件内部的成员排列顺序不作强制要求，成对函数贴紧是唯一的硬约定。

### 4.4 并发与资源

- 一个包内的并发控制收敛到尽量少的锁上；宁可牺牲一点并发度，也不要让锁的持有关系需要推理才能看懂。
- 跨请求生命周期的上下文在交给异步组件之前必须脱钩，否则会串到下一个请求。
- 延迟初始化用双检：先在读锁下看标记，再在写锁下复检。
- 资源关闭统一走延迟释放；例外见镜像那节的第五层。

---

## 五、错误与日志

### 5.1 唯一句式

在错误的产生点，**先记日志、再返回错误，两处文案保持一致**。

- 日志必须携带上下文对象，全链路才串得起来。
- 字段容器即使为空也保留，形态统一。
- 文案固定为「动作，问题」与「动作，完成」，需要携带原始错误时在其后附加。
- 全仓统一一种错误构造方式，不混用多种错误库。

动作名用中文业务词或资源名，让人不看堆栈也知道是哪一步出的事。

### 5.2 级别的判据是"代码能不能处理"

| 级别 | 含义 |
| --- | --- |
| **Error** | 代码逻辑**无法兜底、无法处理**的错误情况 |
| **Warn** | 情况虽然奇怪、或需要特别提醒，但**有相应的兜底或处理逻辑** |
| **Info** | 预期之中的情况 |

同一件事在不同函数里可以是不同级别：一个查询在"目标不存在时返回空列表"的函数里是 Warn，在"目标不存在就没法继续"的函数里是 Error。判据永远是**这段代码接下来还能不能正常走下去**，与故障所在层级、与输入来自谁都无关。

### 5.3 只封装来自业务逻辑外部的错误

- 错误来自**业务逻辑外部**——数据库、外部接口、文件系统、第三方库——**才封装**，加上本层的动作描述。
- 错误来自本系统内部的其他函数，**直接透传**。

内部函数在产生错误时已经记过日志、写过文案，再包一层只会让最终信息变成套娃，同一次故障还会在日志里重复出现多遍。

唯一例外：本层返回的是**片段式文案**，需要调用方补上位置或批次信息才完整。这种情况下日志记在调用方，由它拼出完整文案。

### 5.4 对外的错误呈现

协议层把业务结果统一装进响应体，**传输层状态码保持成功**，业务码与文案放在体内。失败时该返回的基础信息照常返回，不因为出错就吐一个空壳。

内部错误原样透出给调用方，不二次加工——加工过的文案往往丢掉了真正有用的那一段。

---

## 六、注释

**严禁引用文档条款，只留"不看就会改错"的，不写过程注释。**

允许写的只有两类：**踩过的坑**（为什么必须这样写、不这样写会出什么事），与**非显然的取值语义**（某个值的特殊含义无法从类型和名字推出来时）。

禁止写的：

- 文档条款号、需求编号、设计文档章节的引用——文档会变、会改名，代码里的引用只会变成误导。要说就直接说清楚约束本身。
- 过程注释——代码自己说得清的不重复说一遍。
- 仅为满足"导出符号要有注释"而写的门面注释。

一个健康的代码库里注释是**稀疏**的：几十行才有一条，且每条都回答"为什么"而非"是什么"。形态（缩进、空格、位置）在一个仓库内统一即可，重要的是统一。

---

## 七、测试

- **黑盒优先**：默认从包外测公开行为，只有需要触达内部状态时才用白盒。
- **非导出实现是主要抓手**：边界切好之后，绝大部分逻辑都能在不搭建全局环境的前提下被直接调用验证。
- 每个测试单元清理自己产生的残留，不污染源码树；测试之间用临时目录隔离，不共用固定路径。
- 辅助函数要声明自己是辅助函数，失败才能报到真正的调用处。
- **失败信息带实际值与期望值**，只说"不符"的断言等于没写。
- 断言直接手写，不引入断言库；少量场景用映射描述、以场景名作键，不为几个用例套上表驱动的骨架。
- 必测：边界与异常分支、并发路径、**镜像对的往返恒等**、敏感信息不外泄、"无条件"的批量操作不会误伤全量数据，以及每一条被写进规范的不变式。
- 能从最外层入口发起的测试就从最外层发起——它顺带验证整条链路的装配，也降低对接方的联调成本。

---

## 八、选型与精度

- 轮子选型顺序硬性：**自家共享库 → 有积极维护且使用面广的成熟三方库 → 真的没有方案才自造**。ID 生成、编解码、时间格式化、序列化、通用文件与表格处理，一律不自己写。
- 标识符类型选择要考虑全链路，包括前端的数值安全范围。
- **精度敏感的字段必须显式声明存储类型**，不依赖存储引擎的类型推导——推导规则会把数值文本转成浮点而悄悄丢精度，且这种故障只在数据变大之后才显形。
- 敏感字段按"是否需要过线"选遮蔽方式：**根本不该出现在任何序列化结果里的**，在契约上直接标记为不序列化；**必须能序列化但不能进日志的**，在对外呈现方法里置空后再输出——这依赖呈现方法作用于副本而非原值。
- 排序、筛选这类会拼进查询语句的输入走**白名单映射**：对外暴露语义化的键，内部映射为真正的表达式，不在白名单内直接报错。模糊匹配必须转义通配符并声明转义符。分页缺省值由业务门面兜底，底层的"不限"语义不能直接暴露给上层。

---

## 九、自检：这份规范是否遵守了它自己

| 本规范的条款 | 本文的落实 |
| --- | --- |
| 越靠外越通用（1.1） | 包名与职责按通用形态给出，不绑定任何业务领域 |
| 边缘先行、深层后设计（1.2） | 本文自身先立包与镜像这两层地基，再谈错误、注释、测试这些细则 |
| 镜像成对呈现（二） | 通用/专用、无校验/有校验、导出/非导出、过度拆分/职责焊死、封装/透传、读锁/写锁，全部成对给出 |
| 职责单一（3.1） | 九节各管一件事，包、镜像、边界、命名、错误、注释、测试、选型互不重叠 |
| 避免过度拆分（3.4） | 不为凑结构切小节；平级条款用表格平铺，不强行分层 |
| 避免职责焊死（3.4） | 每条规范单独可读、可单独引用，不依赖前文才成立 |
| 注释不引用文档条款（六） | 全文无 prompt 引用、无条款号、无代码路径、无代码片段 |
| 不写过程注释（六） | 无"接下来我们讨论"这类过场文字 |
| 名字直接说明是什么（4.1） | 小节标题直述内容，不用代号 |
| 失败信息带判据（七） | 每条禁止项都给出可执行的判据与处置，不止于"不要这样" |

---

## 十、对前两轮的订正

| 前两轮的说法 | 本轮订正 |
| --- | --- |
| 包层级只是空间布局 | **补全**。同一条轴也是动工顺序：边缘先行，深层等对代码库有把握之后再设计 |
| 规范要抽象，故不提包名 | **订正**。通用包名与职责本身与业务无关，应当讲清楚（1.3） |
| 成对主要体现在函数命名 | **推翻**。命名只是最表层；结构、契约、判据、副作用、并发语义五层才是实质（二） |
| 镜像只存在于工具函数 | **扩展**。业务链路同样成对，写与写、读与写、定义与取用都有镜像关系（2.2） |
| 循环变量用占位词 | **错**。用实际含义命名 |
| 文件内成员排列顺序是规范 | **撤销**。不作要求，只保留成对函数贴紧这一条 |
| Warn 用于"用户输入非法"、Error 用于"系统层异常" | **改判**。判据是代码能否处理，与输入来源、故障层级都无关 |
| 下层错误一律透传 | **收窄**。外部来源的要封装，内部来源的才透传 |
| 导出/非导出切分是"为了可测" | **补全**。更根本的收益是让复合操作复用纯逻辑而不重入锁 |
| 函数边界以行数与分支深度为判据 | **深化**。真正的判据是能否容纳成对的镜像；行数与职责数只是表象 |

---

## 十一、验证与交付

- 本轮为规范梳理，**未改动任何代码**，工作区除本文外无变更。
- 镜像那一节的六个层次全部经过代码复核：结构倒转、共用中转原子、判定与执行各自独立自检、生产侧以校验侧为出口条件、临时资源在两个方向上释放策略相反、读写锁随方向对称——逐条在现有实现里找到了对应，不是推演出来的。
- 上一轮记录的既存往返失败仍未修（不在本轮范围），它恰好就是契约镜像这一层的反例，等你指示。
- 无临时文件残留。

---

# 对话4 · 非风格类 Review 要点

> 风格类要点见前三轮，本轮只列**非风格**的。为了让清单可执行，每条都给出「判据」与「本仓库的重点排查位置」，并已实测核对过——下面区分三种状态：**已确认**（实测复现或代码可直接证实）、**待确认**（有嫌疑但未验证）、**待你拍板**（不是错，是设计取舍没定）。
> 按你的要求：**本轮不改代码**，只输出清单。

---

## 〇、先看这张表：已确认的问题

| # | 问题 | 位置 | 影响 |
| --- | --- | --- | --- |
| A1 | 含空格的口令导出后导不回来 | `db/backup.go:62-65` | 换口令后整库可能再也打不开，**数据不可逆丢失** |
| A2 | 服务没有入口，跑不起来 | 全仓无 `package main` | `handler.Init` 无人调用，当前是个库不是服务 |
| A3 | 源码文件被当静态资源对外提供 | `static/static.go:5` | `/static/static.go` 可被任意客户端下载 |
| A4 | 跨币种在汇率桩期间静默按 1:1 入账 | `exchange_rate/exchange_rate.go:11` + `service/expense/expense.go:99` | 产出**看不出错的错数据**，且已入库无标记 |
| A5 | 审计三字段从未被赋值 | `model/operation_log.go` 的 `ObjectType`/`ObjectId`/`Changes` | 无业务功能的字段 |
| A6 | 四个枚举值生产侧从未产生 | `ResultFailure`/`ResultPartial`/`OperationTypeExpenseEdit`/`OperationTypeCurrencySwitch` | 同上 |
| A7 | 备份清理能力从未接线 | `db.ClearBackup` 仅测试调用 | `DbBackupLimit` 配置项当前无效 |
| A8 | `db` 层大量导出包装无生产调用 | `UpdateExpense`、`InsertFileBlob`、各 `Update*`/`Delete*` | 能力外泄，见 §七 |
| A9 | 无部署产物 | 无 `Dockerfile` / 编排文件 | 与"docker 部署"的既定形态不符 |

下面按主题展开，每条都带判据。

---

## 一、并发与锁

这个包的锁语义不直观，是最容易在 review 中漏判的一块。**先确立前提再往下看**：`dbLock` 保护的是**库文件本身**，不是库里的数据。所以"写数据"走读锁（`Transaction`/`Export`），"换文件"才走写锁（`Create`/`Import`/`ChangeToken`/`ClearBackup`）。数据级并发交给 SQLite 的 `_txlock=immediate` 与 `busy_timeout`。

| 要点 | 判据 | 排查位置 |
| --- | --- | --- |
| **1.1 锁不可重入导致死锁**（**最高危**） | 任何持 `Lock` 的路径，内部若调用了会再次取锁的**导出**函数，必然自锁 | `db/backup.go` 的 `import_`、`changeToken`、`checkImport`。当前它们用的是包内小写 `transaction` 和 `util.Transaction`（直接吃已开的连接），**是对的**；review 要确认的是这条不变式有没有被新代码破坏，以及有没有在注释里被记下来 |
| 1.2 新增能力选错锁 | 会创建/替换/删除库文件的取写锁，只读写数据的取读锁 | 新增任何 `db` 包导出函数时逐一核对 |
| 1.3 `migrated` 标记与整库替换的交互 | 库被换掉后标记若仍为 true，必须保证新库结构完整 | `db/backup.go:178` 的 `setMigrated()` 依赖 `checkImport` 已跑过 `checkSchema`+`AutoMigrate`。这是**隐含契约**，建议 review 时确认它被显式表达 |
| 1.4 双检锁的复检是否真的复检 | 第二次进临界区后必须重读标记 | `db/migrate.go:50-65`，当前正确 |
| 1.5 锁粒度过宽 | 持锁期间是否做了与被保护资源无关的事 | `db/backup.go:250` `ClearBackup` 只动备份目录却持全局写锁；`export` 持读锁期间做完整 IO 拷贝 |
| 1.6 上下文脱钩是否覆盖所有入口 | 会异步读 ctx 的调用之前必须脱钩 | `../../../rdb` `detachCtx`，核对七个导出入口是否都调了（`Create`、`ClearBackup` 不走请求链路，可不调，但要确认理由成立） |
| 1.7 包级可变状态的竞态 | 所有包级变量的读写都在锁内 | `db.migrated`、`config.conf`、`service/expense.parsers`（后者只在 init 写，之后只读——确认没有运行时注册） |

**并发 review 的具体做法**：把七个导出入口的「取锁类型 / 是否预热 / 内部调用链」列成表，逐行核对内部调用链里有没有取锁的函数。这张表列出来，1.1 和 1.2 就无处可藏。

---

## 二、事务与数据一致性

| 要点 | 判据 | 排查位置 |
| --- | --- | --- |
| 2.1 多表写入是否同事务 | 业务单元与审计单元必须在同一个事务里，一起成功或一起回滚 | `service/db/expense.go` 的 `InsertExpense`、`DeleteExpense`，当前是组装式，正确 |
| 2.2 失败操作无审计是否可接受 | 事务回滚会连审计一起回滚，失败记录只进应用日志 | 这是既定取舍，但要与 A6 一起看：`ResultFailure`/`ResultPartial` 因此永远产生不出来 |
| 2.3 乐观锁的冲突处理 | 版本不匹配必须回滚整个事务而非只跳过该行 | `db/expense.go:131-134`，当前正确 |
| 2.4 批量写入的部分成功 | 批量插入中途失败时的语义 | `util.InsertHandler` 走分批插入，确认分批与事务边界的关系 |
| 2.5 软删除不得退化为物理删除 | 删除后行仍在，按"查已删除"能捞回 | `db/expense.go:136-141` 强制 `DeletedNo` 且清分页；这条已有专门用例，保持 |
| 2.6 无条件批量操作 | 没有任何 where 条件的删除/更新会打到全表 | `service/db` 层是否对空条件报错 |
| 2.7 计数与取数的一致性 | 总数与当页数据必须来自同一快照 | `util.SelectHandler` 先 Count 再 Find，在同一事务内，正确 |

---

## 三、资源生命周期与失败回退

这一块对应前几轮说的"副作用镜像"，是 bug 密度最高的地方。

| 要点 | 判据 | 排查位置 |
| --- | --- | --- |
| 3.1 写路径失败必须退回起点 | 失败后重试是安全的，不留半成品 | `db/db.go:110-115`（建库失败删文件+复位标记）、`db/backup.go` 的 `import_`/`changeToken` 每个失败分支删临时文件 |
| 3.2 临时资源的释放策略与方向匹配 | **产出方向**无条件延迟删；**接收方向**必须逐分支显式删（成功时副本被顶替消费掉，延迟删会误删刚生效的库） | `db/backup.go:118` 用 defer（导出，正确）；`164/170/176` 与 `229/236/243` 用逐分支（导入/换口令，正确）。**新增链路时最容易在这里照抄错方向** |
| 3.3 连接与文件句柄泄漏 | 每条错误分支上都已释放 | `db/db.go:82,94` 失败时 `CloseIo(sqlDb)`；`db/backup.go:39,51` |
| 3.4 导入前是否留退路 | 整库覆盖前，原库有没有兜底副本 | `db/backup.go:157` `import_` 直接 rename 覆盖，**原库无备份**。既定取舍是"用户侧先下载"，review 时确认是否要升级为服务端自动留一份 |
| 3.5 备份目录无限增长 | 产生临时文件的链路都有清理，且清理能力已接线 | A7：`ClearBackup` 没有调用点 |
| 3.6 清理的排序依据 | 按文件名字符串排序在 ID 位数变化时会错序 | `db/backup.go:281`，当前 ID 位数稳定，属**待确认**的长期隐患 |

---

## 四、输入域与边界分支

| 要点 | 判据 | 排查位置 |
| --- | --- | --- |
| 4.1 每个可选输入的"留空"语义是否唯一 | 留空、零值、非法三者要能区分 | `service/expense/base_csv/base_csv.go` 的汇率列（空→0→上层查）、摊分月数列（空→0→上层取默认） |
| 4.2 零值与"不限"的区分 | 允许 0 与负数的区间筛选必须用指针 | `model/expense.go:78-79`，当前正确 |
| 4.3 上游校验的组合是否完备 | 逐字段合法 ≠ 组合合法 | "填了汇率必须填记账币种"这类跨字段约束，确认没有遗漏的组合 |
| 4.4 数值溢出 | 极大输入是否被上游挡住或被下游兜住 | 摊分月数无上限，靠 `service/expense/expense.go:85` 的绕回检测兜底——这是防御纵深，确认它没被"优化"掉 |
| 4.5 判定侧与执行侧各自独立自检 | 执行侧不得假设判定侧已跑过 | `base_csv.go:53` 与 `:61` 各调一次列校验，正确 |
| 4.6 上传体积无上限 | 大文件是否会打爆内存 | `service/expense.go:41` 全量读入内存；既定说法是"单批次量不大"，review 时确认是否要加硬上限 |
| 4.7 带 BOM 的 CSV 被判为无法识别 | Excel 另存的文件首列会多出不可见字符 | `base_csv.go:37-47`，**待你拍板**是否兼容 |
| 4.8 空文件 / 只有表头 | 应当明确是成功入 0 条还是报错 | `service/db/expense.go:25` 对空明细报错，与解析器"只有表头也认领"的行为要对齐 |

---

## 五、数值与时间的正确性

| 要点 | 判据 | 排查位置 | 状态 |
| --- | --- | --- | --- |
| 5.1 金额比较与排序走浮点 | 大额或高精度时边界记录会被漏查或错序 | `db/expense.go:18-19` 与 `:67-72` 的 `cast(... as real)`、`InexactFloat64()` | **待确认**：需要构造边界用例验证 |
| 5.2 记账金额不做舍入 | 乘法结果位数无上限，落库是变长文本 | `service/expense/expense.go:81` 全仓无任何 `Round`/`Truncate` | **待你拍板**：早期定过"两位小数四舍五入"，当前模型表只写了乘法式 |
| 5.3 精度敏感列的存储类型 | 必须显式声明，避免存储引擎类型推导丢精度 | `model/expense.go:32` 已声明为变长文本并注明原因，正确 |
| 5.4 时区一致性 | 解析、派生、落库、读回四处必须同一时区 | 解析走共享库默认东八区；派生月份取解析结果的时区；**落库与读回是否保真未验证** | **待确认**：建议加一条"入库再查出来，日期字面值不变"的往返用例 |
| 5.5 月份派生的边界 | 跨年、月末、负数月的处理 | `service/expense/expense.go:82-88`，起始月固定为 1 日，规避了月末问题 | 正确 |
| 5.6 汇率为 1 的两种来源要能区分 | "同币种恒为 1"与"查不到按 1 兜底"是完全不同的事 | `service/expense/expense.go:94-105` | **A4 的核心**：当前两者产出的数据无法区分 |

---

## 六、契约完整性：镜像往返

前一轮定的方法在这里直接当检查表用——**成对能力必须逐对验证往返恒等**：

| 镜像对 | 往返判据 | 状态 |
| --- | --- | --- |
| 导出 / 导入 | 导出的产物必须能被导入吃回去，数据不变 | **A1：含空格口令时断裂**（已有用例复现） |
| 加密 / 解密（口令 → 密钥） | 任何合法口令都能原样打开 | 同 A1，根因在 URI 编码而非加密 |
| 序列化 / 反序列化 | 精度与字段不丢 | 已有用例，通过 |
| 生成口令 / 校验口令 | 生成结果必然通过校验 | `tool/token.go:35` 以校验为出口条件，正确 |
| 签发凭据 / 校验凭据 | 前置条件对称（两侧都拒空密钥） | `tool/jwt.go`，正确 |
| 判定能否解析 / 实际解析 | 判定通过的必然能解析 | 正确 |
| 建库 / 校验库结构 | 自建库必然通过导入时的结构校验 | 建议补一条用例锁死 |
| 写入 / 读回 | 日期、金额、币种字面值不变 | **5.4 的缺口** |

**review 时的动作**：把上表当模板，凡是发现一个新的成对能力，就问"往返用例在哪"。没有就是缺口。

---

## 七、能力外泄与权限面

| 要点 | 判据 | 排查位置 |
| --- | --- | --- |
| 7.1 底层暴露了业务上不允许的能力 | 底层有的能力，业务上是否允许存在 | `db/operation_log.go` 提供了 `UpdateOperationLog`/`DeleteOperationLog`——**审计按定义不可改不可删**。虽然上层没调，但能力摆在那里就是隐患 |
| 7.2 包边界是否只靠自觉 | 上层能不能 import 到不该 import 的包 | 生产代码当前 100% 守住"只调 `service/db`"，但语言层面没有强制。review 时决定是否需要机制保障 |
| 7.3 无鉴权的路由面 | 每条路由的鉴权中间件是否齐全 | `handler/handler.go:30-40`，`GET /api/ping` 无 `validate`（探活需要，确认可接受） |
| 7.4 静态资源目录 | 是否把不该暴露的文件一起对外了 | **A3**：源码文件被嵌入并可下载。前端产物进来之后这个模式会持续生效 |
| 7.5 错误信息的外泄面 | 返回给前端的错误是否含路径、SQL、栈 | 多处 `errors.Errorf("...: %+v", err)` 会把底层错误原文透到响应体 |

---

## 八、"有漏"：未接线与死代码

判据统一为——**存在但无人调用的，要么接线，要么删掉，不留第三种状态**。

| 要点 | 排查位置 |
| --- | --- |
| 8.1 服务入口缺失 | **A2**：无 `main` 包，`handler.Init` 无调用者，也没有优雅关闭 |
| 8.2 部署产物缺失 | **A9**：无 Dockerfile / 编排文件 |
| 8.3 备份清理未接线 | **A7**：谁来触发、什么时机（启动时？每次产生副本后？定时？） |
| 8.4 从未被赋值的字段 | **A5**：审计的对象类型/对象ID/变更内容 |
| 8.5 从未被产生的枚举值 | **A6**：两个结果枚举 + 两个操作类型枚举 |
| 8.6 无生产调用的导出包装 | **A8**：`db` 层的 `Update*`/`Delete*`/部分 `Insert*`。注意其中 `UpdateExpense` 是**功能缺口**（明细编辑还没实现），与纯死代码要分开处理 |
| 8.7 挂起中的桩 | 汇率取数写死返回 1，挂起期间的行为要定（见 A4） |

---

## 九、安全面

| 要点 | 判据 | 排查位置 |
| --- | --- | --- |
| 9.1 口令不得进日志 | 除初始化那一次刻意打印外，任何路径都不得出现 | `model/claims.go`、`model/token.go` 的呈现方法置空；确认新增字段时这层遮蔽没被绕过 |
| 9.2 凭据只从请求头取 | 不接受 query 传参 | 既定约束，确认上游共享库的行为与约定一致 |
| 9.3 注入面 | 所有会拼进查询语句的输入都走白名单或参数化 | `db/inquiry.go` 的排序白名单、模糊匹配转义，当前正确 |
| 9.4 默认排序必须在白名单内 | 否则默认路径直接报错 | 各实体的默认排序值，新增时保持 |
| 9.5 配置热更改签名密钥的影响 | 后端口令变更会让已签发凭据全部失效 | `config/config.go:58-77`，确认这是期望行为 |
| 9.6 上传接口的滥用面 | 体积、频率、并发 | 见 4.6 |

---

## 十、测试盲区

| 盲区 | 说明 |
| --- | --- |
| 10.1 `service` 包零测试 | 编排层没有任何用例 |
| 10.2 `exchange_rate` 零测试 | 桩实现，但接口契约值得先锁住 |
| 10.3 写入→读回的字面值往返 | 见 5.4，日期与金额都缺 |
| 10.4 失败注入 | 磁盘写满、rename 失败、连接中断这类路径几乎没覆盖 |
| 10.5 既存红灯 | `db` 包 `TestTokenSpecialChar` 当前失败——**review 开始前先确认这条的处置**，否则后续任何改动都无法用"全绿"作为回归基线 |
| 10.6 两个测试包缺清理钩子 | `service/expense` 与 `service/expense/base_csv` 跑完在源码树留日志目录 |

---

## 十一、建议的 review 顺序

按"改动代价 × 发现成本"排，从低到高：

1. **先修红灯**（10.5 / A1）——没有绿色基线，后面所有改动都没法验证。
2. **再补入口**（A2 / A9）——服务跑不起来，很多问题只能靠读代码猜，跑起来之后一批问题会自己现形。
3. **然后清死代码**（§八）——删掉之后代码量下降，后续 review 的面积随之缩小。
4. **接着过并发**（§一）——把七个入口的锁与调用链列成表，一次性核完。
5. **再过往返**（§六）——按镜像对逐对补用例，每补一对就锁死一条契约。
6. **最后是取舍类**（5.2 / 4.6 / 4.7 / 3.4 / A4）——这些不是错，需要你先定口径我再落地。

---

## 十二、需要你先拍板的（否则我无法动手）

| # | 待定项 | 选项 |
| --- | --- | --- |
| P1 | 记账金额是否舍入、保留几位 | 不舍入（现状）／按币种小数位舍入／固定两位 |
| P2 | 汇率桩挂起期间，跨币种怎么办 | 维持静默 1:1（现状）／拒绝入库／入库但标记待补 |
| P3 | 导入前是否由服务端自动备份原库 | 否（现状，依赖用户先下载）／是 |
| P4 | 备份清理何时触发 | 启动时／每次产生副本后／定时／删掉这个能力 |
| P5 | 是否兼容带 BOM 的 CSV | 兼容／维持严格 |
| P6 | 上传体积是否设硬上限 | 不设（现状）／设 |
| P7 | 审计的三个空字段与四个空枚举 | 删字段／补业务把它们填上 |
| P8 | `db` 层审计的改删能力 | 删掉这两个包装／保留 |

---

## 十三、验证与交付

- 本轮**未改动任何代码**，工作区除本文外无变更。
- §〇 的九条"已确认"全部经过实测或代码直接证实：红灯用例实跑复现、嵌入文件系统实际遍历过、调用点用全仓检索逐个统计过。
- 标注为"待确认"的条目均**未**下结论，只给出了验证方法。
- 标注为"待你拍板"的条目是设计取舍，不是缺陷。
- 无临时文件残留。

---

# 对话5 · 优化修复（已改代码）

> 本轮基线：`ee5e8ac`。工作区起手时是**红灯**——`db` 包 4 条、`handler` 包 2 条用例失败，根因在上一轮把 `import_` 重构错了（详见 §二.1）。本轮先把红灯修掉，再按你逐条的指示落地。
> 本轮遵守两条硬约束：**不加新功能新接口**；**commit 里被改掉/删掉的写法一律不复活**。`checkImport`、`setMigrated`、`resetMigrate` 是 `6be7d01` 删掉的，本轮**没有**把它们请回来；`0165cee→3013128/ee5e8ac` 里被你删掉的「明细编辑 / 记账币种切换」两组用例，本轮也**没有**重新长出来（这两条直接决定了 §四 的结论）。
> 全仓 `go vet` 通过、`gofmt` 无差异、`go test ./...` 全绿，`main` 实际起服务跑通。

---

## 一、逐条处置

| # | 你的指示 | 处置 | 位置 |
| --- | --- | --- | --- |
| 1 | 口令不允许有空格 | 已在（上一轮落的），本轮复核判据与镜像关系 | `tool/token.go:48` |
| 2 | 服务入口补一下 | 已在，本轮**实跑验证**：起服务、GET 探活 200、无凭据 POST 401、初始口令只打一次、`resource/` 下建出库与配置 | `main.go:8` |
| 3 | 源码当静态资源 | 挂起，零改动 | `static/static.go` |
| 4 | 汇率桩 1:1 | 挂起，零改动 | `exchange_rate/exchange_rate.go:11` |
| 5 | 审计三字段「啥意思」 | **只解释，不改代码**，见 §四 | — |
| 6 | 四个枚举「啥意思」 | **只解释，不改代码**，见 §四 | — |
| 7 | 备份清理接线 | 挂起，零改动；但与第 14 条叠加后有后果，见 §七 | `db/backup.go:277` |
| 8 | db 层导出包装 | 维持现状，零改动 | `db/expense.go` 等 |
| 9 | 部署产物 | 挂起，零改动 | — |
| 10 | 事务先构建 handler 再一起提交 | **改了 1 处**（建库），其余盘点见 §三 | `db/db.go:101` |
| 11 | 并发与锁 | 核对 + 补 2 处预热，见 §五 | `../../../rdb` |
| 12 | 安全：没授权/越权 | **修了 2 个真问题**，见 §六 | `db/backup.go:163`、`handler/handler.go:55` |
| 13 | 记账金额固定两位 | 已在，本轮把字面量 2 提成具名常量 | `service/expense/expense.go:17,84` |
| 14 | 导入前服务端自动备份原库 | **重做**（上一轮的实现连同整个导入链路是坏的） | `db/backup.go:210` |
| 15 | 兼容带 BOM 的 CSV | 已在，复核 | `service/expense/base_csv/base_csv.go:43` |
| 16 | 上传体积设硬上限 | 已在，复核 | `handler/expense.go:18`、`handler/db.go:49` |
| 17 | db 层审计的改删能力 | 保留，零改动 | `db/operation_log.go` |

---

## 二、本轮实际改动

### 1. 导入链路：从红灯改回正确（`db/backup.go:161`）

上一轮把 `checkImport` 内联进 `import_` 时，把**校验对象搞反了**：打开的是 `dbPath`（现役库）而不是刚落盘的上传副本。连锁后果三条，全部由用例实测复现：

- **结构校验白做**：校验的是自己的现役库，上传的库是什么结构根本没看 → `TestImportWrongSchema` 红。
- **审计落错库**：审计写进了即将被顶掉的现役库，随后被整库覆盖抹掉 → `TestImportDb`「导入应记一条审计且落在新库里」红。
- **不带 handler 时直接不导入**：`len(handlers)==0` 的那条早返回吞掉了后面的 `replace`，`db.Import(ctx, reader)` 变成空操作 → `TestImportBackupOrigin` 红。

改法是把目标从 `dbPath` 换成 `backupPath`、去掉那条早返回，**保持上一轮内联的形状**（不恢复 `checkImport` 函数）。现在 `import_` 的步骤序列与 `changeToken` 完全互为镜像：落盘副本 → 在副本上校验/补表/写审计 → 顶替原库；两条链路的临时资源释放策略也一致（接收方向逐分支显式删，不用 defer）。

### 2. 导入前自动备份原库（`db/backup.go:210`）

在 `replace` 之前先 `backup(原库 → 备份目录)`。位置卡在**副本全部校验通过之后**：放在前面的话，一次失败的导入会在备份目录留下一份原库副本，`TestImportIllegal`「导入失败没清理临时文件」就会红。用例 `TestImportBackupOrigin` 把「导入后备份目录里只剩原库那一份，且能拿它把被覆盖的数据换回来」钉死了。

### 3. 建库事务解焊（`../../../rdb`、`db/db.go:126`）

`create` 原来在函数体里自己攒一条「系统初始化」审计再提交——这正是规范 3.4 点名的「在业务写入里塞审计」。本轮把审计对象与 handler 的构建上提到导出包装 `Create`，`create` 改为 `create(ctx, dbPath, token string, handlers ...util.TransactionHandler)`，只管建库、建表、跑调用方给的事务。对外行为不变（库已存在时仍然不记审计、不打印口令）。

**没做到的部分要说清楚**：审计的业务文案仍留在 `db` 包内，因为建库的唯一触发点是 `db` 包自己的 `init()`。要把文案彻底上移到 `service/db`，得先把「建库」从包初始化挪到启动流程——那是结构性改动，超出「严禁颠覆性改造」的边界，本轮不做。

### 4. 记账金额的两位小数提成常量（`service/expense/expense.go:17`）

`Round(2)` 里的 2 是业务口径不是随手取的数，按规范 4.3 做成具名常量并写明为什么必须收敛。

### 5. 凭据只认请求头（`handler/handler.go:55`）

见 §六.2。

### 6. 用例

- 新增 `TestPingQueryJwt`：凭据放 query 里必须 401。
- `newTestCtx` 跟着 `create` 的签名变化，改为自己构建审计 handler 传入——这样测试里「建库」与「建库审计」也是可分开使用的，正是解焊要的效果。
- `handler/expense_insert_test.go:131` 的 `one` 改成 `expense`。

---

## 三、事务盘点：还有哪些需要改造

你问「还有其他需要进行改造的事务吗」。全仓开事务的地方共 8 处，逐一过完，**只有建库那一处不符合**，已改：

| 位置 | 形态 | 判定 |
| --- | --- | --- |
| `db.Create` → `create` | 原先自己攒审计自己提交 | **不符合，本轮已改** |
| `db.Transaction` → `transaction` | 通用入口，handler 全由调用方传 | 符合 |
| `db.Export` → `export` | 审计事务先在原库跑完，快照再备份出去——审计因此落在导出的快照里 | 符合（顺序是必须的，不能挪） |
| `db.Import` → `import_` | 审计事务跑在上传副本上，副本随后顶替原库——审计因此落在新库里 | 符合（本轮修正了跑错库的 bug） |
| `db.ChangeToken` → `changeToken` | 审计事务跑在换好口令的副本上，副本随后顶替原库 | 符合 |
| `service/db.InsertExpense` | 4 个 handler（内容/元数据/明细/审计）组合进一个事务 | 符合，是这套思路的样板 |
| `service/db.DeleteExpense` | 2 个 handler（软删/审计）组合 | 符合 |
| `db/*.go` 的 `Insert*/Update*/Delete*/Select*` 导出包装 | 各自 `New*Handler` + `Transaction`，单 handler | 按你指示**维持现状** |

顺带确认一条不变式：`Export`/`Import`/`ChangeToken` 三条链路的审计**都落在最终生效的那份库文件里**，不是落在会被丢弃的那份。这三者互为镜像，实现结构也是镜像的。

---

## 四、审计三个空字段与四个空枚举「啥意思」

先答「啥意思」，再说为什么本轮不填。

**三个空字段**指 `model/operation_log.go` 的 `ObjectType`（操作对象类型）、`ObjectId`（对象ID）、`Changes`（变更内容）——建表建了、查询条件也支持按它们筛，但全仓**没有任何一处代码给它们赋过值**，落库永远是空串/0。

**四个空枚举**指 `OperationTypeExpenseEdit`（明细编辑）、`OperationTypeCurrencySwitch`（记账币种切换）、`ResultFailure`（失败）、`ResultPartial`（部分成功）——常量定义了、`service/db/operation_log.go` 的白名单也认它们，但**生产侧没有任何代码会产生这四个值**，只有测试构造得出来。

**为什么它们是空的，查完模型文档后的结论是：三个字段目前空着是对的，四个枚举缺的是功能。**

- `doc/decisions/记账系统功能与模型.md` §四.2 明确写了：`操作对象类型` 与 `对象ID` 的**零值语义就是「无具体对象或批量操作」**，`变更内容` 的**零值语义就是「非编辑类操作」**。现有的 5 类操作——系统初始化、数据入库、明细删除、更换口令、数据库导入/导出——要么无具体对象，要么是批量，要么非编辑类。**按文档，它们本来就该是零值**，填上反而违背定义。
- 唯一会给这三个字段赋值的是 §8.4 的第 3 类「明细编辑」（E-4，单行编辑，变更内容记前后值快照）。
- `ResultPartial` 的唯一产生点是 §F-4「记账币种切换」（文档写明「允许部分失败、可中断、重试即续跑」）；`ResultFailure` 要产生，得让审计在事务回滚之后仍然落库，等于推翻「业务单元 + 审计单元同事务」这条现有设计。

**所以「补业务把它们填上」= 实现 E-4 明细编辑 + F-4 记账币种切换两个新接口。** 这与本轮另一条指示「不要添加新功能新接口，先将当前的功能的代码进行优化」直接冲突。

**冲突的采用依据**：你在 `0165cee` 加进来的 `handler/expense_update_test.go` 与 `handler/currency_test.go`，正是这两个功能的用例，随后被你在 `3013128`、`ee5e8ac` 两个 commit 里删掉，并在同一时间段往 prompt 里补上了「不要添加新功能新接口」。时间上后者更晚、且是删除动作，按「最新一轮为准」，本轮**不实现这两个功能，三字段与四枚举保持现状**。字段与枚举都**不删**——它们是已定稿功能的占位，删了等实现时还要改数据。

**需要你确认**：如果本意就是要现在补上这两个功能，请明说，我另起一轮做；在那之前它们会一直是空的。

---

## 五、并发与锁

你的口径是「并发低一点可以接受，优先简洁直接」，所以本轮**没有**动锁的粒度（比如 `ClearBackup` 只动备份目录却持全局写锁，维持不动），只做了两件事：核对不变式、补齐入口骨架。

**核对结果**：`db` 包只有一把 `dbLock`，保护的是**库文件本身**而不是库里的数据——所以「写数据」走读锁、「换文件」走写锁。八个入口逐行核完，**持锁区内没有任何一处调用会再次取锁的函数**，不存在自锁：

| 入口 | 脱钩 ctx | 凭据 | 预热建表 | 锁 | 持锁区内是否再取锁 |
| --- | --- | --- | --- | --- | --- |
| `Create` | 否（启动期，无请求 ctx） | 生产侧现场生成 | 否（库还不存在） | 写 | 否 |
| `Open` | 是 | 消费侧取 | 是 | 读 | 否 |
| `Transaction` | 是 | 消费侧取 | 是 | 读 | 否 |
| `Export` | 是 | 消费侧取 | **是（本轮补）** | 读 | 否 |
| `Import` | 是 | 消费侧取 | 否（整库马上要换掉，预热没有意义） | 写 | 否 |
| `ChangeToken` | 是 | 消费侧取 | **是（本轮补）** | 写 | 否 |
| `ClearBackup` | 否（不走请求链路） | 不需要 | 否 | 写 | 否 |
| `CheckToken` | 经由 `Open` | 经由 `Open` | 经由 `Open` | 读 | 否 |

**补预热的理由**（`db/backup.go:96`、`:234`）：导出与换口令都要往库里写一条审计，而这两条链路原先跳过了建表预热。进程起来后第一个请求如果正好是导出或换口令、且库是旧版本导出的，审计插入会因为缺列直接失败。补上之后，除去「方向决定」的那两格（`Import` 不预热、`Create` 不预热），八个入口的骨架完全一致：脱钩 ctx → 读不变配置 → 取凭据 → 按需预热 → 按方向取锁 → 调纯逻辑实现。

**新增的非导出 `checkToken`**（`db/db.go:213`）与导出的 `CheckToken` 贴成一对：导出的走 `Open` 自带读锁，非导出的直接吃路径与口令、不碰锁，因此可以在已经持有写锁的 `import_` 里调用。这正是规范 3.2 说的「更高层的复合操作复用非导出实现而不重入锁」。

**没改但要记一笔**：六个导出包装都是 `err = 实现(...); if err != nil { return err }; return nil`，尾巴可以直接 `return 实现(...)`。全仓统一成这个写法是六处纯形式改动、零行为变化，权衡之后判定为噪音，未动。

---

## 六、安全：查出两个真问题，都已修

### 1. 越权：拿到后端口令就能顶掉整个库（`db/backup.go:163`）

**问题**：后端口令是 jwt 签名密钥。它一旦泄露，攻击者能签出合法 jwt，但因为不知道前端口令（= 库的加密口令），读写现役库都会失败。**唯独导入这条路不需要知道前端口令**——攻击者上传一份自己加密的库，jwt 里写上自己那把口令，原先的校验「上传的库能用本次请求口令打开」自然通过，`replace` 就把用户的库顶掉了。单靠后端口令泄露即可造成数据覆盖。

**修法**：`import_` 第一步先用本次请求口令去开**原库**（`checkToken`），开不了直接拒，连临时文件都不落。这样「能导入」的前提回到「确实持有现役库的口令」。用例 `TestImportForeignDb` 钉死：外来库自己能用外来口令打开，拦住它的是原库打不开，且原库与原口令一个字节不动。

叠加第 14 条的自动备份，导入这条不可逆链路现在有两道防线：进不来 + 进来了也留了底。

### 2. 口令会跟着 jwt 进日志（`handler/handler.go:55`）

**问题**：`go_common` 的 `util.ValidateGin` 在请求头取不到凭据时，会退而从 **query 参数** `Authorization` 取；而 `util.GinLog` 记录的是 `c.Request.RequestURI`，**带完整 query**。两者一叠加，只要客户端把 jwt 放在 URL 上，jwt 就原样进访问日志——jwt 的 payload 是 base64，前端口令解出来就是明文。这直接破了 `记账系统功能与模型.md` §九不变式 3 与 H-2「日志中只有初始化那一次出现口令」。同一份 URL 还会落到反向代理日志、浏览器历史、Referer 里。

**修法**：本仓库的 `validate` 中间件在调用共享库之前先要求请求头存在，不存在直接 401，query 那条路彻底走不通。改动 6 行，不碰 `go_common`。用例 `TestPingQueryJwt` 钉死。

**上游待办（不在本轮范围）**：`go_common` 的 `ValidateGin` 收 query 凭据这件事本身值得改，或者 `GinLog` 记 uri 时把 query 剥掉。这属于另一个仓库，本轮只在本仓库挡住。

### 3. 复核通过、未改动的安全面

- 全部 `/api/*` 写接口都挂了 `validate`；唯一裸奔的是 `GET /api/ping`（存活探针，`TestPingGet` 锁着），可接受。
- 排序走白名单映射、模糊匹配转义通配符并声明转义符（`db/inquiry.go`），无注入面。
- 口令不进序列化：`model/claims.go:14`、`model/token.go:11` 的呈现方法置空后再输出；`TestPingTokenNotInLog`、`TestExportDb` 分别锁着日志与导出两条外泄路径。
- 上传体积双重硬上限：明细 10MB、库文件 1GB（`config/config.go:21`）。
- `alg=none`、缺 Bearer、过期 jwt、jwt 里没有前端口令，四条已有用例都在。

---

## 七、两条必须让你知道的后果

**1. 备份目录现在只增不减。** 第 14 条（导入前自动备份原库，采纳）与第 7 条（备份清理接线，挂起）叠加的直接结果：每成功导入一次，备份目录就多留一份完整的原库副本，而 `db.ClearBackup` 至今没有调用点，`DbBackupLimit` 配置项当前无效。挂起期间请注意磁盘；等你解冻清理的触发时机，这条自动解决。

**2. 记账金额两位小数与已定稿的不变式冲突。** `记账系统功能与模型.md` §九不变式 1 写的是「`记账金额 = 支出金额 × 折算汇率` **恒成立**」，§8.1 也是这么写的。固定两位四舍五入之后，这个等式在有尾数时不再严格成立（例如 `-20.25 × 7.1234 = -144.24885`，落库 `-144.25`）。本轮按你这一轮的明确指示执行了两位，但**没有去改那份决策文档**——文档改判是你的事，不是我顺手能定的。要我同步改 §8.1 与 §九，说一声即可。

---

## 八、验证

- `go build ./...`、`go vet ./...` 通过；`gofmt -l .` 无输出。
- `go test ./...` 全绿：`config`、`db`、`handler`、`model`、`service/db`、`service/expense`、`service/expense/base_csv`、`tool` 全部 ok。本轮开工前 `db` 4 红、`handler` 2 红，现在 0 红。
- 入口实跑：编译出二进制、在临时目录起服务，GET 探活 200、无凭据 POST 返回 `{"code":401,"msg":"Authorization非法"}`、初始前端口令打印一次、`resource/` 下生成 `jotcash.db` 与 `jotcash.yaml`。验证完临时目录已删。
- 镜像往返逐对复核：导出/导入（含特殊字符口令）、建库/校验库结构、生成口令/校验口令、判定能否解析/实际解析，用例均在且绿。
- 临时资源：`TestImportIllegal`（失败留 0 个文件）、`TestImportBackupOrigin`（成功只留原库那 1 个）、`TestChangeTokenHandlerFail`（失败留 0 个）三条用例把两个方向的释放策略都钉住了。
- 工作区只有 8 个文件的改动，无临时文件、无调试代码、无新增冗余产物；按约定未提交。
