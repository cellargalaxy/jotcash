# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260917-132825-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 我发现服务的内存使用量比较高，无负载的时候也有大几十甚至上百M
+ 你需要分析是什么东西占用里这么多内存，不需要改代码

### 对话2

+ 我纠正一下
+ 服务刚刚启动起来，只有30M作用
+ 但是如果我刷新一下页面，内存会一下子上升到300M

### 对话4

+ 这个问题如何进行优化
+ 能否服务端依然限制分页大小，例如一次最多只能拉取20行
+ 前端依然需要不分页的全量数据时，前端循环拉取全部的分页，以获得全部的数据
    + 需要串行拉取，不能并发拉取，否则后端内存又会暴涨
+ 后端也需要做到，某一次请求拉取20行数据后，内存需要释放，否则多个请求后，内存累加依然占用巨大

### 对话5

+ 实施方案B：服务端流式分批查询 + 流式JSON编码
+ 代码的编辑，需要严格按照当前代码风格与结构进行编写，需要使用最小改动的写法

### 对话6

+ 改为流式之后，依然没有效果
+ 依然是务刚刚启动起来，内存只使用了30M左右，刷新一下页面，内存会一下子上升到300M，并且内存也没有被释放
+ 编写单测进行复现验证，分析原因以及优化的方案输出到answer中，但是先不要写修复代码

### 对话7

+ 去除SelectExpense的流式查询实现，回滚到原本的非流式查询
    + 非流式查询代码可读性与可维护性更高
    + 就算不分页不分批，不会有上万行数据，顶天就是上千行
    + 因此非流式查询应该不会占用多内存？
+ 方案一
    + 意思是创建一个map，key是口令，value是复用的Argon2？
    + 然后加锁读写这个map？避免前端并发请求，多个请求都创建Argon2，但是只有最后一个set到map里？
+ 方案二
    + 不采纳
+ 方案三
    + 没看懂与方案一有什么区别
+ TestSelectExpenseWithoutPageSort
    + 这个问题没看懂，但是数据查询有bug，肯定需要修复
+ 分析并整理优化的方案输出到answer中，先不要写修复代码

### 对话8

+ 实施：回滚SelectExpense到非流式查询
+ 采纳并实施：方案一
+ 修复TestSelectExpenseWithoutPageSort的问题
+ 需要编写单测，保障内存高占用的这个case，避免日后迭代给改坏了