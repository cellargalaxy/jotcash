# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260916-113226-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

+ 每轮对话的回答追加到answer文件的末尾

### 对话1

+ 继续改前端代码：[static](../../../static)
+ 继续是mock数据，先不需要联调

+ 页面里「摊分月数」需要改为「摊销月数」，也就是「摊分」不够专业严谨，叫作「摊销」才对
+ 审计页面的操作摘要是模糊搜索吗，如果是的话，需要添加小字提示支持模糊搜索
+ 文件页面的文件名是模糊搜索吗，如果是的话，需要添加小字提示支持模糊搜索

+ 需要增加对明细金额统计的图表展示
+ 这些图表你需要判断，是放在明细页面里合适，还是新增一个页面单独展示合适
+ 图表的统计范围，依然是使用条件进行筛选，筛选模块与明细页面一样即可
+ 数据的统计，需要前端来实现，后端不进行数据统计，只提供全部原始数据
    + 需要注意避免除法后，出现`.00000000001`或者`.9999999999993`的情况
    + 除不尽的，余数都放到最后一个月中
+ 因此前端需要拉取筛选条件下，全部的明细
+ 拉取筛选条件下全部的明细，这点除了图表，对于明细表格与明细下载也一样复用
+ 因此需要明细表格的分页，需要改为前端来实现，不再每个分页的数据都往后端接口里查询
+ 所展示的图表，我目前想到的起码需要包含这些，如果有别的也实用的图表，你也可以给加上
    + 第一个图表：每月支出金额与支出类型的柱状图
    + 首先这个柱状图，每个月有一个柱子
    + 柱子的金额有两个选项，一个是直接将该月的记账金额累加，另一个是将该月的摊销金额的累加，默认是摊销金额
    + 每个柱子通过不同颜色分为多段，每一段是一个支出类型
    + 标记出每个柱子的金额，每段支出类型的金额与占该月的比例

### 对话2

+ 前端代码能支持写单测吗，如果支持，前端代码也需要写单测，避免改动将逻辑改坏

### 对话3

+ 前端需要支持黑夜模式
+ 前端需要支持多语言
    + 本次只需要实现中文与英文即可
    + 默认语言通过浏览器识别，如果浏览器是中文则默认中文，其余都默认英文
    + 不仅是html的字段需要多语言，对于后端返回的错误文案，也需要多语言展示
    + 但是不改后端代码，对于后端接口传过来的文案，前端负责转为对于语言

### 对话4

+ 由于明细列表页面，统计页面，都需要改为前端分页，不使用后端接口分页
    + 因此筛选条件不能全都为空，不然就会整个数据库的数据都被拉下来里
    + 支出日期起与支出日期止，这两个筛选参数必填，默认值是过去一年
+ 解锁的页面，记账币种如果未设置过，中文语言默认是人民币，其余默认是美元
+ 对于mock的数据，切换语言后，mock的数据也需要改为英文
    + 例如明细的交易对手方/交易备注/支出类型
    + 例如操作审计的操作摘要
    + 例如文件的文件名
+ 文件列表页面，新增预览文件的功能
    + 目前只支持预览PDF/CSV/JSON格式的文件
    + 其余格式你需要想一想在UI交互上如何处理

### 对话5

+ 在解锁页面新增解锁模式
    + 一种是mock模式解锁，方便用户在未部署状态下试用
    + 另一种是真实请求后端解锁，访问后端的真实数据
+ 对接后端的接口，完成前后端联调