# 对话1 · 支出类型筛选支持查空、去除模糊搜索、明细支持按更新时间排序

## 一、依据来源

| 事实 / 契约 | 出处 / 规范 | 采用依据 |
| ----------- | ----------- | -------- |
| 支出类型查空的表达方式 | 任务 Prompt 对话1：「如果ExpenseInquiry的ExpenseType里包含空字符串的元素，则认为需要查询空支出类型」 | 明确要求以 `ExpenseType []string` 内含 `""` 元素表达"查空"，而非另建一个布尔字段 |
| 去除支出类型模糊搜索 | 任务 Prompt 对话1：「支出类型无须支持模糊搜索，去除ExpenseTypeLike字段以及相关逻辑」 | 删除 `model.ExpenseInquiry.ExpenseTypeLike` 及后端/前端全部相关逻辑 |
| 明细按更新时间排序 | 任务 Prompt 对话1：「排序，需要支持按照明细的更新时间，升序或者降序」 | 在排序白名单中补齐 `updated_at asc/desc` |
| 现状：`in` 查询天然支持空字符串命中 | [`rdb/expense.go:56-58`](file:///home/ms/document/code/jotcash/rdb/expense.go#L56-L58)：`if len(this.ExpenseType) > 0 { tx = tx.Where("expense_type in (?)", this.ExpenseType) }`；`model.Expense.ExpenseType` 是普通 `string` 列（[`model/expense.go:44`](file:///home/ms/document/code/jotcash/model/expense.go#L44)），未落库时为空字符串而非 `NULL` | 经 `rdb/expense_test.go` 实测（见下方"已确认"），`ExpenseType: []string{""}` 直接能命中 `expense_type = ''` 的行，无需改动 `Where()` 里 `ExpenseType` 这段逻辑本身，只需去掉 `ExpenseTypeLike` 分支 |
| 项目既有"空类型"措辞 | [`static/js/expense_statistic.js:13-17`](file:///home/ms/document/code/jotcash/static/js/expense_statistic.js#L13-L17)：`unfilled()` 返回 `t('未填写')`，用于统计页把支出类型/交易对手方为空的行归档；该词条已登记在 [`static/js/lang_en.js:55`](file:///home/ms/document/code/jotcash/static/js/lang_en.js#L55) | 筛选框里"查空"复选框沿用同一措辞"未填写"，与统计页对同一概念保持一致命名，不新造词 |
| 既有多选筛选框实现范式 | [`static/js/expense_inquiry.js:120-127`](file:///home/ms/document/code/jotcash/static/js/expense_inquiry.js)（改动前）：`bank_name`/`expense_currency`/`accounting_currency`/`card_last_4` 均用 `comboField` + 逗号分隔多值 + `compact()` 拆分提交 | 支出类型筛选框改造为同款多选组合框，最大化复用已有控件与提交逻辑 |

### 冲突点与处理

历史遗留（非本轮改动引入）的测试文件里存在两处与本轮 Prompt 冲突的旧设计残留，均属于"已提交但字段已被删、调用方未跟着改"的半成品状态（`go vet ./...` 改动前即报错：`unknown field ExpenseTypeEmpty`）：

1. `rdb/expense_test.go`、`handler/expense_test.go` 中存在 `ExpenseTypeEmpty: true` 用例，对应一个从未在 `model.ExpenseInquiry` 落地的布尔字段；
2. `static_test/mock_test.js`、`static_test/i18n_test.js` 中存在 `expense_type_empty: true`、`expense_type_like` 等对应旧设计的调用。

依"流程冲突时以任务 Prompt 为准"，本轮采用 Prompt 的口径（`ExpenseType` 数组含 `""` 元素表达查空），**未**新增 `ExpenseTypeEmpty` 字段，而是将这些残留测试用例改为适配新口径（详见下方"方案取舍与变更说明"），不恢复被删的 `ExpenseTypeEmpty` 设计。

---

## 二、需求分析与方案设计

### 2.1 支出类型查空（前后端）

1. **后端**：`ExpenseType []string` 走 `expense_type in (?)`，SQL 层面天然支持在集合里放入空字符串来命中 `expense_type = ''` 的行，**无需修改 `Where()` 中该分支**。唯一需要的后端改动是删除 `ExpenseTypeLike` 字段与其查询分支（见 2.2）。
2. **前端**：`expense_type` 筛选框改造为与 `bank_name` 等字段一致的"逗号分隔多选组合框"，另加一枚"未填写"复选框；提交时把复选框状态追加为数组里的一个 `''` 元素：
   ```js
   expense_type: [...compact(controls.expense_type.value.split(',')), ...(controls.expense_type_empty.checked ? [''] : [])]
   ```
   拆成"真实类型多选"与"是否查空"两个独立控件，而不是把 `''` 塞进逗号分隔文本里，是因为组合框的 `appendValue`/`compact` 两处逻辑都会把空字符串当噪声清掉（[`static/js/component.js:43`](file:///home/ms/document/code/jotcash/static/js/component.js#L43)、[`static/js/util.js:359-361`](file:///home/ms/document/code/jotcash/static/js/util.js#L359-L361)），这两个函数是多个字段共用的公共逻辑，不能只为支出类型一个字段改语义；复选框方案零touch这两处公共代码。

### 2.2 去除支出类型模糊搜索

- 删除 `model.ExpenseInquiry.ExpenseTypeLike` 字段；
- 删除 `rdb/expense.go` 中 `expense_type like ... escape '\'` 查询分支；
- 前端删除单值模糊组合框 `singleComboField('expense_type_like', ...)`，改为 2.1 所述多选+查空方案；
- 同步清理 `static/js/mock.js` 里 `likeMatch(inquiry.expense_type_like, ...)` 分支，以及不再被引用的英文词条 `'模糊匹配，也可下拉选已有'`。

### 2.3 明细按更新时间排序

- `rdb/expense.go` 的 `expenseSortMap` 白名单新增 `"updated_at asc": "updated_at asc"`、`"updated_at desc": "updated_at desc"`，与既有 `created_at`/`expense_date` 写法完全一致（`updated_at` 是普通时间列，无需像 `expense_amount` 那样转型）；
- 前端 `static/js/config.js` 的 `EXPENSE_SORTS` 白名单同步补两项，命名沿用"创建时间 · 新→旧"的措辞风格；
- `static/js/mock.js` 的 `EXPENSE_SORT_WHITELIST`/`EXPENSE_COMPARATORS` 同步补齐，`updated_at` 用已有的 `compareTime` 比较器。

---

## 三、方案取舍与变更说明

| 文件路径 | 变更类型 | 变更内容 |
| -------- | -------- | -------- |
| [`model/expense.go`](file:///home/ms/document/code/jotcash/model/expense.go) | 字段删除 | 删除 `ExpenseInquiry.ExpenseTypeLike` |
| [`rdb/expense.go`](file:///home/ms/document/code/jotcash/rdb/expense.go) | 逻辑删除 + 白名单扩充 | 1. 删除 `ExpenseTypeLike` 的 `like` 查询分支；<br>2. `expenseSortMap` 新增 `updated_at asc/desc` |
| [`rdb/expense_test.go`](file:///home/ms/document/code/jotcash/rdb/expense_test.go) | 用例调整 | 删除引用 `ExpenseTypeLike`（连带专测下划线转义的注释与用例）、`ExpenseTypeEmpty` 的两条用例；保留 `ExpenseType: []string{""}` 用例验证查空 |
| [`handler/expense_test.go`](file:///home/ms/document/code/jotcash/handler/expense_test.go) | 用例调整 | 同上，删除 `ExpenseTypeLike`、`ExpenseTypeEmpty` 两条用例，保留空切片用例 |
| [`static/js/expense_inquiry.js`](file:///home/ms/document/code/jotcash/static/js/expense_inquiry.js) | 功能实现 | 1. `newInquiry()` 去掉 `expense_type_like`；<br>2. 支出类型筛选框改为 `comboField` 多选 + "未填写"复选框，提交时拼入 `''` 元素；<br>3. 去掉不再使用的 `comboInput` 引入与 `singleComboField` 辅助函数 |
| [`static/js/config.js`](file:///home/ms/document/code/jotcash/static/js/config.js) | 白名单扩充 | `EXPENSE_SORTS` 新增"更新时间 · 新→旧/旧→新"两项 |
| [`static/js/mock.js`](file:///home/ms/document/code/jotcash/static/js/mock.js) | 逻辑删除 + 白名单扩充 | 1. 删除 `expense_type_like` 的 `likeMatch` 分支；<br>2. `EXPENSE_SORT_WHITELIST`/`EXPENSE_COMPARATORS` 新增 `updated_at` |
| [`static/js/lang_en.js`](file:///home/ms/document/code/jotcash/static/js/lang_en.js) | 词条增删 | 新增"更新时间 · 新→旧/旧→新"两条译文；删除不再被引用的"模糊匹配，也可下拉选已有" |
| [`static_test/mock_test.js`](file:///home/ms/document/code/jotcash/static_test/mock_test.js) | 用例调整 | 删除 `expense_type_empty`、`expense_type_like` 两条断言，保留 `expense_type: ['']` 查空断言与 `updated_at` 排序断言 |
| [`static_test/i18n_test.js`](file:///home/ms/document/code/jotcash/static_test/i18n_test.js) | 用例调整 | 把按 `expense_type_like` 筛选的断言改为按 `expense_type: ['Dining']` 筛选 |

---

## 四、验证说明

1. **后端编译与静态检查**：改动前 `go vet ./...` 报错（`rdb/expense_test.go:442`、`handler/expense_test.go:136` 引用不存在的 `ExpenseTypeEmpty`），属历史遗留问题；改动后 `go vet ./...` 无输出，`gofmt -l .` 无输出。
2. **后端单测**：
   - `go test ./rdb/... ./handler/... ./model/... -count=1`：全部 `ok`；`TestExpenseInquiryField`（含"支出类型为空切片"用例）与 `TestSelectExpenseFilter`/`TestSelectExpensePage`（含 `updated_at asc/desc` 排序用例）均通过，验证了空类型筛选与更新时间排序的实际效果。
   - `go test ./... -count=1`：除 `service/expense.TestParseAccountingCurrency` 因该用例会向外部汇率接口发起真实网络请求而失败外，其余全部 `ok`；该失败与本轮改动无关（未触碰任何 `service/expense` 包代码，该用例改动前同样会因外部网络因素失败）。
3. **前端单测**：`sh static_test/run.sh` 全量 189 项 `pass 189 / fail 0`，覆盖 `mock_test.js` 的筛选与排序断言、`i18n_test.js` 的词表完整性与重复键检查。
4. **格式与信息安全**：`git diff` 逐文件走查，改动均为删除旧字段/逻辑与新增排序白名单项，无敏感信息硬编码，命名与注释风格与既有代码一致（中文注释，解释"为什么"而非"是什么"）。

---

## 五、已确认与待确认事项

1. **已确认**：
   - `ExpenseType` 数组内含 `""` 元素即命中 `expense_type = ''` 的行，是 SQL `in` 查询的既有行为，本轮未改动 `rdb/expense.go` 里这段查询逻辑本身；
   - `ExpenseTypeLike` 字段与相关查询逻辑、前端模糊输入框、mock 匹配分支已全部移除；
   - 明细排序白名单新增 `updated_at asc/desc`，前后端与 mock 三处保持一致；
   - 历史遗留的 `ExpenseTypeEmpty` 相关测试引用已按本轮 Prompt 口径改写为 `ExpenseType: []string{''}`，未恢复该布尔字段设计。
2. **待确认**：无（本轮任务范围明确，均已按 Prompt 逐条落实）。
