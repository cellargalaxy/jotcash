# 执行流程

> 你是一位拥有跨学科背景的顶尖资深专家，请以深厚的专业洞察力和严谨的大师视角进行回复
> 流程冲突时以「任务 Prompt」为准
> 关键结论/方案取舍/验证结果统一记录至`260901-223131-answer.md`(`prompt.md`路径相同)

## 步骤

- 背景调研
    - 研读prompt中提及的文档/代码/配置
    - 对齐背景，梳理核心逻辑/组件依赖/改动点
- 需求分析与方案
    - 提取核心目标/交付物/范围边界/关键约束
    - 制定方案：最小改动/复用优先/任务拆解；说明模块职责/输入输出/关键逻辑/异常处理
    - 评估对现有链路/性能/兼容性/下游的副作用
    - 写入`answer.md`：依据来源/方案取舍/实施步骤/验证说明
- 实施
    - 按方案执行，命名/注释/文案与项目风格一致
    - 中文注释与日志，处理边界与异常分支
    - 同步更新类型定义/关联配置/文档
    - 复杂任务使用主agent调度子agent的方式实施
- 验证与交付
    - 执行测试/逻辑校验/数据校验/文档校对
    - 重读`prompt.md`，检查与交付结果是否一致
    - 核心产出同步至`answer.md`
    - 清理临时文件/调试代码/冗余产物，但无须提交变更

## 约束

- 依据：结论只能来自文档/代码/表结构/日志/SQL等明确来源；严厉禁止猜测业务逻辑/捏造接口字段，不能断言的内容都询问用户
- 引用：关键事实引用到具体章节/字段名/代码路径或枚举值，禁止笼统援引
- 信息不足：区分「已确认/待确认/不可推断」并标注，不得脑补
- 历史资料：历史Prompt/answer/旧方案仅供参考，必须与代码交叉核对后才能使用
- 冲突：资料冲突时列出冲突点与采用依据，不得默默取一方
- 风格：命名/注释语言/代码组织与项目既有风格一致，优先复用现有实现

# 任务 Prompt

## 背景信息

- 现状文档：
- 现状代码：
- 现状描述：
- 需求文档：
- 代码风格：
- 交付路径：

## 具体任务

+ 需要设计一套记账系统，先分析梳理业务功能，不要管技术实现，不要输出任何代码
+ 以下是我想到的零碎的功能，你需要梳理分析体系化整套系统的功能与业务流程
+ 每个功能先不要展开说，先使用简练的语言，一项一项的概括有哪些功能即可
    + 不要去翻看其他文档或者git提交记录，你需要在一个纯净的上下文里设计这个项目
+ 每轮新对话追加到anwer末尾即可

### 对话1

+ 多人账户，需要有账户密码，只有管理员才能添加新的账户，系统初始只有一个管理员
+ 需要有登录页面，除了登录业务以外，其余全部页面与接口都需要校验登录态
+ 页面与接口只能查询到用户自己的数据，不能越权
+ 需要有一个页面，能上传银行的PDF对账单，解析PDF作为支出的数据来源
    + PDF文档可能是有密码的，需要支持输入密码解密
+ 对于重复的支出数据进行去重
    + 例如重复两个PDF之间的账期时间有重叠，需要进行去重，避免重复录入
+ 支出的字段至少需要涉及：
    + 支出日期，支出金额，支出币种，交易对手方，交易备注，支出类型，摊分月数
    + 其中支出类型是需要进行打标的，是交通/餐饮/住宿之类的
    + 摊分月数代表这笔支出需要摊分多少个月，有些收益时间比较长的支出，需要将支出摊分到多个月中
    + 对于多币种如何进行统一处理？
+ 需要有一个页面，能逐笔查看支出明细，支持一些实用维度的筛选与累计统计
    + 支出日期，支出金额，支出币种，交易对手方，交易备注，支出类型，摊分月数，这些字段都需要支持修改
+ 需要有一个页面，能按月查看支出金额，支出类型
    + 能查看不经过摊分的每月支出金额，也能查看经过摊分的每月支出金额
    + 修改摊分月数之后，会影响多个月的金额累计，当查询时如何处理筛选和统计数据的范围

### 对话2

+ 我根据你的回答，我整理了一些我发看法
+ 结合我的看法，梳理分析体系化整套系统的功能与业务流程
+ 每个功能先不要展开说，先使用简练的语言，一项一项的概括有哪些功能即可
    + 不要去翻看其他文档或者git提交记录，你需要在一个纯净的上下文里设计这个项目

- A-01 系统初始化：系统首次启动时内置唯一管理员账户。
    - 管理员的账户名称写死为admin，初始密码随机生成
    - 管理员第一次登录后要求强制修改密码，当还为修改密码时，将初始密码打印在日志中
    - 第一次管理员登录，通过日志获得初始密码，登录后修改密码后，不再在日志中打印密码
- A-02 登录：账号 + 密码登录，签发登录态。
    - 登录态最长时间是12个小时，超过12个小时登录态自动过期
- A-03 登出：主动注销登录态。
- A-04 全局登录态校验：除登录页与登录接口外，所有页面与接口强制校验登录态。
- A-05 登录态失效处理：过期/失效后统一跳回登录页。
- A-06 新增账户：仅管理员可创建新账户。
    - 新账户的初始密码随机生成，在新账户刚刚创建出来的时候，会展示一次初始密码，管理员将账户与初始密码给到普通用户
    - 普通用户第一次登录后，需要强制修改密码
- A-07 账户列表：仅管理员可见，仅展示账户元信息，不含任何业务数据。
- A-08 账户启用/禁用：管理员冻结或恢复账户。
    - 无论的管理员账户还是普通账户，都不支持删除
    - 管理员能启用/禁用，除了自身账户以外的任意账户
- A-09 重置密码：管理员重置他人密码。
    - 管理员重置他人密码后，系统随机生成初始密码，会在操作重置之后展示一次初始密码，管理员将账户与初始密码给到用户
    - 用户第一次登录后，需要强制修改密码
- A-10 修改本人密码：用户自助改密。
- A-11 密码安全策略：密码加盐哈希存储、强度校验、登录失败次数限制。
    - 密码字符需要大于等于10，字母数组符号至少包含两种
    - 连续登录失败五次，禁止登录十分钟
- 系统需要支持切换多语言，本次先只实现中文，但是预期是不需要改代码，通过信息语言配置文件，即可新增语言


- B-01 数据归属：所有业务数据（账单文件、导入批次、支出明细、类型、规则、汇率偏好）均绑定归属用户。
- B-02 查询强制过滤：所有列表、详情、统计、导出一律按归属用户过滤。
- B-03 按 ID 访问的归属校验：明细编辑/删除、批次回滚、原始账单文件下载等均校验归属，防止越权。
- B-04 操作审计：记录登录、账户管理、导入、批量修改、删除等敏感操作。
- 数据存储本身是否需要加密？不然直接查询数据库导致越权

- 需要有一个单独的管理文件的模块
- 用户上传的全部文件都需要存储起来
    - 包括PDF的密码也需要存储起来
    - 文件与操作审计进行关联，以知晓该文件是在哪次操作中上传的，以及某次操作上传的是哪个文件

- 上传文件的文件的格式会多种多样，需要做成模块化
    - 可能上传的是CSV或者excel或者PDF
    - 同样是PDF.不同银行的PDF的格式也不一样
    - 本次先简单实现，只实现CSV，先不考虑PDF
- C-03 密码校验反馈：密码错误/文件损坏/非账单文件的明确提示。
    - 解析上传的文件，文件的格式不对或者其他非法情况，进行提示
- C-04 账单解析：从文件中解析出逐笔交易，作为支出数据来源。
- C-06 账期识别：识别该份账单覆盖的账期起止日期（去重与重叠判断的基础）。
    - 去除是每笔明细之间的去除，不是通过正股文件的账期，对整个文件进行去除
    - 两笔明细，如果全部维度的字段都一样，则疑似重复
    - 但是是否真的是重复，需要交给人工判断
    - 因此从文件解析出明细后，需要展示在页面，并且高亮出疑似重复的明细
    - 检查重复的范围，不仅是本身上传的文件自身明细之间的重复，还需要与已经入库的数据进行重复性检查
    - 因此页面里，还需要将已经入库的疑似重复的明细也一并展示
- C-07 解析结果核对：解析出的笔数/金额合计与账单汇总值比对，防止漏解析。
    - 这个不采纳
- C-08 入库前预览：解析结果先进入预览态，可逐条勾选、修改后再确认入库。
    - 默认全部勾选
    - 对于疑似重复的，人工将真重复的明细，去除勾选即可
- C-09 导入批次管理：每次导入生成一条批次记录（文件名、银行卡、账期、笔数、金额、状态、时间）。
    - 这里不需要有一个单独的批次记录的概念，这里复用操作审计
- C-10 批次回滚：整批撤销某次导入产生的数据。
    - 明细关联一个操作审计，如果需要回滚，则将该操作审计的全部数据进行回滚
- C-11 原始文件与原始摘要留存：保留原文件与每笔交易的原始文本，供追溯与复核
    - 复用操作审计与文件模块
- C-12 手工补录：支持手工新增单笔支出，覆盖 PDF 之外的现金/其他来源
    - 在解析完成文件将明细展示在页面后，能够在页面编辑明细的全部字段
    - 包括时间/金额/类型/摊分月数等等全部的字段
    - 此时页面点击了刷新，需要先卡住提示是否刷新页面，刷新页面这些数据都会消失
- C-13 银行卡片档案：维护「银行 + 卡片别名 + 卡号后四位 + 卡片币种」，用于标识数据来源并界定去重范围

- D-01 交易指纹：以「归属用户 + 支出日期 + 金额 + 币种」生成唯一指纹。
    - 不要「卡片，对手方，原始摘要）」这些信息不一定有，如果有的数据源有，有的数据源没有，就识别不出来了
    - 虽然有误报风险，但如上所说，会人工进行疑似重复判断
- D-02 入库前查重：确认入库前对逐笔交易做指纹比对，标记「疑似重复」。
    - 如上所说，在页面中，本文件与已入库的数据都一并展示
- D-03 账期重叠检测：新账单账期与已导入账期存在重叠时提示，并对重叠区间重点查重。
    - 不采纳，使用上文所说的人工确认
- D-04 重复处理策略：对疑似重复项支持「跳过 / 人工确认后仍导入 / 覆盖更新」。
    - 不采纳，使用上文所说的人工确认
- D-05 非重复白名单：允许把「同日同金额同商户的两笔真实消费」人工标记为非重复，避免误杀
    - 不采纳，使用上文所说的人工确认
- D-06 去重日志：记录本次被跳过的条目及判定依据，可回看
    - 不需要，不采纳

- E-01 明细字段：支出日期、支出金额、支出币种、交易对手方、交易备注、支出类型、摊分月数。
- E-02 系统字段：来源批次、来源卡片、原始摘要、本位币折算金额与汇率、创建/更新时间
- E-03 明细列表页：逐笔查看支出明细，分页展示。
- E-04 多维筛选：日期区间、金额区间、币种、对手方（模糊）、备注（模糊）、支出类型（多选）、来源批次/卡片、是否摊分、是否未打标。
    - 不采纳「是否摊分」，改为多选摊分月数，全部的明细都会有摊分月份，默认是1个月
- E-05 排序：按日期/金额等排序
- E-06 逐笔编辑：E-01 的七个字段全部可修改。
- E-07 批量编辑：批量设置支出类型、批量设置摊分月数
    - 不采纳，摊分月数默认所说2个月，需要修改摊分月数只有少数明细
- E-08 新增/删除明细：手工增删单笔支出
    - 该页面可以复用上传文件解析后的展示页面，手工增删的也需要同样的页面来进行重复性检查和编辑
- E-09 筛选态累计统计：当前筛选条件下的笔数、按币种分组金额合计、本位币折算总额。
- E-10 明细导出：导出当前筛选结果
- E-11 编辑留痕：关键字段的修改历史
    - 复用操作审计

- F-01 类型字典：维护交通/餐饮/住宿等支出类型，支持自定义增删改。
- F-02 类型层级：支持一级/二级分类
    - 不采纳，先支出一层分类即可
- F-03 默认类型：未识别的支出统一落入「未分类」
- F-04 自动打标规则：按对手方/备注关键词匹配规则，导入时自动赋类型
- F-05 规则维护与优先级：规则的增删改与命中优先级
    - 待定，这个涉及打标功能具体是如何实现的
- F-06 打标记忆：某对手方被人工改过类型后，后续同对手方自动沿用
    - 待定，这个涉及打标功能具体是如何实现的
- F-07 未分类待办：集中展示未打标支出，提供快速批量打标入口
    - 在明细列表页面新增未分类筛选即可
- F-08 类型删除保护：被引用的类型不可直接删除，需迁移或停用

- G-01 摊分月数：每笔支出可设置摊分月数，默认 1（即不摊分）。
- G-02 摊分起始月：默认以支出日期所属月为第 1 个摊分月（是否允许自定义起始月见待确认 Q4）。
    - 本次不实现自定义起始月，默认就是支出日期所属月为第 1 个摊分月
- G-03 摊分份额生成：一笔支出按摊分月数展开为 N 条「月度份额」
    - 这里的意思是会有多条数据吗，不应该有多条数据
- G-04 均分与尾差规则：金额均分，除不尽的尾差固定归入首月或末月
    - 采纳，除不尽的尾差固定归入末月
- G-05 摊分重算：修改摊分月数（或金额、日期）后，该笔支出的全部份额整体重算，旧份额作废。
- G-06 摊分预览：编辑摊分月数时，即时展示覆盖月份区间与每月分摊金额。
- G-07 跨月影响提示：保存前提示「本次修改将影响 YYYY-MM ~ YYYY-MM 的月度统计」

- H-01 币种字典：维护系统支持的币种
- H-02 本位币设置：设定统一折算的本位币
    - 用户级，默认值为USD，用户可自行修改
- H-03 汇率表维护：维护「币种 + 日期（或月份）+ 汇率」，支持手工录入与修改
    - 不采纳，预期是能自动抓起汇率，但这属于具体的实现方式，本轮对话先不要展开讨论
- H-04 入账折算：记账时按规则折算出本位币金额
- H-05 汇率快照：把折算时使用的汇率随明细一并留存，保证历史报表可复现。
- H-06 单笔汇率覆盖：允许对个别支出手工指定实际成交汇率
- H-07 缺失汇率处理：缺汇率时明确提示并列出待补清单，不静默按 1:1 处理
    - 可以复用上传文件解析后的展示页面，查询不到汇率则使得汇率变成必填项，不然无法提交
- H-08 展示口径切换：统计与列表支持「原币展示 / 本位币折算展示」切换。

- I-01 月度支出总额（未摊分口径）：按支出日期归月的每月支出金额。
- I-02 月度支出总额（摊分口径）：按摊分份额所属月份归月的每月支出金额。
- I-03 口径切换：同一页面上一键切换 I-01 / I-02 两种口径。
- I-04 月度类型分布：按支出类型看每月金额与占比，两种口径均支持。
- I-05 趋势与对比：多月趋势、环比/同比对比
- I-06 下钻明细：从某月某类型下钻到构成它的支出明细（摊分口径下展示「份额 + 来源支出」）。
- I-07 币种维度统计：按币种拆分的月度金额
- I-08 未来月份视图：展示已发生支出摊到未来月份的份额（摊分口径的自然结果，见待确认 Q7）。
- I-09 统计口径说明：页面上明示当前统计的口径与筛选语义，避免用户误读

- J-01 基础数据配置页：币种、本位币、支出类型、银行卡片、汇率的集中维护
    - 银行卡片在上传的文件中自动解析（如果有的话），不额外进行手动维护
    - 汇率自动抓起，不额外进行手动维护
- J-02 操作日志查看：查看本人的关键操作记录
- J-03 数据导出/备份：整体导出本人数据

- 议题 1：多币种如何统一处理？（对应原始诉求「对于多币种如何进行统一处理？」）
    - 采用方案 B
- 议题 2：修改摊分月数后如何处理查询的筛选与统计范围？（对应原始诉求最后一条）
    - 这个问题我提问的不合适，真正的问题是
    - 例如我查询是9月的金额，但是摊分到9月里金额，可能来源是5月/1月甚至更加久远，而这个范围难以确定

### 对话3

+ 议题 2：查某月的摊分金额，来源可能追溯到很久以前，范围如何确定？
    + 摊分月数没有上限，或者上限会非常大，实际上还是没有上限
    + 这种情况下变成需要从记账的第一天的数据开始回溯，数据量和计算成本太大，如何优化
+ 议题 3（新）：数据存储本身是否需要加密
    + 数据库中的数据都放弃加密，包括文件
    + 只加密文件，不加密明细，其实就是没加密
    + 因此假设不应该有人能直接读写数据库，只在应用层做好鉴权与越权校验即可

+ 上一轮的answer整体上没什么问题
+ 你再检查一下是否还有漏的错的，或者不同功能之间能合并简化的，梳理的整个功能要点更加简洁内聚，输出到answer中作为终版